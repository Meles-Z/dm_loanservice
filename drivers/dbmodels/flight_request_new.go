// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// FlightRequestNew is an object representing the database table.
type FlightRequestNew struct {
	ID         int               `boil:"id" json:"id" toml:"id" yaml:"id"`
	Type       string            `boil:"type" json:"type" toml:"type" yaml:"type"`
	Flights    types.StringArray `boil:"flights" json:"flights" toml:"flights" yaml:"flights"`
	FirstName  string            `boil:"first_name" json:"first_name" toml:"first_name" yaml:"first_name"`
	Surname    string            `boil:"surname" json:"surname" toml:"surname" yaml:"surname"`
	FullName   string            `boil:"full_name" json:"full_name" toml:"full_name" yaml:"full_name"`
	Email      string            `boil:"email" json:"email" toml:"email" yaml:"email"`
	Phone      null.String       `boil:"phone" json:"phone,omitempty" toml:"phone" yaml:"phone,omitempty"`
	CreatedAt  time.Time         `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt  time.Time         `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	CreatedBy  null.Int          `boil:"created_by" json:"created_by,omitempty" toml:"created_by" yaml:"created_by,omitempty"`
	UpdatedBy  null.Int          `boil:"updated_by" json:"updated_by,omitempty" toml:"updated_by" yaml:"updated_by,omitempty"`
	Status     null.Int          `boil:"status" json:"status,omitempty" toml:"status" yaml:"status,omitempty"`
	Source     null.String       `boil:"source" json:"source,omitempty" toml:"source" yaml:"source,omitempty"`
	Device     null.String       `boil:"device" json:"device,omitempty" toml:"device" yaml:"device,omitempty"`
	EmptyLegID null.String       `boil:"empty_leg_id" json:"empty_leg_id,omitempty" toml:"empty_leg_id" yaml:"empty_leg_id,omitempty"`

	R *flightRequestNewR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L flightRequestNewL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var FlightRequestNewColumns = struct {
	ID         string
	Type       string
	Flights    string
	FirstName  string
	Surname    string
	FullName   string
	Email      string
	Phone      string
	CreatedAt  string
	UpdatedAt  string
	CreatedBy  string
	UpdatedBy  string
	Status     string
	Source     string
	Device     string
	EmptyLegID string
}{
	ID:         "id",
	Type:       "type",
	Flights:    "flights",
	FirstName:  "first_name",
	Surname:    "surname",
	FullName:   "full_name",
	Email:      "email",
	Phone:      "phone",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
	CreatedBy:  "created_by",
	UpdatedBy:  "updated_by",
	Status:     "status",
	Source:     "source",
	Device:     "device",
	EmptyLegID: "empty_leg_id",
}

var FlightRequestNewTableColumns = struct {
	ID         string
	Type       string
	Flights    string
	FirstName  string
	Surname    string
	FullName   string
	Email      string
	Phone      string
	CreatedAt  string
	UpdatedAt  string
	CreatedBy  string
	UpdatedBy  string
	Status     string
	Source     string
	Device     string
	EmptyLegID string
}{
	ID:         "flight_request_new.id",
	Type:       "flight_request_new.type",
	Flights:    "flight_request_new.flights",
	FirstName:  "flight_request_new.first_name",
	Surname:    "flight_request_new.surname",
	FullName:   "flight_request_new.full_name",
	Email:      "flight_request_new.email",
	Phone:      "flight_request_new.phone",
	CreatedAt:  "flight_request_new.created_at",
	UpdatedAt:  "flight_request_new.updated_at",
	CreatedBy:  "flight_request_new.created_by",
	UpdatedBy:  "flight_request_new.updated_by",
	Status:     "flight_request_new.status",
	Source:     "flight_request_new.source",
	Device:     "flight_request_new.device",
	EmptyLegID: "flight_request_new.empty_leg_id",
}

// Generated where

type whereHelpertypes_StringArray struct{ field string }

func (w whereHelpertypes_StringArray) EQ(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertypes_StringArray) NEQ(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertypes_StringArray) LT(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_StringArray) LTE(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_StringArray) GT(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_StringArray) GTE(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var FlightRequestNewWhere = struct {
	ID         whereHelperint
	Type       whereHelperstring
	Flights    whereHelpertypes_StringArray
	FirstName  whereHelperstring
	Surname    whereHelperstring
	FullName   whereHelperstring
	Email      whereHelperstring
	Phone      whereHelpernull_String
	CreatedAt  whereHelpertime_Time
	UpdatedAt  whereHelpertime_Time
	CreatedBy  whereHelpernull_Int
	UpdatedBy  whereHelpernull_Int
	Status     whereHelpernull_Int
	Source     whereHelpernull_String
	Device     whereHelpernull_String
	EmptyLegID whereHelpernull_String
}{
	ID:         whereHelperint{field: "\"flight_request_new\".\"id\""},
	Type:       whereHelperstring{field: "\"flight_request_new\".\"type\""},
	Flights:    whereHelpertypes_StringArray{field: "\"flight_request_new\".\"flights\""},
	FirstName:  whereHelperstring{field: "\"flight_request_new\".\"first_name\""},
	Surname:    whereHelperstring{field: "\"flight_request_new\".\"surname\""},
	FullName:   whereHelperstring{field: "\"flight_request_new\".\"full_name\""},
	Email:      whereHelperstring{field: "\"flight_request_new\".\"email\""},
	Phone:      whereHelpernull_String{field: "\"flight_request_new\".\"phone\""},
	CreatedAt:  whereHelpertime_Time{field: "\"flight_request_new\".\"created_at\""},
	UpdatedAt:  whereHelpertime_Time{field: "\"flight_request_new\".\"updated_at\""},
	CreatedBy:  whereHelpernull_Int{field: "\"flight_request_new\".\"created_by\""},
	UpdatedBy:  whereHelpernull_Int{field: "\"flight_request_new\".\"updated_by\""},
	Status:     whereHelpernull_Int{field: "\"flight_request_new\".\"status\""},
	Source:     whereHelpernull_String{field: "\"flight_request_new\".\"source\""},
	Device:     whereHelpernull_String{field: "\"flight_request_new\".\"device\""},
	EmptyLegID: whereHelpernull_String{field: "\"flight_request_new\".\"empty_leg_id\""},
}

// FlightRequestNewRels is where relationship names are stored.
var FlightRequestNewRels = struct {
	FlightFlightComments      string
	FlightRequestQuoteHeaders string
}{
	FlightFlightComments:      "FlightFlightComments",
	FlightRequestQuoteHeaders: "FlightRequestQuoteHeaders",
}

// flightRequestNewR is where relationships are stored.
type flightRequestNewR struct {
	FlightFlightComments      FlightCommentSlice `boil:"FlightFlightComments" json:"FlightFlightComments" toml:"FlightFlightComments" yaml:"FlightFlightComments"`
	FlightRequestQuoteHeaders QuoteHeaderSlice   `boil:"FlightRequestQuoteHeaders" json:"FlightRequestQuoteHeaders" toml:"FlightRequestQuoteHeaders" yaml:"FlightRequestQuoteHeaders"`
}

// NewStruct creates a new relationship struct
func (*flightRequestNewR) NewStruct() *flightRequestNewR {
	return &flightRequestNewR{}
}

func (o *FlightRequestNew) GetFlightFlightComments() FlightCommentSlice {
	if o == nil {
		return nil
	}

	return o.R.GetFlightFlightComments()
}

func (r *flightRequestNewR) GetFlightFlightComments() FlightCommentSlice {
	if r == nil {
		return nil
	}

	return r.FlightFlightComments
}

func (o *FlightRequestNew) GetFlightRequestQuoteHeaders() QuoteHeaderSlice {
	if o == nil {
		return nil
	}

	return o.R.GetFlightRequestQuoteHeaders()
}

func (r *flightRequestNewR) GetFlightRequestQuoteHeaders() QuoteHeaderSlice {
	if r == nil {
		return nil
	}

	return r.FlightRequestQuoteHeaders
}

// flightRequestNewL is where Load methods for each relationship are stored.
type flightRequestNewL struct{}

var (
	flightRequestNewAllColumns            = []string{"id", "type", "flights", "first_name", "surname", "full_name", "email", "phone", "created_at", "updated_at", "created_by", "updated_by", "status", "source", "device", "empty_leg_id"}
	flightRequestNewColumnsWithoutDefault = []string{"type", "flights", "first_name", "surname", "full_name", "email", "updated_at"}
	flightRequestNewColumnsWithDefault    = []string{"id", "phone", "created_at", "created_by", "updated_by", "status", "source", "device", "empty_leg_id"}
	flightRequestNewPrimaryKeyColumns     = []string{"id"}
	flightRequestNewGeneratedColumns      = []string{}
)

type (
	// FlightRequestNewSlice is an alias for a slice of pointers to FlightRequestNew.
	// This should almost always be used instead of []FlightRequestNew.
	FlightRequestNewSlice []*FlightRequestNew
	// FlightRequestNewHook is the signature for custom FlightRequestNew hook methods
	FlightRequestNewHook func(context.Context, boil.ContextExecutor, *FlightRequestNew) error

	flightRequestNewQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	flightRequestNewType                 = reflect.TypeOf(&FlightRequestNew{})
	flightRequestNewMapping              = queries.MakeStructMapping(flightRequestNewType)
	flightRequestNewPrimaryKeyMapping, _ = queries.BindMapping(flightRequestNewType, flightRequestNewMapping, flightRequestNewPrimaryKeyColumns)
	flightRequestNewInsertCacheMut       sync.RWMutex
	flightRequestNewInsertCache          = make(map[string]insertCache)
	flightRequestNewUpdateCacheMut       sync.RWMutex
	flightRequestNewUpdateCache          = make(map[string]updateCache)
	flightRequestNewUpsertCacheMut       sync.RWMutex
	flightRequestNewUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var flightRequestNewAfterSelectMu sync.Mutex
var flightRequestNewAfterSelectHooks []FlightRequestNewHook

var flightRequestNewBeforeInsertMu sync.Mutex
var flightRequestNewBeforeInsertHooks []FlightRequestNewHook
var flightRequestNewAfterInsertMu sync.Mutex
var flightRequestNewAfterInsertHooks []FlightRequestNewHook

var flightRequestNewBeforeUpdateMu sync.Mutex
var flightRequestNewBeforeUpdateHooks []FlightRequestNewHook
var flightRequestNewAfterUpdateMu sync.Mutex
var flightRequestNewAfterUpdateHooks []FlightRequestNewHook

var flightRequestNewBeforeDeleteMu sync.Mutex
var flightRequestNewBeforeDeleteHooks []FlightRequestNewHook
var flightRequestNewAfterDeleteMu sync.Mutex
var flightRequestNewAfterDeleteHooks []FlightRequestNewHook

var flightRequestNewBeforeUpsertMu sync.Mutex
var flightRequestNewBeforeUpsertHooks []FlightRequestNewHook
var flightRequestNewAfterUpsertMu sync.Mutex
var flightRequestNewAfterUpsertHooks []FlightRequestNewHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *FlightRequestNew) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range flightRequestNewAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *FlightRequestNew) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range flightRequestNewBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *FlightRequestNew) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range flightRequestNewAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *FlightRequestNew) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range flightRequestNewBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *FlightRequestNew) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range flightRequestNewAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *FlightRequestNew) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range flightRequestNewBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *FlightRequestNew) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range flightRequestNewAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *FlightRequestNew) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range flightRequestNewBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *FlightRequestNew) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range flightRequestNewAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddFlightRequestNewHook registers your hook function for all future operations.
func AddFlightRequestNewHook(hookPoint boil.HookPoint, flightRequestNewHook FlightRequestNewHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		flightRequestNewAfterSelectMu.Lock()
		flightRequestNewAfterSelectHooks = append(flightRequestNewAfterSelectHooks, flightRequestNewHook)
		flightRequestNewAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		flightRequestNewBeforeInsertMu.Lock()
		flightRequestNewBeforeInsertHooks = append(flightRequestNewBeforeInsertHooks, flightRequestNewHook)
		flightRequestNewBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		flightRequestNewAfterInsertMu.Lock()
		flightRequestNewAfterInsertHooks = append(flightRequestNewAfterInsertHooks, flightRequestNewHook)
		flightRequestNewAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		flightRequestNewBeforeUpdateMu.Lock()
		flightRequestNewBeforeUpdateHooks = append(flightRequestNewBeforeUpdateHooks, flightRequestNewHook)
		flightRequestNewBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		flightRequestNewAfterUpdateMu.Lock()
		flightRequestNewAfterUpdateHooks = append(flightRequestNewAfterUpdateHooks, flightRequestNewHook)
		flightRequestNewAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		flightRequestNewBeforeDeleteMu.Lock()
		flightRequestNewBeforeDeleteHooks = append(flightRequestNewBeforeDeleteHooks, flightRequestNewHook)
		flightRequestNewBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		flightRequestNewAfterDeleteMu.Lock()
		flightRequestNewAfterDeleteHooks = append(flightRequestNewAfterDeleteHooks, flightRequestNewHook)
		flightRequestNewAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		flightRequestNewBeforeUpsertMu.Lock()
		flightRequestNewBeforeUpsertHooks = append(flightRequestNewBeforeUpsertHooks, flightRequestNewHook)
		flightRequestNewBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		flightRequestNewAfterUpsertMu.Lock()
		flightRequestNewAfterUpsertHooks = append(flightRequestNewAfterUpsertHooks, flightRequestNewHook)
		flightRequestNewAfterUpsertMu.Unlock()
	}
}

// One returns a single flightRequestNew record from the query.
func (q flightRequestNewQuery) One(ctx context.Context, exec boil.ContextExecutor) (*FlightRequestNew, error) {
	o := &FlightRequestNew{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for flight_request_new")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all FlightRequestNew records from the query.
func (q flightRequestNewQuery) All(ctx context.Context, exec boil.ContextExecutor) (FlightRequestNewSlice, error) {
	var o []*FlightRequestNew

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to FlightRequestNew slice")
	}

	if len(flightRequestNewAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all FlightRequestNew records in the query.
func (q flightRequestNewQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count flight_request_new rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q flightRequestNewQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if flight_request_new exists")
	}

	return count > 0, nil
}

// FlightFlightComments retrieves all the flight_comment's FlightComments with an executor via flight_id column.
func (o *FlightRequestNew) FlightFlightComments(mods ...qm.QueryMod) flightCommentQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"flight_comment\".\"flight_id\"=?", o.ID),
	)

	return FlightComments(queryMods...)
}

// FlightRequestQuoteHeaders retrieves all the quote_header's QuoteHeaders with an executor via flight_request_id column.
func (o *FlightRequestNew) FlightRequestQuoteHeaders(mods ...qm.QueryMod) quoteHeaderQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"quote_header\".\"flight_request_id\"=?", o.ID),
	)

	return QuoteHeaders(queryMods...)
}

// LoadFlightFlightComments allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (flightRequestNewL) LoadFlightFlightComments(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFlightRequestNew interface{}, mods queries.Applicator) error {
	var slice []*FlightRequestNew
	var object *FlightRequestNew

	if singular {
		var ok bool
		object, ok = maybeFlightRequestNew.(*FlightRequestNew)
		if !ok {
			object = new(FlightRequestNew)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFlightRequestNew)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFlightRequestNew))
			}
		}
	} else {
		s, ok := maybeFlightRequestNew.(*[]*FlightRequestNew)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFlightRequestNew)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFlightRequestNew))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &flightRequestNewR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &flightRequestNewR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`flight_comment`),
		qm.WhereIn(`flight_comment.flight_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load flight_comment")
	}

	var resultSlice []*FlightComment
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice flight_comment")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on flight_comment")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for flight_comment")
	}

	if len(flightCommentAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.FlightFlightComments = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &flightCommentR{}
			}
			foreign.R.Flight = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.FlightID) {
				local.R.FlightFlightComments = append(local.R.FlightFlightComments, foreign)
				if foreign.R == nil {
					foreign.R = &flightCommentR{}
				}
				foreign.R.Flight = local
				break
			}
		}
	}

	return nil
}

// LoadFlightRequestQuoteHeaders allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (flightRequestNewL) LoadFlightRequestQuoteHeaders(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFlightRequestNew interface{}, mods queries.Applicator) error {
	var slice []*FlightRequestNew
	var object *FlightRequestNew

	if singular {
		var ok bool
		object, ok = maybeFlightRequestNew.(*FlightRequestNew)
		if !ok {
			object = new(FlightRequestNew)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFlightRequestNew)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFlightRequestNew))
			}
		}
	} else {
		s, ok := maybeFlightRequestNew.(*[]*FlightRequestNew)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFlightRequestNew)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFlightRequestNew))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &flightRequestNewR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &flightRequestNewR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`quote_header`),
		qm.WhereIn(`quote_header.flight_request_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load quote_header")
	}

	var resultSlice []*QuoteHeader
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice quote_header")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on quote_header")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for quote_header")
	}

	if len(quoteHeaderAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.FlightRequestQuoteHeaders = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &quoteHeaderR{}
			}
			foreign.R.FlightRequest = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.FlightRequestID {
				local.R.FlightRequestQuoteHeaders = append(local.R.FlightRequestQuoteHeaders, foreign)
				if foreign.R == nil {
					foreign.R = &quoteHeaderR{}
				}
				foreign.R.FlightRequest = local
				break
			}
		}
	}

	return nil
}

// AddFlightFlightComments adds the given related objects to the existing relationships
// of the flight_request_new, optionally inserting them as new records.
// Appends related to o.R.FlightFlightComments.
// Sets related.R.Flight appropriately.
func (o *FlightRequestNew) AddFlightFlightComments(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FlightComment) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.FlightID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"flight_comment\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"flight_id"}),
				strmangle.WhereClause("\"", "\"", 2, flightCommentPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.FlightID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &flightRequestNewR{
			FlightFlightComments: related,
		}
	} else {
		o.R.FlightFlightComments = append(o.R.FlightFlightComments, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &flightCommentR{
				Flight: o,
			}
		} else {
			rel.R.Flight = o
		}
	}
	return nil
}

// SetFlightFlightComments removes all previously related items of the
// flight_request_new replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Flight's FlightFlightComments accordingly.
// Replaces o.R.FlightFlightComments with related.
// Sets related.R.Flight's FlightFlightComments accordingly.
func (o *FlightRequestNew) SetFlightFlightComments(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FlightComment) error {
	query := "update \"flight_comment\" set \"flight_id\" = null where \"flight_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.FlightFlightComments {
			queries.SetScanner(&rel.FlightID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Flight = nil
		}
		o.R.FlightFlightComments = nil
	}

	return o.AddFlightFlightComments(ctx, exec, insert, related...)
}

// RemoveFlightFlightComments relationships from objects passed in.
// Removes related items from R.FlightFlightComments (uses pointer comparison, removal does not keep order)
// Sets related.R.Flight.
func (o *FlightRequestNew) RemoveFlightFlightComments(ctx context.Context, exec boil.ContextExecutor, related ...*FlightComment) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.FlightID, nil)
		if rel.R != nil {
			rel.R.Flight = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("flight_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.FlightFlightComments {
			if rel != ri {
				continue
			}

			ln := len(o.R.FlightFlightComments)
			if ln > 1 && i < ln-1 {
				o.R.FlightFlightComments[i] = o.R.FlightFlightComments[ln-1]
			}
			o.R.FlightFlightComments = o.R.FlightFlightComments[:ln-1]
			break
		}
	}

	return nil
}

// AddFlightRequestQuoteHeaders adds the given related objects to the existing relationships
// of the flight_request_new, optionally inserting them as new records.
// Appends related to o.R.FlightRequestQuoteHeaders.
// Sets related.R.FlightRequest appropriately.
func (o *FlightRequestNew) AddFlightRequestQuoteHeaders(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*QuoteHeader) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.FlightRequestID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"quote_header\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"flight_request_id"}),
				strmangle.WhereClause("\"", "\"", 2, quoteHeaderPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.FlightRequestID = o.ID
		}
	}

	if o.R == nil {
		o.R = &flightRequestNewR{
			FlightRequestQuoteHeaders: related,
		}
	} else {
		o.R.FlightRequestQuoteHeaders = append(o.R.FlightRequestQuoteHeaders, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &quoteHeaderR{
				FlightRequest: o,
			}
		} else {
			rel.R.FlightRequest = o
		}
	}
	return nil
}

// FlightRequestNews retrieves all the records using an executor.
func FlightRequestNews(mods ...qm.QueryMod) flightRequestNewQuery {
	mods = append(mods, qm.From("\"flight_request_new\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"flight_request_new\".*"})
	}

	return flightRequestNewQuery{q}
}

// FindFlightRequestNew retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindFlightRequestNew(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*FlightRequestNew, error) {
	flightRequestNewObj := &FlightRequestNew{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"flight_request_new\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, flightRequestNewObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from flight_request_new")
	}

	if err = flightRequestNewObj.doAfterSelectHooks(ctx, exec); err != nil {
		return flightRequestNewObj, err
	}

	return flightRequestNewObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *FlightRequestNew) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no flight_request_new provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(flightRequestNewColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	flightRequestNewInsertCacheMut.RLock()
	cache, cached := flightRequestNewInsertCache[key]
	flightRequestNewInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			flightRequestNewAllColumns,
			flightRequestNewColumnsWithDefault,
			flightRequestNewColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(flightRequestNewType, flightRequestNewMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(flightRequestNewType, flightRequestNewMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"flight_request_new\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"flight_request_new\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into flight_request_new")
	}

	if !cached {
		flightRequestNewInsertCacheMut.Lock()
		flightRequestNewInsertCache[key] = cache
		flightRequestNewInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the FlightRequestNew.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *FlightRequestNew) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	flightRequestNewUpdateCacheMut.RLock()
	cache, cached := flightRequestNewUpdateCache[key]
	flightRequestNewUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			flightRequestNewAllColumns,
			flightRequestNewPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update flight_request_new, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"flight_request_new\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, flightRequestNewPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(flightRequestNewType, flightRequestNewMapping, append(wl, flightRequestNewPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update flight_request_new row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for flight_request_new")
	}

	if !cached {
		flightRequestNewUpdateCacheMut.Lock()
		flightRequestNewUpdateCache[key] = cache
		flightRequestNewUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q flightRequestNewQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for flight_request_new")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for flight_request_new")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o FlightRequestNewSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), flightRequestNewPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"flight_request_new\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, flightRequestNewPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in flightRequestNew slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all flightRequestNew")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *FlightRequestNew) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no flight_request_new provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(flightRequestNewColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	flightRequestNewUpsertCacheMut.RLock()
	cache, cached := flightRequestNewUpsertCache[key]
	flightRequestNewUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			flightRequestNewAllColumns,
			flightRequestNewColumnsWithDefault,
			flightRequestNewColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			flightRequestNewAllColumns,
			flightRequestNewPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert flight_request_new, could not build update column list")
		}

		ret := strmangle.SetComplement(flightRequestNewAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(flightRequestNewPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert flight_request_new, could not build conflict column list")
			}

			conflict = make([]string, len(flightRequestNewPrimaryKeyColumns))
			copy(conflict, flightRequestNewPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"flight_request_new\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(flightRequestNewType, flightRequestNewMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(flightRequestNewType, flightRequestNewMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert flight_request_new")
	}

	if !cached {
		flightRequestNewUpsertCacheMut.Lock()
		flightRequestNewUpsertCache[key] = cache
		flightRequestNewUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single FlightRequestNew record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *FlightRequestNew) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no FlightRequestNew provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), flightRequestNewPrimaryKeyMapping)
	sql := "DELETE FROM \"flight_request_new\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from flight_request_new")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for flight_request_new")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q flightRequestNewQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no flightRequestNewQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from flight_request_new")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for flight_request_new")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o FlightRequestNewSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(flightRequestNewBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), flightRequestNewPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"flight_request_new\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, flightRequestNewPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from flightRequestNew slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for flight_request_new")
	}

	if len(flightRequestNewAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *FlightRequestNew) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindFlightRequestNew(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *FlightRequestNewSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := FlightRequestNewSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), flightRequestNewPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"flight_request_new\".* FROM \"flight_request_new\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, flightRequestNewPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in FlightRequestNewSlice")
	}

	*o = slice

	return nil
}

// FlightRequestNewExists checks if the FlightRequestNew row exists.
func FlightRequestNewExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"flight_request_new\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if flight_request_new exists")
	}

	return exists, nil
}

// Exists checks if the FlightRequestNew row exists.
func (o *FlightRequestNew) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return FlightRequestNewExists(ctx, exec, o.ID)
}
