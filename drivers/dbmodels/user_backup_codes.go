// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// UserBackupCode is an object representing the database table.
type UserBackupCode struct {
	ID        int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID    int       `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Code      string    `boil:"code" json:"code" toml:"code" yaml:"code"`
	IsUsed    null.Bool `boil:"is_used" json:"is_used,omitempty" toml:"is_used" yaml:"is_used,omitempty"`
	UsedAt    null.Time `boil:"used_at" json:"used_at,omitempty" toml:"used_at" yaml:"used_at,omitempty"`
	CreatedAt null.Time `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`

	R *userBackupCodeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userBackupCodeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserBackupCodeColumns = struct {
	ID        string
	UserID    string
	Code      string
	IsUsed    string
	UsedAt    string
	CreatedAt string
}{
	ID:        "id",
	UserID:    "user_id",
	Code:      "code",
	IsUsed:    "is_used",
	UsedAt:    "used_at",
	CreatedAt: "created_at",
}

var UserBackupCodeTableColumns = struct {
	ID        string
	UserID    string
	Code      string
	IsUsed    string
	UsedAt    string
	CreatedAt string
}{
	ID:        "user_backup_codes.id",
	UserID:    "user_backup_codes.user_id",
	Code:      "user_backup_codes.code",
	IsUsed:    "user_backup_codes.is_used",
	UsedAt:    "user_backup_codes.used_at",
	CreatedAt: "user_backup_codes.created_at",
}

// Generated where

var UserBackupCodeWhere = struct {
	ID        whereHelperint
	UserID    whereHelperint
	Code      whereHelperstring
	IsUsed    whereHelpernull_Bool
	UsedAt    whereHelpernull_Time
	CreatedAt whereHelpernull_Time
}{
	ID:        whereHelperint{field: "\"user_backup_codes\".\"id\""},
	UserID:    whereHelperint{field: "\"user_backup_codes\".\"user_id\""},
	Code:      whereHelperstring{field: "\"user_backup_codes\".\"code\""},
	IsUsed:    whereHelpernull_Bool{field: "\"user_backup_codes\".\"is_used\""},
	UsedAt:    whereHelpernull_Time{field: "\"user_backup_codes\".\"used_at\""},
	CreatedAt: whereHelpernull_Time{field: "\"user_backup_codes\".\"created_at\""},
}

// UserBackupCodeRels is where relationship names are stored.
var UserBackupCodeRels = struct {
	User string
}{
	User: "User",
}

// userBackupCodeR is where relationships are stored.
type userBackupCodeR struct {
	User *User `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*userBackupCodeR) NewStruct() *userBackupCodeR {
	return &userBackupCodeR{}
}

func (o *UserBackupCode) GetUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetUser()
}

func (r *userBackupCodeR) GetUser() *User {
	if r == nil {
		return nil
	}

	return r.User
}

// userBackupCodeL is where Load methods for each relationship are stored.
type userBackupCodeL struct{}

var (
	userBackupCodeAllColumns            = []string{"id", "user_id", "code", "is_used", "used_at", "created_at"}
	userBackupCodeColumnsWithoutDefault = []string{"user_id", "code"}
	userBackupCodeColumnsWithDefault    = []string{"id", "is_used", "used_at", "created_at"}
	userBackupCodePrimaryKeyColumns     = []string{"id"}
	userBackupCodeGeneratedColumns      = []string{}
)

type (
	// UserBackupCodeSlice is an alias for a slice of pointers to UserBackupCode.
	// This should almost always be used instead of []UserBackupCode.
	UserBackupCodeSlice []*UserBackupCode
	// UserBackupCodeHook is the signature for custom UserBackupCode hook methods
	UserBackupCodeHook func(context.Context, boil.ContextExecutor, *UserBackupCode) error

	userBackupCodeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userBackupCodeType                 = reflect.TypeOf(&UserBackupCode{})
	userBackupCodeMapping              = queries.MakeStructMapping(userBackupCodeType)
	userBackupCodePrimaryKeyMapping, _ = queries.BindMapping(userBackupCodeType, userBackupCodeMapping, userBackupCodePrimaryKeyColumns)
	userBackupCodeInsertCacheMut       sync.RWMutex
	userBackupCodeInsertCache          = make(map[string]insertCache)
	userBackupCodeUpdateCacheMut       sync.RWMutex
	userBackupCodeUpdateCache          = make(map[string]updateCache)
	userBackupCodeUpsertCacheMut       sync.RWMutex
	userBackupCodeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var userBackupCodeAfterSelectMu sync.Mutex
var userBackupCodeAfterSelectHooks []UserBackupCodeHook

var userBackupCodeBeforeInsertMu sync.Mutex
var userBackupCodeBeforeInsertHooks []UserBackupCodeHook
var userBackupCodeAfterInsertMu sync.Mutex
var userBackupCodeAfterInsertHooks []UserBackupCodeHook

var userBackupCodeBeforeUpdateMu sync.Mutex
var userBackupCodeBeforeUpdateHooks []UserBackupCodeHook
var userBackupCodeAfterUpdateMu sync.Mutex
var userBackupCodeAfterUpdateHooks []UserBackupCodeHook

var userBackupCodeBeforeDeleteMu sync.Mutex
var userBackupCodeBeforeDeleteHooks []UserBackupCodeHook
var userBackupCodeAfterDeleteMu sync.Mutex
var userBackupCodeAfterDeleteHooks []UserBackupCodeHook

var userBackupCodeBeforeUpsertMu sync.Mutex
var userBackupCodeBeforeUpsertHooks []UserBackupCodeHook
var userBackupCodeAfterUpsertMu sync.Mutex
var userBackupCodeAfterUpsertHooks []UserBackupCodeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UserBackupCode) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userBackupCodeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UserBackupCode) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userBackupCodeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UserBackupCode) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userBackupCodeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UserBackupCode) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userBackupCodeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UserBackupCode) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userBackupCodeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UserBackupCode) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userBackupCodeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UserBackupCode) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userBackupCodeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UserBackupCode) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userBackupCodeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UserBackupCode) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userBackupCodeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUserBackupCodeHook registers your hook function for all future operations.
func AddUserBackupCodeHook(hookPoint boil.HookPoint, userBackupCodeHook UserBackupCodeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		userBackupCodeAfterSelectMu.Lock()
		userBackupCodeAfterSelectHooks = append(userBackupCodeAfterSelectHooks, userBackupCodeHook)
		userBackupCodeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		userBackupCodeBeforeInsertMu.Lock()
		userBackupCodeBeforeInsertHooks = append(userBackupCodeBeforeInsertHooks, userBackupCodeHook)
		userBackupCodeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		userBackupCodeAfterInsertMu.Lock()
		userBackupCodeAfterInsertHooks = append(userBackupCodeAfterInsertHooks, userBackupCodeHook)
		userBackupCodeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		userBackupCodeBeforeUpdateMu.Lock()
		userBackupCodeBeforeUpdateHooks = append(userBackupCodeBeforeUpdateHooks, userBackupCodeHook)
		userBackupCodeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		userBackupCodeAfterUpdateMu.Lock()
		userBackupCodeAfterUpdateHooks = append(userBackupCodeAfterUpdateHooks, userBackupCodeHook)
		userBackupCodeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		userBackupCodeBeforeDeleteMu.Lock()
		userBackupCodeBeforeDeleteHooks = append(userBackupCodeBeforeDeleteHooks, userBackupCodeHook)
		userBackupCodeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		userBackupCodeAfterDeleteMu.Lock()
		userBackupCodeAfterDeleteHooks = append(userBackupCodeAfterDeleteHooks, userBackupCodeHook)
		userBackupCodeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		userBackupCodeBeforeUpsertMu.Lock()
		userBackupCodeBeforeUpsertHooks = append(userBackupCodeBeforeUpsertHooks, userBackupCodeHook)
		userBackupCodeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		userBackupCodeAfterUpsertMu.Lock()
		userBackupCodeAfterUpsertHooks = append(userBackupCodeAfterUpsertHooks, userBackupCodeHook)
		userBackupCodeAfterUpsertMu.Unlock()
	}
}

// One returns a single userBackupCode record from the query.
func (q userBackupCodeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*UserBackupCode, error) {
	o := &UserBackupCode{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for user_backup_codes")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all UserBackupCode records from the query.
func (q userBackupCodeQuery) All(ctx context.Context, exec boil.ContextExecutor) (UserBackupCodeSlice, error) {
	var o []*UserBackupCode

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to UserBackupCode slice")
	}

	if len(userBackupCodeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all UserBackupCode records in the query.
func (q userBackupCodeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count user_backup_codes rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q userBackupCodeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if user_backup_codes exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *UserBackupCode) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userBackupCodeL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUserBackupCode interface{}, mods queries.Applicator) error {
	var slice []*UserBackupCode
	var object *UserBackupCode

	if singular {
		var ok bool
		object, ok = maybeUserBackupCode.(*UserBackupCode)
		if !ok {
			object = new(UserBackupCode)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUserBackupCode)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUserBackupCode))
			}
		}
	} else {
		s, ok := maybeUserBackupCode.(*[]*UserBackupCode)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUserBackupCode)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUserBackupCode))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &userBackupCodeR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userBackupCodeR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.UserBackupCodes = append(foreign.R.UserBackupCodes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.UserBackupCodes = append(foreign.R.UserBackupCodes, local)
				break
			}
		}
	}

	return nil
}

// SetUser of the userBackupCode to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserBackupCodes.
func (o *UserBackupCode) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"user_backup_codes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, userBackupCodePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &userBackupCodeR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			UserBackupCodes: UserBackupCodeSlice{o},
		}
	} else {
		related.R.UserBackupCodes = append(related.R.UserBackupCodes, o)
	}

	return nil
}

// UserBackupCodes retrieves all the records using an executor.
func UserBackupCodes(mods ...qm.QueryMod) userBackupCodeQuery {
	mods = append(mods, qm.From("\"user_backup_codes\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"user_backup_codes\".*"})
	}

	return userBackupCodeQuery{q}
}

// FindUserBackupCode retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUserBackupCode(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*UserBackupCode, error) {
	userBackupCodeObj := &UserBackupCode{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"user_backup_codes\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, userBackupCodeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from user_backup_codes")
	}

	if err = userBackupCodeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return userBackupCodeObj, err
	}

	return userBackupCodeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UserBackupCode) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no user_backup_codes provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userBackupCodeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userBackupCodeInsertCacheMut.RLock()
	cache, cached := userBackupCodeInsertCache[key]
	userBackupCodeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userBackupCodeAllColumns,
			userBackupCodeColumnsWithDefault,
			userBackupCodeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(userBackupCodeType, userBackupCodeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userBackupCodeType, userBackupCodeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"user_backup_codes\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"user_backup_codes\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into user_backup_codes")
	}

	if !cached {
		userBackupCodeInsertCacheMut.Lock()
		userBackupCodeInsertCache[key] = cache
		userBackupCodeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the UserBackupCode.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UserBackupCode) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	userBackupCodeUpdateCacheMut.RLock()
	cache, cached := userBackupCodeUpdateCache[key]
	userBackupCodeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userBackupCodeAllColumns,
			userBackupCodePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update user_backup_codes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"user_backup_codes\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, userBackupCodePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userBackupCodeType, userBackupCodeMapping, append(wl, userBackupCodePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update user_backup_codes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for user_backup_codes")
	}

	if !cached {
		userBackupCodeUpdateCacheMut.Lock()
		userBackupCodeUpdateCache[key] = cache
		userBackupCodeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q userBackupCodeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for user_backup_codes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for user_backup_codes")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserBackupCodeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userBackupCodePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"user_backup_codes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, userBackupCodePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in userBackupCode slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all userBackupCode")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UserBackupCode) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no user_backup_codes provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userBackupCodeColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userBackupCodeUpsertCacheMut.RLock()
	cache, cached := userBackupCodeUpsertCache[key]
	userBackupCodeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			userBackupCodeAllColumns,
			userBackupCodeColumnsWithDefault,
			userBackupCodeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			userBackupCodeAllColumns,
			userBackupCodePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert user_backup_codes, could not build update column list")
		}

		ret := strmangle.SetComplement(userBackupCodeAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(userBackupCodePrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert user_backup_codes, could not build conflict column list")
			}

			conflict = make([]string, len(userBackupCodePrimaryKeyColumns))
			copy(conflict, userBackupCodePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"user_backup_codes\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(userBackupCodeType, userBackupCodeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userBackupCodeType, userBackupCodeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert user_backup_codes")
	}

	if !cached {
		userBackupCodeUpsertCacheMut.Lock()
		userBackupCodeUpsertCache[key] = cache
		userBackupCodeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single UserBackupCode record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UserBackupCode) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no UserBackupCode provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userBackupCodePrimaryKeyMapping)
	sql := "DELETE FROM \"user_backup_codes\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from user_backup_codes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for user_backup_codes")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q userBackupCodeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no userBackupCodeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from user_backup_codes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for user_backup_codes")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserBackupCodeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(userBackupCodeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userBackupCodePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"user_backup_codes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userBackupCodePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from userBackupCode slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for user_backup_codes")
	}

	if len(userBackupCodeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UserBackupCode) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUserBackupCode(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserBackupCodeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserBackupCodeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userBackupCodePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"user_backup_codes\".* FROM \"user_backup_codes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userBackupCodePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in UserBackupCodeSlice")
	}

	*o = slice

	return nil
}

// UserBackupCodeExists checks if the UserBackupCode row exists.
func UserBackupCodeExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"user_backup_codes\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if user_backup_codes exists")
	}

	return exists, nil
}

// Exists checks if the UserBackupCode row exists.
func (o *UserBackupCode) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return UserBackupCodeExists(ctx, exec, o.ID)
}
