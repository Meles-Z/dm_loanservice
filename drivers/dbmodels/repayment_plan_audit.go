// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// RepaymentPlanAudit is an object representing the database table.
type RepaymentPlanAudit struct {
	ID        string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	PlanID    string    `boil:"plan_id" json:"plan_id" toml:"plan_id" yaml:"plan_id"`
	Action    string    `boil:"action" json:"action" toml:"action" yaml:"action"`
	UserID    int       `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Timestamp time.Time `boil:"timestamp" json:"timestamp" toml:"timestamp" yaml:"timestamp"`
	Details   string    `boil:"details" json:"details" toml:"details" yaml:"details"`

	R *repaymentPlanAuditR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L repaymentPlanAuditL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RepaymentPlanAuditColumns = struct {
	ID        string
	PlanID    string
	Action    string
	UserID    string
	Timestamp string
	Details   string
}{
	ID:        "id",
	PlanID:    "plan_id",
	Action:    "action",
	UserID:    "user_id",
	Timestamp: "timestamp",
	Details:   "details",
}

var RepaymentPlanAuditTableColumns = struct {
	ID        string
	PlanID    string
	Action    string
	UserID    string
	Timestamp string
	Details   string
}{
	ID:        "repayment_plan_audit.id",
	PlanID:    "repayment_plan_audit.plan_id",
	Action:    "repayment_plan_audit.action",
	UserID:    "repayment_plan_audit.user_id",
	Timestamp: "repayment_plan_audit.timestamp",
	Details:   "repayment_plan_audit.details",
}

// Generated where

var RepaymentPlanAuditWhere = struct {
	ID        whereHelperstring
	PlanID    whereHelperstring
	Action    whereHelperstring
	UserID    whereHelperint
	Timestamp whereHelpertime_Time
	Details   whereHelperstring
}{
	ID:        whereHelperstring{field: "\"repayment_plan_audit\".\"id\""},
	PlanID:    whereHelperstring{field: "\"repayment_plan_audit\".\"plan_id\""},
	Action:    whereHelperstring{field: "\"repayment_plan_audit\".\"action\""},
	UserID:    whereHelperint{field: "\"repayment_plan_audit\".\"user_id\""},
	Timestamp: whereHelpertime_Time{field: "\"repayment_plan_audit\".\"timestamp\""},
	Details:   whereHelperstring{field: "\"repayment_plan_audit\".\"details\""},
}

// RepaymentPlanAuditRels is where relationship names are stored.
var RepaymentPlanAuditRels = struct {
	Plan string
	User string
}{
	Plan: "Plan",
	User: "User",
}

// repaymentPlanAuditR is where relationships are stored.
type repaymentPlanAuditR struct {
	Plan *RepaymentPlan `boil:"Plan" json:"Plan" toml:"Plan" yaml:"Plan"`
	User *User          `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*repaymentPlanAuditR) NewStruct() *repaymentPlanAuditR {
	return &repaymentPlanAuditR{}
}

func (o *RepaymentPlanAudit) GetPlan() *RepaymentPlan {
	if o == nil {
		return nil
	}

	return o.R.GetPlan()
}

func (r *repaymentPlanAuditR) GetPlan() *RepaymentPlan {
	if r == nil {
		return nil
	}

	return r.Plan
}

func (o *RepaymentPlanAudit) GetUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetUser()
}

func (r *repaymentPlanAuditR) GetUser() *User {
	if r == nil {
		return nil
	}

	return r.User
}

// repaymentPlanAuditL is where Load methods for each relationship are stored.
type repaymentPlanAuditL struct{}

var (
	repaymentPlanAuditAllColumns            = []string{"id", "plan_id", "action", "user_id", "timestamp", "details"}
	repaymentPlanAuditColumnsWithoutDefault = []string{"id", "plan_id", "action", "user_id", "details"}
	repaymentPlanAuditColumnsWithDefault    = []string{"timestamp"}
	repaymentPlanAuditPrimaryKeyColumns     = []string{"id"}
	repaymentPlanAuditGeneratedColumns      = []string{}
)

type (
	// RepaymentPlanAuditSlice is an alias for a slice of pointers to RepaymentPlanAudit.
	// This should almost always be used instead of []RepaymentPlanAudit.
	RepaymentPlanAuditSlice []*RepaymentPlanAudit
	// RepaymentPlanAuditHook is the signature for custom RepaymentPlanAudit hook methods
	RepaymentPlanAuditHook func(context.Context, boil.ContextExecutor, *RepaymentPlanAudit) error

	repaymentPlanAuditQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	repaymentPlanAuditType                 = reflect.TypeOf(&RepaymentPlanAudit{})
	repaymentPlanAuditMapping              = queries.MakeStructMapping(repaymentPlanAuditType)
	repaymentPlanAuditPrimaryKeyMapping, _ = queries.BindMapping(repaymentPlanAuditType, repaymentPlanAuditMapping, repaymentPlanAuditPrimaryKeyColumns)
	repaymentPlanAuditInsertCacheMut       sync.RWMutex
	repaymentPlanAuditInsertCache          = make(map[string]insertCache)
	repaymentPlanAuditUpdateCacheMut       sync.RWMutex
	repaymentPlanAuditUpdateCache          = make(map[string]updateCache)
	repaymentPlanAuditUpsertCacheMut       sync.RWMutex
	repaymentPlanAuditUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var repaymentPlanAuditAfterSelectMu sync.Mutex
var repaymentPlanAuditAfterSelectHooks []RepaymentPlanAuditHook

var repaymentPlanAuditBeforeInsertMu sync.Mutex
var repaymentPlanAuditBeforeInsertHooks []RepaymentPlanAuditHook
var repaymentPlanAuditAfterInsertMu sync.Mutex
var repaymentPlanAuditAfterInsertHooks []RepaymentPlanAuditHook

var repaymentPlanAuditBeforeUpdateMu sync.Mutex
var repaymentPlanAuditBeforeUpdateHooks []RepaymentPlanAuditHook
var repaymentPlanAuditAfterUpdateMu sync.Mutex
var repaymentPlanAuditAfterUpdateHooks []RepaymentPlanAuditHook

var repaymentPlanAuditBeforeDeleteMu sync.Mutex
var repaymentPlanAuditBeforeDeleteHooks []RepaymentPlanAuditHook
var repaymentPlanAuditAfterDeleteMu sync.Mutex
var repaymentPlanAuditAfterDeleteHooks []RepaymentPlanAuditHook

var repaymentPlanAuditBeforeUpsertMu sync.Mutex
var repaymentPlanAuditBeforeUpsertHooks []RepaymentPlanAuditHook
var repaymentPlanAuditAfterUpsertMu sync.Mutex
var repaymentPlanAuditAfterUpsertHooks []RepaymentPlanAuditHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RepaymentPlanAudit) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAuditAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RepaymentPlanAudit) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAuditBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RepaymentPlanAudit) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAuditAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RepaymentPlanAudit) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAuditBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RepaymentPlanAudit) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAuditAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RepaymentPlanAudit) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAuditBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RepaymentPlanAudit) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAuditAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RepaymentPlanAudit) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAuditBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RepaymentPlanAudit) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAuditAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRepaymentPlanAuditHook registers your hook function for all future operations.
func AddRepaymentPlanAuditHook(hookPoint boil.HookPoint, repaymentPlanAuditHook RepaymentPlanAuditHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		repaymentPlanAuditAfterSelectMu.Lock()
		repaymentPlanAuditAfterSelectHooks = append(repaymentPlanAuditAfterSelectHooks, repaymentPlanAuditHook)
		repaymentPlanAuditAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		repaymentPlanAuditBeforeInsertMu.Lock()
		repaymentPlanAuditBeforeInsertHooks = append(repaymentPlanAuditBeforeInsertHooks, repaymentPlanAuditHook)
		repaymentPlanAuditBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		repaymentPlanAuditAfterInsertMu.Lock()
		repaymentPlanAuditAfterInsertHooks = append(repaymentPlanAuditAfterInsertHooks, repaymentPlanAuditHook)
		repaymentPlanAuditAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		repaymentPlanAuditBeforeUpdateMu.Lock()
		repaymentPlanAuditBeforeUpdateHooks = append(repaymentPlanAuditBeforeUpdateHooks, repaymentPlanAuditHook)
		repaymentPlanAuditBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		repaymentPlanAuditAfterUpdateMu.Lock()
		repaymentPlanAuditAfterUpdateHooks = append(repaymentPlanAuditAfterUpdateHooks, repaymentPlanAuditHook)
		repaymentPlanAuditAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		repaymentPlanAuditBeforeDeleteMu.Lock()
		repaymentPlanAuditBeforeDeleteHooks = append(repaymentPlanAuditBeforeDeleteHooks, repaymentPlanAuditHook)
		repaymentPlanAuditBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		repaymentPlanAuditAfterDeleteMu.Lock()
		repaymentPlanAuditAfterDeleteHooks = append(repaymentPlanAuditAfterDeleteHooks, repaymentPlanAuditHook)
		repaymentPlanAuditAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		repaymentPlanAuditBeforeUpsertMu.Lock()
		repaymentPlanAuditBeforeUpsertHooks = append(repaymentPlanAuditBeforeUpsertHooks, repaymentPlanAuditHook)
		repaymentPlanAuditBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		repaymentPlanAuditAfterUpsertMu.Lock()
		repaymentPlanAuditAfterUpsertHooks = append(repaymentPlanAuditAfterUpsertHooks, repaymentPlanAuditHook)
		repaymentPlanAuditAfterUpsertMu.Unlock()
	}
}

// One returns a single repaymentPlanAudit record from the query.
func (q repaymentPlanAuditQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RepaymentPlanAudit, error) {
	o := &RepaymentPlanAudit{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for repayment_plan_audit")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RepaymentPlanAudit records from the query.
func (q repaymentPlanAuditQuery) All(ctx context.Context, exec boil.ContextExecutor) (RepaymentPlanAuditSlice, error) {
	var o []*RepaymentPlanAudit

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to RepaymentPlanAudit slice")
	}

	if len(repaymentPlanAuditAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RepaymentPlanAudit records in the query.
func (q repaymentPlanAuditQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count repayment_plan_audit rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q repaymentPlanAuditQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if repayment_plan_audit exists")
	}

	return count > 0, nil
}

// Plan pointed to by the foreign key.
func (o *RepaymentPlanAudit) Plan(mods ...qm.QueryMod) repaymentPlanQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PlanID),
	}

	queryMods = append(queryMods, mods...)

	return RepaymentPlans(queryMods...)
}

// User pointed to by the foreign key.
func (o *RepaymentPlanAudit) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadPlan allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (repaymentPlanAuditL) LoadPlan(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRepaymentPlanAudit interface{}, mods queries.Applicator) error {
	var slice []*RepaymentPlanAudit
	var object *RepaymentPlanAudit

	if singular {
		var ok bool
		object, ok = maybeRepaymentPlanAudit.(*RepaymentPlanAudit)
		if !ok {
			object = new(RepaymentPlanAudit)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRepaymentPlanAudit)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRepaymentPlanAudit))
			}
		}
	} else {
		s, ok := maybeRepaymentPlanAudit.(*[]*RepaymentPlanAudit)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRepaymentPlanAudit)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRepaymentPlanAudit))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &repaymentPlanAuditR{}
		}
		args[object.PlanID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &repaymentPlanAuditR{}
			}

			args[obj.PlanID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`repayment_plans`),
		qm.WhereIn(`repayment_plans.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RepaymentPlan")
	}

	var resultSlice []*RepaymentPlan
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RepaymentPlan")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for repayment_plans")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for repayment_plans")
	}

	if len(repaymentPlanAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Plan = foreign
		if foreign.R == nil {
			foreign.R = &repaymentPlanR{}
		}
		foreign.R.PlanRepaymentPlanAudits = append(foreign.R.PlanRepaymentPlanAudits, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PlanID == foreign.ID {
				local.R.Plan = foreign
				if foreign.R == nil {
					foreign.R = &repaymentPlanR{}
				}
				foreign.R.PlanRepaymentPlanAudits = append(foreign.R.PlanRepaymentPlanAudits, local)
				break
			}
		}
	}

	return nil
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (repaymentPlanAuditL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRepaymentPlanAudit interface{}, mods queries.Applicator) error {
	var slice []*RepaymentPlanAudit
	var object *RepaymentPlanAudit

	if singular {
		var ok bool
		object, ok = maybeRepaymentPlanAudit.(*RepaymentPlanAudit)
		if !ok {
			object = new(RepaymentPlanAudit)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRepaymentPlanAudit)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRepaymentPlanAudit))
			}
		}
	} else {
		s, ok := maybeRepaymentPlanAudit.(*[]*RepaymentPlanAudit)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRepaymentPlanAudit)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRepaymentPlanAudit))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &repaymentPlanAuditR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &repaymentPlanAuditR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.RepaymentPlanAudits = append(foreign.R.RepaymentPlanAudits, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.RepaymentPlanAudits = append(foreign.R.RepaymentPlanAudits, local)
				break
			}
		}
	}

	return nil
}

// SetPlan of the repaymentPlanAudit to the related item.
// Sets o.R.Plan to related.
// Adds o to related.R.PlanRepaymentPlanAudits.
func (o *RepaymentPlanAudit) SetPlan(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RepaymentPlan) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"repayment_plan_audit\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"plan_id"}),
		strmangle.WhereClause("\"", "\"", 2, repaymentPlanAuditPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PlanID = related.ID
	if o.R == nil {
		o.R = &repaymentPlanAuditR{
			Plan: related,
		}
	} else {
		o.R.Plan = related
	}

	if related.R == nil {
		related.R = &repaymentPlanR{
			PlanRepaymentPlanAudits: RepaymentPlanAuditSlice{o},
		}
	} else {
		related.R.PlanRepaymentPlanAudits = append(related.R.PlanRepaymentPlanAudits, o)
	}

	return nil
}

// SetUser of the repaymentPlanAudit to the related item.
// Sets o.R.User to related.
// Adds o to related.R.RepaymentPlanAudits.
func (o *RepaymentPlanAudit) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"repayment_plan_audit\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, repaymentPlanAuditPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &repaymentPlanAuditR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			RepaymentPlanAudits: RepaymentPlanAuditSlice{o},
		}
	} else {
		related.R.RepaymentPlanAudits = append(related.R.RepaymentPlanAudits, o)
	}

	return nil
}

// RepaymentPlanAudits retrieves all the records using an executor.
func RepaymentPlanAudits(mods ...qm.QueryMod) repaymentPlanAuditQuery {
	mods = append(mods, qm.From("\"repayment_plan_audit\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"repayment_plan_audit\".*"})
	}

	return repaymentPlanAuditQuery{q}
}

// FindRepaymentPlanAudit retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRepaymentPlanAudit(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*RepaymentPlanAudit, error) {
	repaymentPlanAuditObj := &RepaymentPlanAudit{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"repayment_plan_audit\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, repaymentPlanAuditObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from repayment_plan_audit")
	}

	if err = repaymentPlanAuditObj.doAfterSelectHooks(ctx, exec); err != nil {
		return repaymentPlanAuditObj, err
	}

	return repaymentPlanAuditObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RepaymentPlanAudit) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no repayment_plan_audit provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(repaymentPlanAuditColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	repaymentPlanAuditInsertCacheMut.RLock()
	cache, cached := repaymentPlanAuditInsertCache[key]
	repaymentPlanAuditInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			repaymentPlanAuditAllColumns,
			repaymentPlanAuditColumnsWithDefault,
			repaymentPlanAuditColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(repaymentPlanAuditType, repaymentPlanAuditMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(repaymentPlanAuditType, repaymentPlanAuditMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"repayment_plan_audit\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"repayment_plan_audit\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into repayment_plan_audit")
	}

	if !cached {
		repaymentPlanAuditInsertCacheMut.Lock()
		repaymentPlanAuditInsertCache[key] = cache
		repaymentPlanAuditInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RepaymentPlanAudit.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RepaymentPlanAudit) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	repaymentPlanAuditUpdateCacheMut.RLock()
	cache, cached := repaymentPlanAuditUpdateCache[key]
	repaymentPlanAuditUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			repaymentPlanAuditAllColumns,
			repaymentPlanAuditPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update repayment_plan_audit, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"repayment_plan_audit\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, repaymentPlanAuditPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(repaymentPlanAuditType, repaymentPlanAuditMapping, append(wl, repaymentPlanAuditPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update repayment_plan_audit row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for repayment_plan_audit")
	}

	if !cached {
		repaymentPlanAuditUpdateCacheMut.Lock()
		repaymentPlanAuditUpdateCache[key] = cache
		repaymentPlanAuditUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q repaymentPlanAuditQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for repayment_plan_audit")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for repayment_plan_audit")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RepaymentPlanAuditSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), repaymentPlanAuditPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"repayment_plan_audit\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, repaymentPlanAuditPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in repaymentPlanAudit slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all repaymentPlanAudit")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RepaymentPlanAudit) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no repayment_plan_audit provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(repaymentPlanAuditColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	repaymentPlanAuditUpsertCacheMut.RLock()
	cache, cached := repaymentPlanAuditUpsertCache[key]
	repaymentPlanAuditUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			repaymentPlanAuditAllColumns,
			repaymentPlanAuditColumnsWithDefault,
			repaymentPlanAuditColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			repaymentPlanAuditAllColumns,
			repaymentPlanAuditPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert repayment_plan_audit, could not build update column list")
		}

		ret := strmangle.SetComplement(repaymentPlanAuditAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(repaymentPlanAuditPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert repayment_plan_audit, could not build conflict column list")
			}

			conflict = make([]string, len(repaymentPlanAuditPrimaryKeyColumns))
			copy(conflict, repaymentPlanAuditPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"repayment_plan_audit\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(repaymentPlanAuditType, repaymentPlanAuditMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(repaymentPlanAuditType, repaymentPlanAuditMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert repayment_plan_audit")
	}

	if !cached {
		repaymentPlanAuditUpsertCacheMut.Lock()
		repaymentPlanAuditUpsertCache[key] = cache
		repaymentPlanAuditUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RepaymentPlanAudit record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RepaymentPlanAudit) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no RepaymentPlanAudit provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), repaymentPlanAuditPrimaryKeyMapping)
	sql := "DELETE FROM \"repayment_plan_audit\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from repayment_plan_audit")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for repayment_plan_audit")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q repaymentPlanAuditQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no repaymentPlanAuditQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from repayment_plan_audit")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for repayment_plan_audit")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RepaymentPlanAuditSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(repaymentPlanAuditBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), repaymentPlanAuditPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"repayment_plan_audit\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, repaymentPlanAuditPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from repaymentPlanAudit slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for repayment_plan_audit")
	}

	if len(repaymentPlanAuditAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RepaymentPlanAudit) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRepaymentPlanAudit(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RepaymentPlanAuditSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RepaymentPlanAuditSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), repaymentPlanAuditPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"repayment_plan_audit\".* FROM \"repayment_plan_audit\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, repaymentPlanAuditPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in RepaymentPlanAuditSlice")
	}

	*o = slice

	return nil
}

// RepaymentPlanAuditExists checks if the RepaymentPlanAudit row exists.
func RepaymentPlanAuditExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"repayment_plan_audit\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if repayment_plan_audit exists")
	}

	return exists, nil
}

// Exists checks if the RepaymentPlanAudit row exists.
func (o *RepaymentPlanAudit) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return RepaymentPlanAuditExists(ctx, exec, o.ID)
}
