// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// CategoryField is an object representing the database table.
type CategoryField struct {
	ID         int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	CategoryID int       `boil:"category_id" json:"category_id" toml:"category_id" yaml:"category_id"`
	Field      string    `boil:"field" json:"field" toml:"field" yaml:"field"`
	FieldType  string    `boil:"field_type" json:"field_type" toml:"field_type" yaml:"field_type"`
	Required   bool      `boil:"required" json:"required" toml:"required" yaml:"required"`
	Key        string    `boil:"key" json:"key" toml:"key" yaml:"key"`
	ColumnType string    `boil:"column_type" json:"column_type" toml:"column_type" yaml:"column_type"`
	FieldOrder int       `boil:"field_order" json:"field_order" toml:"field_order" yaml:"field_order"`
	CreatedAt  time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt  null.Time `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *categoryFieldR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L categoryFieldL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CategoryFieldColumns = struct {
	ID         string
	CategoryID string
	Field      string
	FieldType  string
	Required   string
	Key        string
	ColumnType string
	FieldOrder string
	CreatedAt  string
	UpdatedAt  string
}{
	ID:         "id",
	CategoryID: "category_id",
	Field:      "field",
	FieldType:  "field_type",
	Required:   "required",
	Key:        "key",
	ColumnType: "column_type",
	FieldOrder: "field_order",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
}

var CategoryFieldTableColumns = struct {
	ID         string
	CategoryID string
	Field      string
	FieldType  string
	Required   string
	Key        string
	ColumnType string
	FieldOrder string
	CreatedAt  string
	UpdatedAt  string
}{
	ID:         "category_field.id",
	CategoryID: "category_field.category_id",
	Field:      "category_field.field",
	FieldType:  "category_field.field_type",
	Required:   "category_field.required",
	Key:        "category_field.key",
	ColumnType: "category_field.column_type",
	FieldOrder: "category_field.field_order",
	CreatedAt:  "category_field.created_at",
	UpdatedAt:  "category_field.updated_at",
}

// Generated where

var CategoryFieldWhere = struct {
	ID         whereHelperint
	CategoryID whereHelperint
	Field      whereHelperstring
	FieldType  whereHelperstring
	Required   whereHelperbool
	Key        whereHelperstring
	ColumnType whereHelperstring
	FieldOrder whereHelperint
	CreatedAt  whereHelpertime_Time
	UpdatedAt  whereHelpernull_Time
}{
	ID:         whereHelperint{field: "\"category_field\".\"id\""},
	CategoryID: whereHelperint{field: "\"category_field\".\"category_id\""},
	Field:      whereHelperstring{field: "\"category_field\".\"field\""},
	FieldType:  whereHelperstring{field: "\"category_field\".\"field_type\""},
	Required:   whereHelperbool{field: "\"category_field\".\"required\""},
	Key:        whereHelperstring{field: "\"category_field\".\"key\""},
	ColumnType: whereHelperstring{field: "\"category_field\".\"column_type\""},
	FieldOrder: whereHelperint{field: "\"category_field\".\"field_order\""},
	CreatedAt:  whereHelpertime_Time{field: "\"category_field\".\"created_at\""},
	UpdatedAt:  whereHelpernull_Time{field: "\"category_field\".\"updated_at\""},
}

// CategoryFieldRels is where relationship names are stored.
var CategoryFieldRels = struct {
	Category                 string
	CategoryfieldUserDetails string
}{
	Category:                 "Category",
	CategoryfieldUserDetails: "CategoryfieldUserDetails",
}

// categoryFieldR is where relationships are stored.
type categoryFieldR struct {
	Category                 *CategoryDetail `boil:"Category" json:"Category" toml:"Category" yaml:"Category"`
	CategoryfieldUserDetails UserDetailSlice `boil:"CategoryfieldUserDetails" json:"CategoryfieldUserDetails" toml:"CategoryfieldUserDetails" yaml:"CategoryfieldUserDetails"`
}

// NewStruct creates a new relationship struct
func (*categoryFieldR) NewStruct() *categoryFieldR {
	return &categoryFieldR{}
}

func (o *CategoryField) GetCategory() *CategoryDetail {
	if o == nil {
		return nil
	}

	return o.R.GetCategory()
}

func (r *categoryFieldR) GetCategory() *CategoryDetail {
	if r == nil {
		return nil
	}

	return r.Category
}

func (o *CategoryField) GetCategoryfieldUserDetails() UserDetailSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCategoryfieldUserDetails()
}

func (r *categoryFieldR) GetCategoryfieldUserDetails() UserDetailSlice {
	if r == nil {
		return nil
	}

	return r.CategoryfieldUserDetails
}

// categoryFieldL is where Load methods for each relationship are stored.
type categoryFieldL struct{}

var (
	categoryFieldAllColumns            = []string{"id", "category_id", "field", "field_type", "required", "key", "column_type", "field_order", "created_at", "updated_at"}
	categoryFieldColumnsWithoutDefault = []string{"category_id", "field", "field_type", "key", "column_type", "created_at"}
	categoryFieldColumnsWithDefault    = []string{"id", "required", "field_order", "updated_at"}
	categoryFieldPrimaryKeyColumns     = []string{"id"}
	categoryFieldGeneratedColumns      = []string{}
)

type (
	// CategoryFieldSlice is an alias for a slice of pointers to CategoryField.
	// This should almost always be used instead of []CategoryField.
	CategoryFieldSlice []*CategoryField
	// CategoryFieldHook is the signature for custom CategoryField hook methods
	CategoryFieldHook func(context.Context, boil.ContextExecutor, *CategoryField) error

	categoryFieldQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	categoryFieldType                 = reflect.TypeOf(&CategoryField{})
	categoryFieldMapping              = queries.MakeStructMapping(categoryFieldType)
	categoryFieldPrimaryKeyMapping, _ = queries.BindMapping(categoryFieldType, categoryFieldMapping, categoryFieldPrimaryKeyColumns)
	categoryFieldInsertCacheMut       sync.RWMutex
	categoryFieldInsertCache          = make(map[string]insertCache)
	categoryFieldUpdateCacheMut       sync.RWMutex
	categoryFieldUpdateCache          = make(map[string]updateCache)
	categoryFieldUpsertCacheMut       sync.RWMutex
	categoryFieldUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var categoryFieldAfterSelectMu sync.Mutex
var categoryFieldAfterSelectHooks []CategoryFieldHook

var categoryFieldBeforeInsertMu sync.Mutex
var categoryFieldBeforeInsertHooks []CategoryFieldHook
var categoryFieldAfterInsertMu sync.Mutex
var categoryFieldAfterInsertHooks []CategoryFieldHook

var categoryFieldBeforeUpdateMu sync.Mutex
var categoryFieldBeforeUpdateHooks []CategoryFieldHook
var categoryFieldAfterUpdateMu sync.Mutex
var categoryFieldAfterUpdateHooks []CategoryFieldHook

var categoryFieldBeforeDeleteMu sync.Mutex
var categoryFieldBeforeDeleteHooks []CategoryFieldHook
var categoryFieldAfterDeleteMu sync.Mutex
var categoryFieldAfterDeleteHooks []CategoryFieldHook

var categoryFieldBeforeUpsertMu sync.Mutex
var categoryFieldBeforeUpsertHooks []CategoryFieldHook
var categoryFieldAfterUpsertMu sync.Mutex
var categoryFieldAfterUpsertHooks []CategoryFieldHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CategoryField) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryFieldAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CategoryField) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryFieldBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CategoryField) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryFieldAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CategoryField) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryFieldBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CategoryField) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryFieldAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CategoryField) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryFieldBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CategoryField) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryFieldAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CategoryField) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryFieldBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CategoryField) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryFieldAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCategoryFieldHook registers your hook function for all future operations.
func AddCategoryFieldHook(hookPoint boil.HookPoint, categoryFieldHook CategoryFieldHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		categoryFieldAfterSelectMu.Lock()
		categoryFieldAfterSelectHooks = append(categoryFieldAfterSelectHooks, categoryFieldHook)
		categoryFieldAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		categoryFieldBeforeInsertMu.Lock()
		categoryFieldBeforeInsertHooks = append(categoryFieldBeforeInsertHooks, categoryFieldHook)
		categoryFieldBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		categoryFieldAfterInsertMu.Lock()
		categoryFieldAfterInsertHooks = append(categoryFieldAfterInsertHooks, categoryFieldHook)
		categoryFieldAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		categoryFieldBeforeUpdateMu.Lock()
		categoryFieldBeforeUpdateHooks = append(categoryFieldBeforeUpdateHooks, categoryFieldHook)
		categoryFieldBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		categoryFieldAfterUpdateMu.Lock()
		categoryFieldAfterUpdateHooks = append(categoryFieldAfterUpdateHooks, categoryFieldHook)
		categoryFieldAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		categoryFieldBeforeDeleteMu.Lock()
		categoryFieldBeforeDeleteHooks = append(categoryFieldBeforeDeleteHooks, categoryFieldHook)
		categoryFieldBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		categoryFieldAfterDeleteMu.Lock()
		categoryFieldAfterDeleteHooks = append(categoryFieldAfterDeleteHooks, categoryFieldHook)
		categoryFieldAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		categoryFieldBeforeUpsertMu.Lock()
		categoryFieldBeforeUpsertHooks = append(categoryFieldBeforeUpsertHooks, categoryFieldHook)
		categoryFieldBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		categoryFieldAfterUpsertMu.Lock()
		categoryFieldAfterUpsertHooks = append(categoryFieldAfterUpsertHooks, categoryFieldHook)
		categoryFieldAfterUpsertMu.Unlock()
	}
}

// One returns a single categoryField record from the query.
func (q categoryFieldQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CategoryField, error) {
	o := &CategoryField{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for category_field")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CategoryField records from the query.
func (q categoryFieldQuery) All(ctx context.Context, exec boil.ContextExecutor) (CategoryFieldSlice, error) {
	var o []*CategoryField

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to CategoryField slice")
	}

	if len(categoryFieldAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CategoryField records in the query.
func (q categoryFieldQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count category_field rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q categoryFieldQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if category_field exists")
	}

	return count > 0, nil
}

// Category pointed to by the foreign key.
func (o *CategoryField) Category(mods ...qm.QueryMod) categoryDetailQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CategoryID),
	}

	queryMods = append(queryMods, mods...)

	return CategoryDetails(queryMods...)
}

// CategoryfieldUserDetails retrieves all the user_detail's UserDetails with an executor via categoryfield_id column.
func (o *CategoryField) CategoryfieldUserDetails(mods ...qm.QueryMod) userDetailQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"user_detail\".\"categoryfield_id\"=?", o.ID),
	)

	return UserDetails(queryMods...)
}

// LoadCategory allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (categoryFieldL) LoadCategory(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCategoryField interface{}, mods queries.Applicator) error {
	var slice []*CategoryField
	var object *CategoryField

	if singular {
		var ok bool
		object, ok = maybeCategoryField.(*CategoryField)
		if !ok {
			object = new(CategoryField)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCategoryField)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCategoryField))
			}
		}
	} else {
		s, ok := maybeCategoryField.(*[]*CategoryField)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCategoryField)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCategoryField))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &categoryFieldR{}
		}
		args[object.CategoryID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &categoryFieldR{}
			}

			args[obj.CategoryID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`category_detail`),
		qm.WhereIn(`category_detail.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load CategoryDetail")
	}

	var resultSlice []*CategoryDetail
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice CategoryDetail")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for category_detail")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for category_detail")
	}

	if len(categoryDetailAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Category = foreign
		if foreign.R == nil {
			foreign.R = &categoryDetailR{}
		}
		foreign.R.CategoryCategoryFields = append(foreign.R.CategoryCategoryFields, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CategoryID == foreign.ID {
				local.R.Category = foreign
				if foreign.R == nil {
					foreign.R = &categoryDetailR{}
				}
				foreign.R.CategoryCategoryFields = append(foreign.R.CategoryCategoryFields, local)
				break
			}
		}
	}

	return nil
}

// LoadCategoryfieldUserDetails allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (categoryFieldL) LoadCategoryfieldUserDetails(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCategoryField interface{}, mods queries.Applicator) error {
	var slice []*CategoryField
	var object *CategoryField

	if singular {
		var ok bool
		object, ok = maybeCategoryField.(*CategoryField)
		if !ok {
			object = new(CategoryField)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCategoryField)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCategoryField))
			}
		}
	} else {
		s, ok := maybeCategoryField.(*[]*CategoryField)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCategoryField)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCategoryField))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &categoryFieldR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &categoryFieldR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`user_detail`),
		qm.WhereIn(`user_detail.categoryfield_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load user_detail")
	}

	var resultSlice []*UserDetail
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice user_detail")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on user_detail")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for user_detail")
	}

	if len(userDetailAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CategoryfieldUserDetails = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &userDetailR{}
			}
			foreign.R.Categoryfield = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.CategoryfieldID) {
				local.R.CategoryfieldUserDetails = append(local.R.CategoryfieldUserDetails, foreign)
				if foreign.R == nil {
					foreign.R = &userDetailR{}
				}
				foreign.R.Categoryfield = local
				break
			}
		}
	}

	return nil
}

// SetCategory of the categoryField to the related item.
// Sets o.R.Category to related.
// Adds o to related.R.CategoryCategoryFields.
func (o *CategoryField) SetCategory(ctx context.Context, exec boil.ContextExecutor, insert bool, related *CategoryDetail) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"category_field\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"category_id"}),
		strmangle.WhereClause("\"", "\"", 2, categoryFieldPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CategoryID = related.ID
	if o.R == nil {
		o.R = &categoryFieldR{
			Category: related,
		}
	} else {
		o.R.Category = related
	}

	if related.R == nil {
		related.R = &categoryDetailR{
			CategoryCategoryFields: CategoryFieldSlice{o},
		}
	} else {
		related.R.CategoryCategoryFields = append(related.R.CategoryCategoryFields, o)
	}

	return nil
}

// AddCategoryfieldUserDetails adds the given related objects to the existing relationships
// of the category_field, optionally inserting them as new records.
// Appends related to o.R.CategoryfieldUserDetails.
// Sets related.R.Categoryfield appropriately.
func (o *CategoryField) AddCategoryfieldUserDetails(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*UserDetail) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.CategoryfieldID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"user_detail\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"categoryfield_id"}),
				strmangle.WhereClause("\"", "\"", 2, userDetailPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.CategoryfieldID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &categoryFieldR{
			CategoryfieldUserDetails: related,
		}
	} else {
		o.R.CategoryfieldUserDetails = append(o.R.CategoryfieldUserDetails, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &userDetailR{
				Categoryfield: o,
			}
		} else {
			rel.R.Categoryfield = o
		}
	}
	return nil
}

// SetCategoryfieldUserDetails removes all previously related items of the
// category_field replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Categoryfield's CategoryfieldUserDetails accordingly.
// Replaces o.R.CategoryfieldUserDetails with related.
// Sets related.R.Categoryfield's CategoryfieldUserDetails accordingly.
func (o *CategoryField) SetCategoryfieldUserDetails(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*UserDetail) error {
	query := "update \"user_detail\" set \"categoryfield_id\" = null where \"categoryfield_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.CategoryfieldUserDetails {
			queries.SetScanner(&rel.CategoryfieldID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Categoryfield = nil
		}
		o.R.CategoryfieldUserDetails = nil
	}

	return o.AddCategoryfieldUserDetails(ctx, exec, insert, related...)
}

// RemoveCategoryfieldUserDetails relationships from objects passed in.
// Removes related items from R.CategoryfieldUserDetails (uses pointer comparison, removal does not keep order)
// Sets related.R.Categoryfield.
func (o *CategoryField) RemoveCategoryfieldUserDetails(ctx context.Context, exec boil.ContextExecutor, related ...*UserDetail) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.CategoryfieldID, nil)
		if rel.R != nil {
			rel.R.Categoryfield = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("categoryfield_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.CategoryfieldUserDetails {
			if rel != ri {
				continue
			}

			ln := len(o.R.CategoryfieldUserDetails)
			if ln > 1 && i < ln-1 {
				o.R.CategoryfieldUserDetails[i] = o.R.CategoryfieldUserDetails[ln-1]
			}
			o.R.CategoryfieldUserDetails = o.R.CategoryfieldUserDetails[:ln-1]
			break
		}
	}

	return nil
}

// CategoryFields retrieves all the records using an executor.
func CategoryFields(mods ...qm.QueryMod) categoryFieldQuery {
	mods = append(mods, qm.From("\"category_field\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"category_field\".*"})
	}

	return categoryFieldQuery{q}
}

// FindCategoryField retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCategoryField(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*CategoryField, error) {
	categoryFieldObj := &CategoryField{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"category_field\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, categoryFieldObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from category_field")
	}

	if err = categoryFieldObj.doAfterSelectHooks(ctx, exec); err != nil {
		return categoryFieldObj, err
	}

	return categoryFieldObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CategoryField) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no category_field provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(categoryFieldColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	categoryFieldInsertCacheMut.RLock()
	cache, cached := categoryFieldInsertCache[key]
	categoryFieldInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			categoryFieldAllColumns,
			categoryFieldColumnsWithDefault,
			categoryFieldColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(categoryFieldType, categoryFieldMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(categoryFieldType, categoryFieldMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"category_field\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"category_field\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into category_field")
	}

	if !cached {
		categoryFieldInsertCacheMut.Lock()
		categoryFieldInsertCache[key] = cache
		categoryFieldInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CategoryField.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CategoryField) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	categoryFieldUpdateCacheMut.RLock()
	cache, cached := categoryFieldUpdateCache[key]
	categoryFieldUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			categoryFieldAllColumns,
			categoryFieldPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update category_field, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"category_field\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, categoryFieldPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(categoryFieldType, categoryFieldMapping, append(wl, categoryFieldPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update category_field row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for category_field")
	}

	if !cached {
		categoryFieldUpdateCacheMut.Lock()
		categoryFieldUpdateCache[key] = cache
		categoryFieldUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q categoryFieldQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for category_field")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for category_field")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CategoryFieldSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), categoryFieldPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"category_field\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, categoryFieldPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in categoryField slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all categoryField")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CategoryField) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no category_field provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(categoryFieldColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	categoryFieldUpsertCacheMut.RLock()
	cache, cached := categoryFieldUpsertCache[key]
	categoryFieldUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			categoryFieldAllColumns,
			categoryFieldColumnsWithDefault,
			categoryFieldColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			categoryFieldAllColumns,
			categoryFieldPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert category_field, could not build update column list")
		}

		ret := strmangle.SetComplement(categoryFieldAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(categoryFieldPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert category_field, could not build conflict column list")
			}

			conflict = make([]string, len(categoryFieldPrimaryKeyColumns))
			copy(conflict, categoryFieldPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"category_field\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(categoryFieldType, categoryFieldMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(categoryFieldType, categoryFieldMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert category_field")
	}

	if !cached {
		categoryFieldUpsertCacheMut.Lock()
		categoryFieldUpsertCache[key] = cache
		categoryFieldUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CategoryField record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CategoryField) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no CategoryField provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), categoryFieldPrimaryKeyMapping)
	sql := "DELETE FROM \"category_field\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from category_field")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for category_field")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q categoryFieldQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no categoryFieldQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from category_field")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for category_field")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CategoryFieldSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(categoryFieldBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), categoryFieldPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"category_field\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, categoryFieldPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from categoryField slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for category_field")
	}

	if len(categoryFieldAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CategoryField) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCategoryField(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CategoryFieldSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CategoryFieldSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), categoryFieldPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"category_field\".* FROM \"category_field\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, categoryFieldPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in CategoryFieldSlice")
	}

	*o = slice

	return nil
}

// CategoryFieldExists checks if the CategoryField row exists.
func CategoryFieldExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"category_field\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if category_field exists")
	}

	return exists, nil
}

// Exists checks if the CategoryField row exists.
func (o *CategoryField) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CategoryFieldExists(ctx, exec, o.ID)
}
