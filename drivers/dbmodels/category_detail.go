// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// CategoryDetail is an object representing the database table.
type CategoryDetail struct {
	ID          int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	Name        string    `boil:"name" json:"name" toml:"name" yaml:"name"`
	Description string    `boil:"description" json:"description" toml:"description" yaml:"description"`
	CreatedAt   time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt   null.Time `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *categoryDetailR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L categoryDetailL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CategoryDetailColumns = struct {
	ID          string
	Name        string
	Description string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "id",
	Name:        "name",
	Description: "description",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
}

var CategoryDetailTableColumns = struct {
	ID          string
	Name        string
	Description string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "category_detail.id",
	Name:        "category_detail.name",
	Description: "category_detail.description",
	CreatedAt:   "category_detail.created_at",
	UpdatedAt:   "category_detail.updated_at",
}

// Generated where

var CategoryDetailWhere = struct {
	ID          whereHelperint
	Name        whereHelperstring
	Description whereHelperstring
	CreatedAt   whereHelpertime_Time
	UpdatedAt   whereHelpernull_Time
}{
	ID:          whereHelperint{field: "\"category_detail\".\"id\""},
	Name:        whereHelperstring{field: "\"category_detail\".\"name\""},
	Description: whereHelperstring{field: "\"category_detail\".\"description\""},
	CreatedAt:   whereHelpertime_Time{field: "\"category_detail\".\"created_at\""},
	UpdatedAt:   whereHelpernull_Time{field: "\"category_detail\".\"updated_at\""},
}

// CategoryDetailRels is where relationship names are stored.
var CategoryDetailRels = struct {
	CategoryCategoryFields string
	CategoryLanguages      string
}{
	CategoryCategoryFields: "CategoryCategoryFields",
	CategoryLanguages:      "CategoryLanguages",
}

// categoryDetailR is where relationships are stored.
type categoryDetailR struct {
	CategoryCategoryFields CategoryFieldSlice `boil:"CategoryCategoryFields" json:"CategoryCategoryFields" toml:"CategoryCategoryFields" yaml:"CategoryCategoryFields"`
	CategoryLanguages      LanguageSlice      `boil:"CategoryLanguages" json:"CategoryLanguages" toml:"CategoryLanguages" yaml:"CategoryLanguages"`
}

// NewStruct creates a new relationship struct
func (*categoryDetailR) NewStruct() *categoryDetailR {
	return &categoryDetailR{}
}

func (o *CategoryDetail) GetCategoryCategoryFields() CategoryFieldSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCategoryCategoryFields()
}

func (r *categoryDetailR) GetCategoryCategoryFields() CategoryFieldSlice {
	if r == nil {
		return nil
	}

	return r.CategoryCategoryFields
}

func (o *CategoryDetail) GetCategoryLanguages() LanguageSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCategoryLanguages()
}

func (r *categoryDetailR) GetCategoryLanguages() LanguageSlice {
	if r == nil {
		return nil
	}

	return r.CategoryLanguages
}

// categoryDetailL is where Load methods for each relationship are stored.
type categoryDetailL struct{}

var (
	categoryDetailAllColumns            = []string{"id", "name", "description", "created_at", "updated_at"}
	categoryDetailColumnsWithoutDefault = []string{"name", "description", "created_at"}
	categoryDetailColumnsWithDefault    = []string{"id", "updated_at"}
	categoryDetailPrimaryKeyColumns     = []string{"id"}
	categoryDetailGeneratedColumns      = []string{}
)

type (
	// CategoryDetailSlice is an alias for a slice of pointers to CategoryDetail.
	// This should almost always be used instead of []CategoryDetail.
	CategoryDetailSlice []*CategoryDetail
	// CategoryDetailHook is the signature for custom CategoryDetail hook methods
	CategoryDetailHook func(context.Context, boil.ContextExecutor, *CategoryDetail) error

	categoryDetailQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	categoryDetailType                 = reflect.TypeOf(&CategoryDetail{})
	categoryDetailMapping              = queries.MakeStructMapping(categoryDetailType)
	categoryDetailPrimaryKeyMapping, _ = queries.BindMapping(categoryDetailType, categoryDetailMapping, categoryDetailPrimaryKeyColumns)
	categoryDetailInsertCacheMut       sync.RWMutex
	categoryDetailInsertCache          = make(map[string]insertCache)
	categoryDetailUpdateCacheMut       sync.RWMutex
	categoryDetailUpdateCache          = make(map[string]updateCache)
	categoryDetailUpsertCacheMut       sync.RWMutex
	categoryDetailUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var categoryDetailAfterSelectMu sync.Mutex
var categoryDetailAfterSelectHooks []CategoryDetailHook

var categoryDetailBeforeInsertMu sync.Mutex
var categoryDetailBeforeInsertHooks []CategoryDetailHook
var categoryDetailAfterInsertMu sync.Mutex
var categoryDetailAfterInsertHooks []CategoryDetailHook

var categoryDetailBeforeUpdateMu sync.Mutex
var categoryDetailBeforeUpdateHooks []CategoryDetailHook
var categoryDetailAfterUpdateMu sync.Mutex
var categoryDetailAfterUpdateHooks []CategoryDetailHook

var categoryDetailBeforeDeleteMu sync.Mutex
var categoryDetailBeforeDeleteHooks []CategoryDetailHook
var categoryDetailAfterDeleteMu sync.Mutex
var categoryDetailAfterDeleteHooks []CategoryDetailHook

var categoryDetailBeforeUpsertMu sync.Mutex
var categoryDetailBeforeUpsertHooks []CategoryDetailHook
var categoryDetailAfterUpsertMu sync.Mutex
var categoryDetailAfterUpsertHooks []CategoryDetailHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CategoryDetail) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryDetailAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CategoryDetail) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryDetailBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CategoryDetail) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryDetailAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CategoryDetail) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryDetailBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CategoryDetail) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryDetailAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CategoryDetail) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryDetailBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CategoryDetail) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryDetailAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CategoryDetail) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryDetailBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CategoryDetail) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range categoryDetailAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCategoryDetailHook registers your hook function for all future operations.
func AddCategoryDetailHook(hookPoint boil.HookPoint, categoryDetailHook CategoryDetailHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		categoryDetailAfterSelectMu.Lock()
		categoryDetailAfterSelectHooks = append(categoryDetailAfterSelectHooks, categoryDetailHook)
		categoryDetailAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		categoryDetailBeforeInsertMu.Lock()
		categoryDetailBeforeInsertHooks = append(categoryDetailBeforeInsertHooks, categoryDetailHook)
		categoryDetailBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		categoryDetailAfterInsertMu.Lock()
		categoryDetailAfterInsertHooks = append(categoryDetailAfterInsertHooks, categoryDetailHook)
		categoryDetailAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		categoryDetailBeforeUpdateMu.Lock()
		categoryDetailBeforeUpdateHooks = append(categoryDetailBeforeUpdateHooks, categoryDetailHook)
		categoryDetailBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		categoryDetailAfterUpdateMu.Lock()
		categoryDetailAfterUpdateHooks = append(categoryDetailAfterUpdateHooks, categoryDetailHook)
		categoryDetailAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		categoryDetailBeforeDeleteMu.Lock()
		categoryDetailBeforeDeleteHooks = append(categoryDetailBeforeDeleteHooks, categoryDetailHook)
		categoryDetailBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		categoryDetailAfterDeleteMu.Lock()
		categoryDetailAfterDeleteHooks = append(categoryDetailAfterDeleteHooks, categoryDetailHook)
		categoryDetailAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		categoryDetailBeforeUpsertMu.Lock()
		categoryDetailBeforeUpsertHooks = append(categoryDetailBeforeUpsertHooks, categoryDetailHook)
		categoryDetailBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		categoryDetailAfterUpsertMu.Lock()
		categoryDetailAfterUpsertHooks = append(categoryDetailAfterUpsertHooks, categoryDetailHook)
		categoryDetailAfterUpsertMu.Unlock()
	}
}

// One returns a single categoryDetail record from the query.
func (q categoryDetailQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CategoryDetail, error) {
	o := &CategoryDetail{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for category_detail")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CategoryDetail records from the query.
func (q categoryDetailQuery) All(ctx context.Context, exec boil.ContextExecutor) (CategoryDetailSlice, error) {
	var o []*CategoryDetail

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to CategoryDetail slice")
	}

	if len(categoryDetailAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CategoryDetail records in the query.
func (q categoryDetailQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count category_detail rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q categoryDetailQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if category_detail exists")
	}

	return count > 0, nil
}

// CategoryCategoryFields retrieves all the category_field's CategoryFields with an executor via category_id column.
func (o *CategoryDetail) CategoryCategoryFields(mods ...qm.QueryMod) categoryFieldQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"category_field\".\"category_id\"=?", o.ID),
	)

	return CategoryFields(queryMods...)
}

// CategoryLanguages retrieves all the language's Languages with an executor via category_id column.
func (o *CategoryDetail) CategoryLanguages(mods ...qm.QueryMod) languageQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"languages\".\"category_id\"=?", o.ID),
	)

	return Languages(queryMods...)
}

// LoadCategoryCategoryFields allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (categoryDetailL) LoadCategoryCategoryFields(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCategoryDetail interface{}, mods queries.Applicator) error {
	var slice []*CategoryDetail
	var object *CategoryDetail

	if singular {
		var ok bool
		object, ok = maybeCategoryDetail.(*CategoryDetail)
		if !ok {
			object = new(CategoryDetail)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCategoryDetail)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCategoryDetail))
			}
		}
	} else {
		s, ok := maybeCategoryDetail.(*[]*CategoryDetail)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCategoryDetail)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCategoryDetail))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &categoryDetailR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &categoryDetailR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`category_field`),
		qm.WhereIn(`category_field.category_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load category_field")
	}

	var resultSlice []*CategoryField
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice category_field")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on category_field")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for category_field")
	}

	if len(categoryFieldAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CategoryCategoryFields = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &categoryFieldR{}
			}
			foreign.R.Category = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CategoryID {
				local.R.CategoryCategoryFields = append(local.R.CategoryCategoryFields, foreign)
				if foreign.R == nil {
					foreign.R = &categoryFieldR{}
				}
				foreign.R.Category = local
				break
			}
		}
	}

	return nil
}

// LoadCategoryLanguages allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (categoryDetailL) LoadCategoryLanguages(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCategoryDetail interface{}, mods queries.Applicator) error {
	var slice []*CategoryDetail
	var object *CategoryDetail

	if singular {
		var ok bool
		object, ok = maybeCategoryDetail.(*CategoryDetail)
		if !ok {
			object = new(CategoryDetail)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCategoryDetail)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCategoryDetail))
			}
		}
	} else {
		s, ok := maybeCategoryDetail.(*[]*CategoryDetail)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCategoryDetail)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCategoryDetail))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &categoryDetailR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &categoryDetailR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`languages`),
		qm.WhereIn(`languages.category_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load languages")
	}

	var resultSlice []*Language
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice languages")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on languages")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for languages")
	}

	if len(languageAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CategoryLanguages = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &languageR{}
			}
			foreign.R.Category = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CategoryID {
				local.R.CategoryLanguages = append(local.R.CategoryLanguages, foreign)
				if foreign.R == nil {
					foreign.R = &languageR{}
				}
				foreign.R.Category = local
				break
			}
		}
	}

	return nil
}

// AddCategoryCategoryFields adds the given related objects to the existing relationships
// of the category_detail, optionally inserting them as new records.
// Appends related to o.R.CategoryCategoryFields.
// Sets related.R.Category appropriately.
func (o *CategoryDetail) AddCategoryCategoryFields(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CategoryField) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CategoryID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"category_field\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"category_id"}),
				strmangle.WhereClause("\"", "\"", 2, categoryFieldPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CategoryID = o.ID
		}
	}

	if o.R == nil {
		o.R = &categoryDetailR{
			CategoryCategoryFields: related,
		}
	} else {
		o.R.CategoryCategoryFields = append(o.R.CategoryCategoryFields, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &categoryFieldR{
				Category: o,
			}
		} else {
			rel.R.Category = o
		}
	}
	return nil
}

// AddCategoryLanguages adds the given related objects to the existing relationships
// of the category_detail, optionally inserting them as new records.
// Appends related to o.R.CategoryLanguages.
// Sets related.R.Category appropriately.
func (o *CategoryDetail) AddCategoryLanguages(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Language) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CategoryID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"languages\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"category_id"}),
				strmangle.WhereClause("\"", "\"", 2, languagePrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CategoryID = o.ID
		}
	}

	if o.R == nil {
		o.R = &categoryDetailR{
			CategoryLanguages: related,
		}
	} else {
		o.R.CategoryLanguages = append(o.R.CategoryLanguages, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &languageR{
				Category: o,
			}
		} else {
			rel.R.Category = o
		}
	}
	return nil
}

// CategoryDetails retrieves all the records using an executor.
func CategoryDetails(mods ...qm.QueryMod) categoryDetailQuery {
	mods = append(mods, qm.From("\"category_detail\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"category_detail\".*"})
	}

	return categoryDetailQuery{q}
}

// FindCategoryDetail retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCategoryDetail(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*CategoryDetail, error) {
	categoryDetailObj := &CategoryDetail{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"category_detail\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, categoryDetailObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from category_detail")
	}

	if err = categoryDetailObj.doAfterSelectHooks(ctx, exec); err != nil {
		return categoryDetailObj, err
	}

	return categoryDetailObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CategoryDetail) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no category_detail provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(categoryDetailColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	categoryDetailInsertCacheMut.RLock()
	cache, cached := categoryDetailInsertCache[key]
	categoryDetailInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			categoryDetailAllColumns,
			categoryDetailColumnsWithDefault,
			categoryDetailColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(categoryDetailType, categoryDetailMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(categoryDetailType, categoryDetailMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"category_detail\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"category_detail\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into category_detail")
	}

	if !cached {
		categoryDetailInsertCacheMut.Lock()
		categoryDetailInsertCache[key] = cache
		categoryDetailInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CategoryDetail.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CategoryDetail) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	categoryDetailUpdateCacheMut.RLock()
	cache, cached := categoryDetailUpdateCache[key]
	categoryDetailUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			categoryDetailAllColumns,
			categoryDetailPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update category_detail, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"category_detail\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, categoryDetailPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(categoryDetailType, categoryDetailMapping, append(wl, categoryDetailPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update category_detail row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for category_detail")
	}

	if !cached {
		categoryDetailUpdateCacheMut.Lock()
		categoryDetailUpdateCache[key] = cache
		categoryDetailUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q categoryDetailQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for category_detail")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for category_detail")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CategoryDetailSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), categoryDetailPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"category_detail\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, categoryDetailPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in categoryDetail slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all categoryDetail")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CategoryDetail) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no category_detail provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(categoryDetailColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	categoryDetailUpsertCacheMut.RLock()
	cache, cached := categoryDetailUpsertCache[key]
	categoryDetailUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			categoryDetailAllColumns,
			categoryDetailColumnsWithDefault,
			categoryDetailColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			categoryDetailAllColumns,
			categoryDetailPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert category_detail, could not build update column list")
		}

		ret := strmangle.SetComplement(categoryDetailAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(categoryDetailPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert category_detail, could not build conflict column list")
			}

			conflict = make([]string, len(categoryDetailPrimaryKeyColumns))
			copy(conflict, categoryDetailPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"category_detail\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(categoryDetailType, categoryDetailMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(categoryDetailType, categoryDetailMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert category_detail")
	}

	if !cached {
		categoryDetailUpsertCacheMut.Lock()
		categoryDetailUpsertCache[key] = cache
		categoryDetailUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CategoryDetail record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CategoryDetail) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no CategoryDetail provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), categoryDetailPrimaryKeyMapping)
	sql := "DELETE FROM \"category_detail\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from category_detail")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for category_detail")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q categoryDetailQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no categoryDetailQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from category_detail")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for category_detail")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CategoryDetailSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(categoryDetailBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), categoryDetailPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"category_detail\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, categoryDetailPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from categoryDetail slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for category_detail")
	}

	if len(categoryDetailAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CategoryDetail) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCategoryDetail(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CategoryDetailSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CategoryDetailSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), categoryDetailPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"category_detail\".* FROM \"category_detail\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, categoryDetailPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in CategoryDetailSlice")
	}

	*o = slice

	return nil
}

// CategoryDetailExists checks if the CategoryDetail row exists.
func CategoryDetailExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"category_detail\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if category_detail exists")
	}

	return exists, nil
}

// Exists checks if the CategoryDetail row exists.
func (o *CategoryDetail) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CategoryDetailExists(ctx, exec, o.ID)
}
