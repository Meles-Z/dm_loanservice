// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// UserMfaSetting is an object representing the database table.
type UserMfaSetting struct {
	ID              int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID          int       `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	PreferredMethod string    `boil:"preferred_method" json:"preferred_method" toml:"preferred_method" yaml:"preferred_method"`
	IsEnabled       null.Bool `boil:"is_enabled" json:"is_enabled,omitempty" toml:"is_enabled" yaml:"is_enabled,omitempty"`
	CreatedAt       null.Time `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt       null.Time `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *userMfaSettingR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userMfaSettingL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserMfaSettingColumns = struct {
	ID              string
	UserID          string
	PreferredMethod string
	IsEnabled       string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "id",
	UserID:          "user_id",
	PreferredMethod: "preferred_method",
	IsEnabled:       "is_enabled",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
}

var UserMfaSettingTableColumns = struct {
	ID              string
	UserID          string
	PreferredMethod string
	IsEnabled       string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "user_mfa_settings.id",
	UserID:          "user_mfa_settings.user_id",
	PreferredMethod: "user_mfa_settings.preferred_method",
	IsEnabled:       "user_mfa_settings.is_enabled",
	CreatedAt:       "user_mfa_settings.created_at",
	UpdatedAt:       "user_mfa_settings.updated_at",
}

// Generated where

var UserMfaSettingWhere = struct {
	ID              whereHelperint
	UserID          whereHelperint
	PreferredMethod whereHelperstring
	IsEnabled       whereHelpernull_Bool
	CreatedAt       whereHelpernull_Time
	UpdatedAt       whereHelpernull_Time
}{
	ID:              whereHelperint{field: "\"user_mfa_settings\".\"id\""},
	UserID:          whereHelperint{field: "\"user_mfa_settings\".\"user_id\""},
	PreferredMethod: whereHelperstring{field: "\"user_mfa_settings\".\"preferred_method\""},
	IsEnabled:       whereHelpernull_Bool{field: "\"user_mfa_settings\".\"is_enabled\""},
	CreatedAt:       whereHelpernull_Time{field: "\"user_mfa_settings\".\"created_at\""},
	UpdatedAt:       whereHelpernull_Time{field: "\"user_mfa_settings\".\"updated_at\""},
}

// UserMfaSettingRels is where relationship names are stored.
var UserMfaSettingRels = struct {
	User string
}{
	User: "User",
}

// userMfaSettingR is where relationships are stored.
type userMfaSettingR struct {
	User *User `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*userMfaSettingR) NewStruct() *userMfaSettingR {
	return &userMfaSettingR{}
}

func (o *UserMfaSetting) GetUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetUser()
}

func (r *userMfaSettingR) GetUser() *User {
	if r == nil {
		return nil
	}

	return r.User
}

// userMfaSettingL is where Load methods for each relationship are stored.
type userMfaSettingL struct{}

var (
	userMfaSettingAllColumns            = []string{"id", "user_id", "preferred_method", "is_enabled", "created_at", "updated_at"}
	userMfaSettingColumnsWithoutDefault = []string{"user_id", "preferred_method"}
	userMfaSettingColumnsWithDefault    = []string{"id", "is_enabled", "created_at", "updated_at"}
	userMfaSettingPrimaryKeyColumns     = []string{"id"}
	userMfaSettingGeneratedColumns      = []string{}
)

type (
	// UserMfaSettingSlice is an alias for a slice of pointers to UserMfaSetting.
	// This should almost always be used instead of []UserMfaSetting.
	UserMfaSettingSlice []*UserMfaSetting
	// UserMfaSettingHook is the signature for custom UserMfaSetting hook methods
	UserMfaSettingHook func(context.Context, boil.ContextExecutor, *UserMfaSetting) error

	userMfaSettingQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userMfaSettingType                 = reflect.TypeOf(&UserMfaSetting{})
	userMfaSettingMapping              = queries.MakeStructMapping(userMfaSettingType)
	userMfaSettingPrimaryKeyMapping, _ = queries.BindMapping(userMfaSettingType, userMfaSettingMapping, userMfaSettingPrimaryKeyColumns)
	userMfaSettingInsertCacheMut       sync.RWMutex
	userMfaSettingInsertCache          = make(map[string]insertCache)
	userMfaSettingUpdateCacheMut       sync.RWMutex
	userMfaSettingUpdateCache          = make(map[string]updateCache)
	userMfaSettingUpsertCacheMut       sync.RWMutex
	userMfaSettingUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var userMfaSettingAfterSelectMu sync.Mutex
var userMfaSettingAfterSelectHooks []UserMfaSettingHook

var userMfaSettingBeforeInsertMu sync.Mutex
var userMfaSettingBeforeInsertHooks []UserMfaSettingHook
var userMfaSettingAfterInsertMu sync.Mutex
var userMfaSettingAfterInsertHooks []UserMfaSettingHook

var userMfaSettingBeforeUpdateMu sync.Mutex
var userMfaSettingBeforeUpdateHooks []UserMfaSettingHook
var userMfaSettingAfterUpdateMu sync.Mutex
var userMfaSettingAfterUpdateHooks []UserMfaSettingHook

var userMfaSettingBeforeDeleteMu sync.Mutex
var userMfaSettingBeforeDeleteHooks []UserMfaSettingHook
var userMfaSettingAfterDeleteMu sync.Mutex
var userMfaSettingAfterDeleteHooks []UserMfaSettingHook

var userMfaSettingBeforeUpsertMu sync.Mutex
var userMfaSettingBeforeUpsertHooks []UserMfaSettingHook
var userMfaSettingAfterUpsertMu sync.Mutex
var userMfaSettingAfterUpsertHooks []UserMfaSettingHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UserMfaSetting) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMfaSettingAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UserMfaSetting) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMfaSettingBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UserMfaSetting) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMfaSettingAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UserMfaSetting) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMfaSettingBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UserMfaSetting) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMfaSettingAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UserMfaSetting) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMfaSettingBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UserMfaSetting) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMfaSettingAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UserMfaSetting) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMfaSettingBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UserMfaSetting) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMfaSettingAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUserMfaSettingHook registers your hook function for all future operations.
func AddUserMfaSettingHook(hookPoint boil.HookPoint, userMfaSettingHook UserMfaSettingHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		userMfaSettingAfterSelectMu.Lock()
		userMfaSettingAfterSelectHooks = append(userMfaSettingAfterSelectHooks, userMfaSettingHook)
		userMfaSettingAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		userMfaSettingBeforeInsertMu.Lock()
		userMfaSettingBeforeInsertHooks = append(userMfaSettingBeforeInsertHooks, userMfaSettingHook)
		userMfaSettingBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		userMfaSettingAfterInsertMu.Lock()
		userMfaSettingAfterInsertHooks = append(userMfaSettingAfterInsertHooks, userMfaSettingHook)
		userMfaSettingAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		userMfaSettingBeforeUpdateMu.Lock()
		userMfaSettingBeforeUpdateHooks = append(userMfaSettingBeforeUpdateHooks, userMfaSettingHook)
		userMfaSettingBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		userMfaSettingAfterUpdateMu.Lock()
		userMfaSettingAfterUpdateHooks = append(userMfaSettingAfterUpdateHooks, userMfaSettingHook)
		userMfaSettingAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		userMfaSettingBeforeDeleteMu.Lock()
		userMfaSettingBeforeDeleteHooks = append(userMfaSettingBeforeDeleteHooks, userMfaSettingHook)
		userMfaSettingBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		userMfaSettingAfterDeleteMu.Lock()
		userMfaSettingAfterDeleteHooks = append(userMfaSettingAfterDeleteHooks, userMfaSettingHook)
		userMfaSettingAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		userMfaSettingBeforeUpsertMu.Lock()
		userMfaSettingBeforeUpsertHooks = append(userMfaSettingBeforeUpsertHooks, userMfaSettingHook)
		userMfaSettingBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		userMfaSettingAfterUpsertMu.Lock()
		userMfaSettingAfterUpsertHooks = append(userMfaSettingAfterUpsertHooks, userMfaSettingHook)
		userMfaSettingAfterUpsertMu.Unlock()
	}
}

// One returns a single userMfaSetting record from the query.
func (q userMfaSettingQuery) One(ctx context.Context, exec boil.ContextExecutor) (*UserMfaSetting, error) {
	o := &UserMfaSetting{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for user_mfa_settings")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all UserMfaSetting records from the query.
func (q userMfaSettingQuery) All(ctx context.Context, exec boil.ContextExecutor) (UserMfaSettingSlice, error) {
	var o []*UserMfaSetting

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to UserMfaSetting slice")
	}

	if len(userMfaSettingAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all UserMfaSetting records in the query.
func (q userMfaSettingQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count user_mfa_settings rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q userMfaSettingQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if user_mfa_settings exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *UserMfaSetting) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userMfaSettingL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUserMfaSetting interface{}, mods queries.Applicator) error {
	var slice []*UserMfaSetting
	var object *UserMfaSetting

	if singular {
		var ok bool
		object, ok = maybeUserMfaSetting.(*UserMfaSetting)
		if !ok {
			object = new(UserMfaSetting)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUserMfaSetting)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUserMfaSetting))
			}
		}
	} else {
		s, ok := maybeUserMfaSetting.(*[]*UserMfaSetting)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUserMfaSetting)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUserMfaSetting))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &userMfaSettingR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userMfaSettingR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.UserMfaSettings = append(foreign.R.UserMfaSettings, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.UserMfaSettings = append(foreign.R.UserMfaSettings, local)
				break
			}
		}
	}

	return nil
}

// SetUser of the userMfaSetting to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserMfaSettings.
func (o *UserMfaSetting) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"user_mfa_settings\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, userMfaSettingPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &userMfaSettingR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			UserMfaSettings: UserMfaSettingSlice{o},
		}
	} else {
		related.R.UserMfaSettings = append(related.R.UserMfaSettings, o)
	}

	return nil
}

// UserMfaSettings retrieves all the records using an executor.
func UserMfaSettings(mods ...qm.QueryMod) userMfaSettingQuery {
	mods = append(mods, qm.From("\"user_mfa_settings\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"user_mfa_settings\".*"})
	}

	return userMfaSettingQuery{q}
}

// FindUserMfaSetting retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUserMfaSetting(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*UserMfaSetting, error) {
	userMfaSettingObj := &UserMfaSetting{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"user_mfa_settings\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, userMfaSettingObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from user_mfa_settings")
	}

	if err = userMfaSettingObj.doAfterSelectHooks(ctx, exec); err != nil {
		return userMfaSettingObj, err
	}

	return userMfaSettingObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UserMfaSetting) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no user_mfa_settings provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userMfaSettingColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userMfaSettingInsertCacheMut.RLock()
	cache, cached := userMfaSettingInsertCache[key]
	userMfaSettingInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userMfaSettingAllColumns,
			userMfaSettingColumnsWithDefault,
			userMfaSettingColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(userMfaSettingType, userMfaSettingMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userMfaSettingType, userMfaSettingMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"user_mfa_settings\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"user_mfa_settings\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into user_mfa_settings")
	}

	if !cached {
		userMfaSettingInsertCacheMut.Lock()
		userMfaSettingInsertCache[key] = cache
		userMfaSettingInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the UserMfaSetting.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UserMfaSetting) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	userMfaSettingUpdateCacheMut.RLock()
	cache, cached := userMfaSettingUpdateCache[key]
	userMfaSettingUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userMfaSettingAllColumns,
			userMfaSettingPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update user_mfa_settings, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"user_mfa_settings\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, userMfaSettingPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userMfaSettingType, userMfaSettingMapping, append(wl, userMfaSettingPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update user_mfa_settings row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for user_mfa_settings")
	}

	if !cached {
		userMfaSettingUpdateCacheMut.Lock()
		userMfaSettingUpdateCache[key] = cache
		userMfaSettingUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q userMfaSettingQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for user_mfa_settings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for user_mfa_settings")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserMfaSettingSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userMfaSettingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"user_mfa_settings\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, userMfaSettingPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in userMfaSetting slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all userMfaSetting")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UserMfaSetting) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no user_mfa_settings provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userMfaSettingColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userMfaSettingUpsertCacheMut.RLock()
	cache, cached := userMfaSettingUpsertCache[key]
	userMfaSettingUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			userMfaSettingAllColumns,
			userMfaSettingColumnsWithDefault,
			userMfaSettingColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			userMfaSettingAllColumns,
			userMfaSettingPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert user_mfa_settings, could not build update column list")
		}

		ret := strmangle.SetComplement(userMfaSettingAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(userMfaSettingPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert user_mfa_settings, could not build conflict column list")
			}

			conflict = make([]string, len(userMfaSettingPrimaryKeyColumns))
			copy(conflict, userMfaSettingPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"user_mfa_settings\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(userMfaSettingType, userMfaSettingMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userMfaSettingType, userMfaSettingMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert user_mfa_settings")
	}

	if !cached {
		userMfaSettingUpsertCacheMut.Lock()
		userMfaSettingUpsertCache[key] = cache
		userMfaSettingUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single UserMfaSetting record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UserMfaSetting) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no UserMfaSetting provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userMfaSettingPrimaryKeyMapping)
	sql := "DELETE FROM \"user_mfa_settings\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from user_mfa_settings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for user_mfa_settings")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q userMfaSettingQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no userMfaSettingQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from user_mfa_settings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for user_mfa_settings")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserMfaSettingSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(userMfaSettingBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userMfaSettingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"user_mfa_settings\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userMfaSettingPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from userMfaSetting slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for user_mfa_settings")
	}

	if len(userMfaSettingAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UserMfaSetting) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUserMfaSetting(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserMfaSettingSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserMfaSettingSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userMfaSettingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"user_mfa_settings\".* FROM \"user_mfa_settings\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userMfaSettingPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in UserMfaSettingSlice")
	}

	*o = slice

	return nil
}

// UserMfaSettingExists checks if the UserMfaSetting row exists.
func UserMfaSettingExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"user_mfa_settings\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if user_mfa_settings exists")
	}

	return exists, nil
}

// Exists checks if the UserMfaSetting row exists.
func (o *UserMfaSetting) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return UserMfaSettingExists(ctx, exec, o.ID)
}
