// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// QuoteResponse is an object representing the database table.
type QuoteResponse struct {
	ID        string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	ExpiresAt time.Time `boil:"expires_at" json:"expires_at" toml:"expires_at" yaml:"expires_at"`
	IP        string    `boil:"ip" json:"ip" toml:"ip" yaml:"ip"`
	UserID    int       `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Type      string    `boil:"type" json:"type" toml:"type" yaml:"type"`

	R *quoteResponseR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L quoteResponseL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var QuoteResponseColumns = struct {
	ID        string
	CreatedAt string
	UpdatedAt string
	ExpiresAt string
	IP        string
	UserID    string
	Type      string
}{
	ID:        "id",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
	ExpiresAt: "expires_at",
	IP:        "ip",
	UserID:    "user_id",
	Type:      "type",
}

var QuoteResponseTableColumns = struct {
	ID        string
	CreatedAt string
	UpdatedAt string
	ExpiresAt string
	IP        string
	UserID    string
	Type      string
}{
	ID:        "quote_response.id",
	CreatedAt: "quote_response.created_at",
	UpdatedAt: "quote_response.updated_at",
	ExpiresAt: "quote_response.expires_at",
	IP:        "quote_response.ip",
	UserID:    "quote_response.user_id",
	Type:      "quote_response.type",
}

// Generated where

var QuoteResponseWhere = struct {
	ID        whereHelperstring
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
	ExpiresAt whereHelpertime_Time
	IP        whereHelperstring
	UserID    whereHelperint
	Type      whereHelperstring
}{
	ID:        whereHelperstring{field: "\"quote_response\".\"id\""},
	CreatedAt: whereHelpertime_Time{field: "\"quote_response\".\"created_at\""},
	UpdatedAt: whereHelpertime_Time{field: "\"quote_response\".\"updated_at\""},
	ExpiresAt: whereHelpertime_Time{field: "\"quote_response\".\"expires_at\""},
	IP:        whereHelperstring{field: "\"quote_response\".\"ip\""},
	UserID:    whereHelperint{field: "\"quote_response\".\"user_id\""},
	Type:      whereHelperstring{field: "\"quote_response\".\"type\""},
}

// QuoteResponseRels is where relationship names are stored.
var QuoteResponseRels = struct {
	User           string
	Quote          string
	QuoteOperation string
}{
	User:           "User",
	Quote:          "Quote",
	QuoteOperation: "QuoteOperation",
}

// quoteResponseR is where relationships are stored.
type quoteResponseR struct {
	User           *User           `boil:"User" json:"User" toml:"User" yaml:"User"`
	Quote          *Quote          `boil:"Quote" json:"Quote" toml:"Quote" yaml:"Quote"`
	QuoteOperation *QuoteOperation `boil:"QuoteOperation" json:"QuoteOperation" toml:"QuoteOperation" yaml:"QuoteOperation"`
}

// NewStruct creates a new relationship struct
func (*quoteResponseR) NewStruct() *quoteResponseR {
	return &quoteResponseR{}
}

func (o *QuoteResponse) GetUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetUser()
}

func (r *quoteResponseR) GetUser() *User {
	if r == nil {
		return nil
	}

	return r.User
}

func (o *QuoteResponse) GetQuote() *Quote {
	if o == nil {
		return nil
	}

	return o.R.GetQuote()
}

func (r *quoteResponseR) GetQuote() *Quote {
	if r == nil {
		return nil
	}

	return r.Quote
}

func (o *QuoteResponse) GetQuoteOperation() *QuoteOperation {
	if o == nil {
		return nil
	}

	return o.R.GetQuoteOperation()
}

func (r *quoteResponseR) GetQuoteOperation() *QuoteOperation {
	if r == nil {
		return nil
	}

	return r.QuoteOperation
}

// quoteResponseL is where Load methods for each relationship are stored.
type quoteResponseL struct{}

var (
	quoteResponseAllColumns            = []string{"id", "created_at", "updated_at", "expires_at", "ip", "user_id", "type"}
	quoteResponseColumnsWithoutDefault = []string{"id", "updated_at", "expires_at", "ip", "user_id"}
	quoteResponseColumnsWithDefault    = []string{"created_at", "type"}
	quoteResponsePrimaryKeyColumns     = []string{"id"}
	quoteResponseGeneratedColumns      = []string{}
)

type (
	// QuoteResponseSlice is an alias for a slice of pointers to QuoteResponse.
	// This should almost always be used instead of []QuoteResponse.
	QuoteResponseSlice []*QuoteResponse
	// QuoteResponseHook is the signature for custom QuoteResponse hook methods
	QuoteResponseHook func(context.Context, boil.ContextExecutor, *QuoteResponse) error

	quoteResponseQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	quoteResponseType                 = reflect.TypeOf(&QuoteResponse{})
	quoteResponseMapping              = queries.MakeStructMapping(quoteResponseType)
	quoteResponsePrimaryKeyMapping, _ = queries.BindMapping(quoteResponseType, quoteResponseMapping, quoteResponsePrimaryKeyColumns)
	quoteResponseInsertCacheMut       sync.RWMutex
	quoteResponseInsertCache          = make(map[string]insertCache)
	quoteResponseUpdateCacheMut       sync.RWMutex
	quoteResponseUpdateCache          = make(map[string]updateCache)
	quoteResponseUpsertCacheMut       sync.RWMutex
	quoteResponseUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var quoteResponseAfterSelectMu sync.Mutex
var quoteResponseAfterSelectHooks []QuoteResponseHook

var quoteResponseBeforeInsertMu sync.Mutex
var quoteResponseBeforeInsertHooks []QuoteResponseHook
var quoteResponseAfterInsertMu sync.Mutex
var quoteResponseAfterInsertHooks []QuoteResponseHook

var quoteResponseBeforeUpdateMu sync.Mutex
var quoteResponseBeforeUpdateHooks []QuoteResponseHook
var quoteResponseAfterUpdateMu sync.Mutex
var quoteResponseAfterUpdateHooks []QuoteResponseHook

var quoteResponseBeforeDeleteMu sync.Mutex
var quoteResponseBeforeDeleteHooks []QuoteResponseHook
var quoteResponseAfterDeleteMu sync.Mutex
var quoteResponseAfterDeleteHooks []QuoteResponseHook

var quoteResponseBeforeUpsertMu sync.Mutex
var quoteResponseBeforeUpsertHooks []QuoteResponseHook
var quoteResponseAfterUpsertMu sync.Mutex
var quoteResponseAfterUpsertHooks []QuoteResponseHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *QuoteResponse) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteResponseAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *QuoteResponse) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteResponseBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *QuoteResponse) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteResponseAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *QuoteResponse) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteResponseBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *QuoteResponse) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteResponseAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *QuoteResponse) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteResponseBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *QuoteResponse) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteResponseAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *QuoteResponse) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteResponseBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *QuoteResponse) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteResponseAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddQuoteResponseHook registers your hook function for all future operations.
func AddQuoteResponseHook(hookPoint boil.HookPoint, quoteResponseHook QuoteResponseHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		quoteResponseAfterSelectMu.Lock()
		quoteResponseAfterSelectHooks = append(quoteResponseAfterSelectHooks, quoteResponseHook)
		quoteResponseAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		quoteResponseBeforeInsertMu.Lock()
		quoteResponseBeforeInsertHooks = append(quoteResponseBeforeInsertHooks, quoteResponseHook)
		quoteResponseBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		quoteResponseAfterInsertMu.Lock()
		quoteResponseAfterInsertHooks = append(quoteResponseAfterInsertHooks, quoteResponseHook)
		quoteResponseAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		quoteResponseBeforeUpdateMu.Lock()
		quoteResponseBeforeUpdateHooks = append(quoteResponseBeforeUpdateHooks, quoteResponseHook)
		quoteResponseBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		quoteResponseAfterUpdateMu.Lock()
		quoteResponseAfterUpdateHooks = append(quoteResponseAfterUpdateHooks, quoteResponseHook)
		quoteResponseAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		quoteResponseBeforeDeleteMu.Lock()
		quoteResponseBeforeDeleteHooks = append(quoteResponseBeforeDeleteHooks, quoteResponseHook)
		quoteResponseBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		quoteResponseAfterDeleteMu.Lock()
		quoteResponseAfterDeleteHooks = append(quoteResponseAfterDeleteHooks, quoteResponseHook)
		quoteResponseAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		quoteResponseBeforeUpsertMu.Lock()
		quoteResponseBeforeUpsertHooks = append(quoteResponseBeforeUpsertHooks, quoteResponseHook)
		quoteResponseBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		quoteResponseAfterUpsertMu.Lock()
		quoteResponseAfterUpsertHooks = append(quoteResponseAfterUpsertHooks, quoteResponseHook)
		quoteResponseAfterUpsertMu.Unlock()
	}
}

// One returns a single quoteResponse record from the query.
func (q quoteResponseQuery) One(ctx context.Context, exec boil.ContextExecutor) (*QuoteResponse, error) {
	o := &QuoteResponse{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for quote_response")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all QuoteResponse records from the query.
func (q quoteResponseQuery) All(ctx context.Context, exec boil.ContextExecutor) (QuoteResponseSlice, error) {
	var o []*QuoteResponse

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to QuoteResponse slice")
	}

	if len(quoteResponseAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all QuoteResponse records in the query.
func (q quoteResponseQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count quote_response rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q quoteResponseQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if quote_response exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *QuoteResponse) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// Quote pointed to by the foreign key.
func (o *QuoteResponse) Quote(mods ...qm.QueryMod) quoteQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"quote_response_id\" = ?", o.ID),
	}

	queryMods = append(queryMods, mods...)

	return Quotes(queryMods...)
}

// QuoteOperation pointed to by the foreign key.
func (o *QuoteResponse) QuoteOperation(mods ...qm.QueryMod) quoteOperationQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"quote_response_id\" = ?", o.ID),
	}

	queryMods = append(queryMods, mods...)

	return QuoteOperations(queryMods...)
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (quoteResponseL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeQuoteResponse interface{}, mods queries.Applicator) error {
	var slice []*QuoteResponse
	var object *QuoteResponse

	if singular {
		var ok bool
		object, ok = maybeQuoteResponse.(*QuoteResponse)
		if !ok {
			object = new(QuoteResponse)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeQuoteResponse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeQuoteResponse))
			}
		}
	} else {
		s, ok := maybeQuoteResponse.(*[]*QuoteResponse)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeQuoteResponse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeQuoteResponse))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &quoteResponseR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &quoteResponseR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.QuoteResponses = append(foreign.R.QuoteResponses, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.QuoteResponses = append(foreign.R.QuoteResponses, local)
				break
			}
		}
	}

	return nil
}

// LoadQuote allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-1 relationship.
func (quoteResponseL) LoadQuote(ctx context.Context, e boil.ContextExecutor, singular bool, maybeQuoteResponse interface{}, mods queries.Applicator) error {
	var slice []*QuoteResponse
	var object *QuoteResponse

	if singular {
		var ok bool
		object, ok = maybeQuoteResponse.(*QuoteResponse)
		if !ok {
			object = new(QuoteResponse)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeQuoteResponse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeQuoteResponse))
			}
		}
	} else {
		s, ok := maybeQuoteResponse.(*[]*QuoteResponse)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeQuoteResponse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeQuoteResponse))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &quoteResponseR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &quoteResponseR{}
			}

			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`quote`),
		qm.WhereIn(`quote.quote_response_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Quote")
	}

	var resultSlice []*Quote
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Quote")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for quote")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for quote")
	}

	if len(quoteAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Quote = foreign
		if foreign.R == nil {
			foreign.R = &quoteR{}
		}
		foreign.R.QuoteResponse = object
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ID, foreign.QuoteResponseID) {
				local.R.Quote = foreign
				if foreign.R == nil {
					foreign.R = &quoteR{}
				}
				foreign.R.QuoteResponse = local
				break
			}
		}
	}

	return nil
}

// LoadQuoteOperation allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-1 relationship.
func (quoteResponseL) LoadQuoteOperation(ctx context.Context, e boil.ContextExecutor, singular bool, maybeQuoteResponse interface{}, mods queries.Applicator) error {
	var slice []*QuoteResponse
	var object *QuoteResponse

	if singular {
		var ok bool
		object, ok = maybeQuoteResponse.(*QuoteResponse)
		if !ok {
			object = new(QuoteResponse)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeQuoteResponse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeQuoteResponse))
			}
		}
	} else {
		s, ok := maybeQuoteResponse.(*[]*QuoteResponse)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeQuoteResponse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeQuoteResponse))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &quoteResponseR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &quoteResponseR{}
			}

			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`quote_operation`),
		qm.WhereIn(`quote_operation.quote_response_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load QuoteOperation")
	}

	var resultSlice []*QuoteOperation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice QuoteOperation")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for quote_operation")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for quote_operation")
	}

	if len(quoteOperationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.QuoteOperation = foreign
		if foreign.R == nil {
			foreign.R = &quoteOperationR{}
		}
		foreign.R.QuoteResponse = object
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ID, foreign.QuoteResponseID) {
				local.R.QuoteOperation = foreign
				if foreign.R == nil {
					foreign.R = &quoteOperationR{}
				}
				foreign.R.QuoteResponse = local
				break
			}
		}
	}

	return nil
}

// SetUser of the quoteResponse to the related item.
// Sets o.R.User to related.
// Adds o to related.R.QuoteResponses.
func (o *QuoteResponse) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"quote_response\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, quoteResponsePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &quoteResponseR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			QuoteResponses: QuoteResponseSlice{o},
		}
	} else {
		related.R.QuoteResponses = append(related.R.QuoteResponses, o)
	}

	return nil
}

// SetQuote of the quoteResponse to the related item.
// Sets o.R.Quote to related.
// Adds o to related.R.QuoteResponse.
func (o *QuoteResponse) SetQuote(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Quote) error {
	var err error

	if insert {
		queries.Assign(&related.QuoteResponseID, o.ID)

		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	} else {
		updateQuery := fmt.Sprintf(
			"UPDATE \"quote\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, []string{"quote_response_id"}),
			strmangle.WhereClause("\"", "\"", 2, quotePrimaryKeyColumns),
		)
		values := []interface{}{o.ID, related.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, updateQuery)
			fmt.Fprintln(writer, values)
		}
		if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
			return errors.Wrap(err, "failed to update foreign table")
		}

		queries.Assign(&related.QuoteResponseID, o.ID)
	}

	if o.R == nil {
		o.R = &quoteResponseR{
			Quote: related,
		}
	} else {
		o.R.Quote = related
	}

	if related.R == nil {
		related.R = &quoteR{
			QuoteResponse: o,
		}
	} else {
		related.R.QuoteResponse = o
	}
	return nil
}

// RemoveQuote relationship.
// Sets o.R.Quote to nil.
// Removes o from all passed in related items' relationships struct.
func (o *QuoteResponse) RemoveQuote(ctx context.Context, exec boil.ContextExecutor, related *Quote) error {
	var err error

	queries.SetScanner(&related.QuoteResponseID, nil)
	if _, err = related.Update(ctx, exec, boil.Whitelist("quote_response_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Quote = nil
	}

	if related == nil || related.R == nil {
		return nil
	}

	related.R.QuoteResponse = nil

	return nil
}

// SetQuoteOperation of the quoteResponse to the related item.
// Sets o.R.QuoteOperation to related.
// Adds o to related.R.QuoteResponse.
func (o *QuoteResponse) SetQuoteOperation(ctx context.Context, exec boil.ContextExecutor, insert bool, related *QuoteOperation) error {
	var err error

	if insert {
		queries.Assign(&related.QuoteResponseID, o.ID)

		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	} else {
		updateQuery := fmt.Sprintf(
			"UPDATE \"quote_operation\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, []string{"quote_response_id"}),
			strmangle.WhereClause("\"", "\"", 2, quoteOperationPrimaryKeyColumns),
		)
		values := []interface{}{o.ID, related.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, updateQuery)
			fmt.Fprintln(writer, values)
		}
		if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
			return errors.Wrap(err, "failed to update foreign table")
		}

		queries.Assign(&related.QuoteResponseID, o.ID)
	}

	if o.R == nil {
		o.R = &quoteResponseR{
			QuoteOperation: related,
		}
	} else {
		o.R.QuoteOperation = related
	}

	if related.R == nil {
		related.R = &quoteOperationR{
			QuoteResponse: o,
		}
	} else {
		related.R.QuoteResponse = o
	}
	return nil
}

// RemoveQuoteOperation relationship.
// Sets o.R.QuoteOperation to nil.
// Removes o from all passed in related items' relationships struct.
func (o *QuoteResponse) RemoveQuoteOperation(ctx context.Context, exec boil.ContextExecutor, related *QuoteOperation) error {
	var err error

	queries.SetScanner(&related.QuoteResponseID, nil)
	if _, err = related.Update(ctx, exec, boil.Whitelist("quote_response_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.QuoteOperation = nil
	}

	if related == nil || related.R == nil {
		return nil
	}

	related.R.QuoteResponse = nil

	return nil
}

// QuoteResponses retrieves all the records using an executor.
func QuoteResponses(mods ...qm.QueryMod) quoteResponseQuery {
	mods = append(mods, qm.From("\"quote_response\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"quote_response\".*"})
	}

	return quoteResponseQuery{q}
}

// FindQuoteResponse retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindQuoteResponse(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*QuoteResponse, error) {
	quoteResponseObj := &QuoteResponse{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"quote_response\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, quoteResponseObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from quote_response")
	}

	if err = quoteResponseObj.doAfterSelectHooks(ctx, exec); err != nil {
		return quoteResponseObj, err
	}

	return quoteResponseObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *QuoteResponse) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no quote_response provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(quoteResponseColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	quoteResponseInsertCacheMut.RLock()
	cache, cached := quoteResponseInsertCache[key]
	quoteResponseInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			quoteResponseAllColumns,
			quoteResponseColumnsWithDefault,
			quoteResponseColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(quoteResponseType, quoteResponseMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(quoteResponseType, quoteResponseMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"quote_response\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"quote_response\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into quote_response")
	}

	if !cached {
		quoteResponseInsertCacheMut.Lock()
		quoteResponseInsertCache[key] = cache
		quoteResponseInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the QuoteResponse.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *QuoteResponse) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	quoteResponseUpdateCacheMut.RLock()
	cache, cached := quoteResponseUpdateCache[key]
	quoteResponseUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			quoteResponseAllColumns,
			quoteResponsePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update quote_response, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"quote_response\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, quoteResponsePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(quoteResponseType, quoteResponseMapping, append(wl, quoteResponsePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update quote_response row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for quote_response")
	}

	if !cached {
		quoteResponseUpdateCacheMut.Lock()
		quoteResponseUpdateCache[key] = cache
		quoteResponseUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q quoteResponseQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for quote_response")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for quote_response")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o QuoteResponseSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), quoteResponsePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"quote_response\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, quoteResponsePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in quoteResponse slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all quoteResponse")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *QuoteResponse) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no quote_response provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(quoteResponseColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	quoteResponseUpsertCacheMut.RLock()
	cache, cached := quoteResponseUpsertCache[key]
	quoteResponseUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			quoteResponseAllColumns,
			quoteResponseColumnsWithDefault,
			quoteResponseColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			quoteResponseAllColumns,
			quoteResponsePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert quote_response, could not build update column list")
		}

		ret := strmangle.SetComplement(quoteResponseAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(quoteResponsePrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert quote_response, could not build conflict column list")
			}

			conflict = make([]string, len(quoteResponsePrimaryKeyColumns))
			copy(conflict, quoteResponsePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"quote_response\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(quoteResponseType, quoteResponseMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(quoteResponseType, quoteResponseMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert quote_response")
	}

	if !cached {
		quoteResponseUpsertCacheMut.Lock()
		quoteResponseUpsertCache[key] = cache
		quoteResponseUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single QuoteResponse record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *QuoteResponse) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no QuoteResponse provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), quoteResponsePrimaryKeyMapping)
	sql := "DELETE FROM \"quote_response\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from quote_response")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for quote_response")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q quoteResponseQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no quoteResponseQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from quote_response")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for quote_response")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o QuoteResponseSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(quoteResponseBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), quoteResponsePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"quote_response\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, quoteResponsePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from quoteResponse slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for quote_response")
	}

	if len(quoteResponseAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *QuoteResponse) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindQuoteResponse(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *QuoteResponseSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := QuoteResponseSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), quoteResponsePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"quote_response\".* FROM \"quote_response\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, quoteResponsePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in QuoteResponseSlice")
	}

	*o = slice

	return nil
}

// QuoteResponseExists checks if the QuoteResponse row exists.
func QuoteResponseExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"quote_response\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if quote_response exists")
	}

	return exists, nil
}

// Exists checks if the QuoteResponse row exists.
func (o *QuoteResponse) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return QuoteResponseExists(ctx, exec, o.ID)
}
