// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// RepaymentPlan is an object representing the database table.
type RepaymentPlan struct {
	ID                string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	AccountID         string    `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	CreatedBy         int       `boil:"created_by" json:"created_by" toml:"created_by" yaml:"created_by"`
	StartDate         time.Time `boil:"start_date" json:"start_date" toml:"start_date" yaml:"start_date"`
	EndDate           time.Time `boil:"end_date" json:"end_date" toml:"end_date" yaml:"end_date"`
	Frequency         string    `boil:"frequency" json:"frequency" toml:"frequency" yaml:"frequency"`
	InstallmentAmount string    `boil:"installment_amount" json:"installment_amount" toml:"installment_amount" yaml:"installment_amount"`
	Status            string    `boil:"status" json:"status" toml:"status" yaml:"status"`
	TotalDue          string    `boil:"total_due" json:"total_due" toml:"total_due" yaml:"total_due"`
	TotalPaid         string    `boil:"total_paid" json:"total_paid" toml:"total_paid" yaml:"total_paid"`
	ApprovalRequired  bool      `boil:"approval_required" json:"approval_required" toml:"approval_required" yaml:"approval_required"`
	ApprovedBy        null.Int  `boil:"approved_by" json:"approved_by,omitempty" toml:"approved_by" yaml:"approved_by,omitempty"`
	ApprovedAt        null.Time `boil:"approved_at" json:"approved_at,omitempty" toml:"approved_at" yaml:"approved_at,omitempty"`
	CreatedAt         time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt         time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *repaymentPlanR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L repaymentPlanL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RepaymentPlanColumns = struct {
	ID                string
	AccountID         string
	CreatedBy         string
	StartDate         string
	EndDate           string
	Frequency         string
	InstallmentAmount string
	Status            string
	TotalDue          string
	TotalPaid         string
	ApprovalRequired  string
	ApprovedBy        string
	ApprovedAt        string
	CreatedAt         string
	UpdatedAt         string
}{
	ID:                "id",
	AccountID:         "account_id",
	CreatedBy:         "created_by",
	StartDate:         "start_date",
	EndDate:           "end_date",
	Frequency:         "frequency",
	InstallmentAmount: "installment_amount",
	Status:            "status",
	TotalDue:          "total_due",
	TotalPaid:         "total_paid",
	ApprovalRequired:  "approval_required",
	ApprovedBy:        "approved_by",
	ApprovedAt:        "approved_at",
	CreatedAt:         "created_at",
	UpdatedAt:         "updated_at",
}

var RepaymentPlanTableColumns = struct {
	ID                string
	AccountID         string
	CreatedBy         string
	StartDate         string
	EndDate           string
	Frequency         string
	InstallmentAmount string
	Status            string
	TotalDue          string
	TotalPaid         string
	ApprovalRequired  string
	ApprovedBy        string
	ApprovedAt        string
	CreatedAt         string
	UpdatedAt         string
}{
	ID:                "repayment_plans.id",
	AccountID:         "repayment_plans.account_id",
	CreatedBy:         "repayment_plans.created_by",
	StartDate:         "repayment_plans.start_date",
	EndDate:           "repayment_plans.end_date",
	Frequency:         "repayment_plans.frequency",
	InstallmentAmount: "repayment_plans.installment_amount",
	Status:            "repayment_plans.status",
	TotalDue:          "repayment_plans.total_due",
	TotalPaid:         "repayment_plans.total_paid",
	ApprovalRequired:  "repayment_plans.approval_required",
	ApprovedBy:        "repayment_plans.approved_by",
	ApprovedAt:        "repayment_plans.approved_at",
	CreatedAt:         "repayment_plans.created_at",
	UpdatedAt:         "repayment_plans.updated_at",
}

// Generated where

var RepaymentPlanWhere = struct {
	ID                whereHelperstring
	AccountID         whereHelperstring
	CreatedBy         whereHelperint
	StartDate         whereHelpertime_Time
	EndDate           whereHelpertime_Time
	Frequency         whereHelperstring
	InstallmentAmount whereHelperstring
	Status            whereHelperstring
	TotalDue          whereHelperstring
	TotalPaid         whereHelperstring
	ApprovalRequired  whereHelperbool
	ApprovedBy        whereHelpernull_Int
	ApprovedAt        whereHelpernull_Time
	CreatedAt         whereHelpertime_Time
	UpdatedAt         whereHelpertime_Time
}{
	ID:                whereHelperstring{field: "\"repayment_plans\".\"id\""},
	AccountID:         whereHelperstring{field: "\"repayment_plans\".\"account_id\""},
	CreatedBy:         whereHelperint{field: "\"repayment_plans\".\"created_by\""},
	StartDate:         whereHelpertime_Time{field: "\"repayment_plans\".\"start_date\""},
	EndDate:           whereHelpertime_Time{field: "\"repayment_plans\".\"end_date\""},
	Frequency:         whereHelperstring{field: "\"repayment_plans\".\"frequency\""},
	InstallmentAmount: whereHelperstring{field: "\"repayment_plans\".\"installment_amount\""},
	Status:            whereHelperstring{field: "\"repayment_plans\".\"status\""},
	TotalDue:          whereHelperstring{field: "\"repayment_plans\".\"total_due\""},
	TotalPaid:         whereHelperstring{field: "\"repayment_plans\".\"total_paid\""},
	ApprovalRequired:  whereHelperbool{field: "\"repayment_plans\".\"approval_required\""},
	ApprovedBy:        whereHelpernull_Int{field: "\"repayment_plans\".\"approved_by\""},
	ApprovedAt:        whereHelpernull_Time{field: "\"repayment_plans\".\"approved_at\""},
	CreatedAt:         whereHelpertime_Time{field: "\"repayment_plans\".\"created_at\""},
	UpdatedAt:         whereHelpertime_Time{field: "\"repayment_plans\".\"updated_at\""},
}

// RepaymentPlanRels is where relationship names are stored.
var RepaymentPlanRels = struct {
	PlanRepaymentPlanAudits       string
	PlanRepaymentPlanInstallments string
}{
	PlanRepaymentPlanAudits:       "PlanRepaymentPlanAudits",
	PlanRepaymentPlanInstallments: "PlanRepaymentPlanInstallments",
}

// repaymentPlanR is where relationships are stored.
type repaymentPlanR struct {
	PlanRepaymentPlanAudits       RepaymentPlanAuditSlice       `boil:"PlanRepaymentPlanAudits" json:"PlanRepaymentPlanAudits" toml:"PlanRepaymentPlanAudits" yaml:"PlanRepaymentPlanAudits"`
	PlanRepaymentPlanInstallments RepaymentPlanInstallmentSlice `boil:"PlanRepaymentPlanInstallments" json:"PlanRepaymentPlanInstallments" toml:"PlanRepaymentPlanInstallments" yaml:"PlanRepaymentPlanInstallments"`
}

// NewStruct creates a new relationship struct
func (*repaymentPlanR) NewStruct() *repaymentPlanR {
	return &repaymentPlanR{}
}

func (o *RepaymentPlan) GetPlanRepaymentPlanAudits() RepaymentPlanAuditSlice {
	if o == nil {
		return nil
	}

	return o.R.GetPlanRepaymentPlanAudits()
}

func (r *repaymentPlanR) GetPlanRepaymentPlanAudits() RepaymentPlanAuditSlice {
	if r == nil {
		return nil
	}

	return r.PlanRepaymentPlanAudits
}

func (o *RepaymentPlan) GetPlanRepaymentPlanInstallments() RepaymentPlanInstallmentSlice {
	if o == nil {
		return nil
	}

	return o.R.GetPlanRepaymentPlanInstallments()
}

func (r *repaymentPlanR) GetPlanRepaymentPlanInstallments() RepaymentPlanInstallmentSlice {
	if r == nil {
		return nil
	}

	return r.PlanRepaymentPlanInstallments
}

// repaymentPlanL is where Load methods for each relationship are stored.
type repaymentPlanL struct{}

var (
	repaymentPlanAllColumns            = []string{"id", "account_id", "created_by", "start_date", "end_date", "frequency", "installment_amount", "status", "total_due", "total_paid", "approval_required", "approved_by", "approved_at", "created_at", "updated_at"}
	repaymentPlanColumnsWithoutDefault = []string{"id", "account_id", "created_by", "start_date", "end_date", "frequency", "installment_amount", "status"}
	repaymentPlanColumnsWithDefault    = []string{"total_due", "total_paid", "approval_required", "approved_by", "approved_at", "created_at", "updated_at"}
	repaymentPlanPrimaryKeyColumns     = []string{"id"}
	repaymentPlanGeneratedColumns      = []string{}
)

type (
	// RepaymentPlanSlice is an alias for a slice of pointers to RepaymentPlan.
	// This should almost always be used instead of []RepaymentPlan.
	RepaymentPlanSlice []*RepaymentPlan
	// RepaymentPlanHook is the signature for custom RepaymentPlan hook methods
	RepaymentPlanHook func(context.Context, boil.ContextExecutor, *RepaymentPlan) error

	repaymentPlanQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	repaymentPlanType                 = reflect.TypeOf(&RepaymentPlan{})
	repaymentPlanMapping              = queries.MakeStructMapping(repaymentPlanType)
	repaymentPlanPrimaryKeyMapping, _ = queries.BindMapping(repaymentPlanType, repaymentPlanMapping, repaymentPlanPrimaryKeyColumns)
	repaymentPlanInsertCacheMut       sync.RWMutex
	repaymentPlanInsertCache          = make(map[string]insertCache)
	repaymentPlanUpdateCacheMut       sync.RWMutex
	repaymentPlanUpdateCache          = make(map[string]updateCache)
	repaymentPlanUpsertCacheMut       sync.RWMutex
	repaymentPlanUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var repaymentPlanAfterSelectMu sync.Mutex
var repaymentPlanAfterSelectHooks []RepaymentPlanHook

var repaymentPlanBeforeInsertMu sync.Mutex
var repaymentPlanBeforeInsertHooks []RepaymentPlanHook
var repaymentPlanAfterInsertMu sync.Mutex
var repaymentPlanAfterInsertHooks []RepaymentPlanHook

var repaymentPlanBeforeUpdateMu sync.Mutex
var repaymentPlanBeforeUpdateHooks []RepaymentPlanHook
var repaymentPlanAfterUpdateMu sync.Mutex
var repaymentPlanAfterUpdateHooks []RepaymentPlanHook

var repaymentPlanBeforeDeleteMu sync.Mutex
var repaymentPlanBeforeDeleteHooks []RepaymentPlanHook
var repaymentPlanAfterDeleteMu sync.Mutex
var repaymentPlanAfterDeleteHooks []RepaymentPlanHook

var repaymentPlanBeforeUpsertMu sync.Mutex
var repaymentPlanBeforeUpsertHooks []RepaymentPlanHook
var repaymentPlanAfterUpsertMu sync.Mutex
var repaymentPlanAfterUpsertHooks []RepaymentPlanHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RepaymentPlan) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RepaymentPlan) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RepaymentPlan) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RepaymentPlan) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RepaymentPlan) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RepaymentPlan) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RepaymentPlan) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RepaymentPlan) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RepaymentPlan) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRepaymentPlanHook registers your hook function for all future operations.
func AddRepaymentPlanHook(hookPoint boil.HookPoint, repaymentPlanHook RepaymentPlanHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		repaymentPlanAfterSelectMu.Lock()
		repaymentPlanAfterSelectHooks = append(repaymentPlanAfterSelectHooks, repaymentPlanHook)
		repaymentPlanAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		repaymentPlanBeforeInsertMu.Lock()
		repaymentPlanBeforeInsertHooks = append(repaymentPlanBeforeInsertHooks, repaymentPlanHook)
		repaymentPlanBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		repaymentPlanAfterInsertMu.Lock()
		repaymentPlanAfterInsertHooks = append(repaymentPlanAfterInsertHooks, repaymentPlanHook)
		repaymentPlanAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		repaymentPlanBeforeUpdateMu.Lock()
		repaymentPlanBeforeUpdateHooks = append(repaymentPlanBeforeUpdateHooks, repaymentPlanHook)
		repaymentPlanBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		repaymentPlanAfterUpdateMu.Lock()
		repaymentPlanAfterUpdateHooks = append(repaymentPlanAfterUpdateHooks, repaymentPlanHook)
		repaymentPlanAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		repaymentPlanBeforeDeleteMu.Lock()
		repaymentPlanBeforeDeleteHooks = append(repaymentPlanBeforeDeleteHooks, repaymentPlanHook)
		repaymentPlanBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		repaymentPlanAfterDeleteMu.Lock()
		repaymentPlanAfterDeleteHooks = append(repaymentPlanAfterDeleteHooks, repaymentPlanHook)
		repaymentPlanAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		repaymentPlanBeforeUpsertMu.Lock()
		repaymentPlanBeforeUpsertHooks = append(repaymentPlanBeforeUpsertHooks, repaymentPlanHook)
		repaymentPlanBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		repaymentPlanAfterUpsertMu.Lock()
		repaymentPlanAfterUpsertHooks = append(repaymentPlanAfterUpsertHooks, repaymentPlanHook)
		repaymentPlanAfterUpsertMu.Unlock()
	}
}

// One returns a single repaymentPlan record from the query.
func (q repaymentPlanQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RepaymentPlan, error) {
	o := &RepaymentPlan{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for repayment_plans")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RepaymentPlan records from the query.
func (q repaymentPlanQuery) All(ctx context.Context, exec boil.ContextExecutor) (RepaymentPlanSlice, error) {
	var o []*RepaymentPlan

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to RepaymentPlan slice")
	}

	if len(repaymentPlanAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RepaymentPlan records in the query.
func (q repaymentPlanQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count repayment_plans rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q repaymentPlanQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if repayment_plans exists")
	}

	return count > 0, nil
}

// PlanRepaymentPlanAudits retrieves all the repayment_plan_audit's RepaymentPlanAudits with an executor via plan_id column.
func (o *RepaymentPlan) PlanRepaymentPlanAudits(mods ...qm.QueryMod) repaymentPlanAuditQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"repayment_plan_audit\".\"plan_id\"=?", o.ID),
	)

	return RepaymentPlanAudits(queryMods...)
}

// PlanRepaymentPlanInstallments retrieves all the repayment_plan_installment's RepaymentPlanInstallments with an executor via plan_id column.
func (o *RepaymentPlan) PlanRepaymentPlanInstallments(mods ...qm.QueryMod) repaymentPlanInstallmentQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"repayment_plan_installments\".\"plan_id\"=?", o.ID),
	)

	return RepaymentPlanInstallments(queryMods...)
}

// LoadPlanRepaymentPlanAudits allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (repaymentPlanL) LoadPlanRepaymentPlanAudits(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRepaymentPlan interface{}, mods queries.Applicator) error {
	var slice []*RepaymentPlan
	var object *RepaymentPlan

	if singular {
		var ok bool
		object, ok = maybeRepaymentPlan.(*RepaymentPlan)
		if !ok {
			object = new(RepaymentPlan)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRepaymentPlan)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRepaymentPlan))
			}
		}
	} else {
		s, ok := maybeRepaymentPlan.(*[]*RepaymentPlan)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRepaymentPlan)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRepaymentPlan))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &repaymentPlanR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &repaymentPlanR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`repayment_plan_audit`),
		qm.WhereIn(`repayment_plan_audit.plan_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load repayment_plan_audit")
	}

	var resultSlice []*RepaymentPlanAudit
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice repayment_plan_audit")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on repayment_plan_audit")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for repayment_plan_audit")
	}

	if len(repaymentPlanAuditAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PlanRepaymentPlanAudits = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &repaymentPlanAuditR{}
			}
			foreign.R.Plan = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.PlanID {
				local.R.PlanRepaymentPlanAudits = append(local.R.PlanRepaymentPlanAudits, foreign)
				if foreign.R == nil {
					foreign.R = &repaymentPlanAuditR{}
				}
				foreign.R.Plan = local
				break
			}
		}
	}

	return nil
}

// LoadPlanRepaymentPlanInstallments allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (repaymentPlanL) LoadPlanRepaymentPlanInstallments(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRepaymentPlan interface{}, mods queries.Applicator) error {
	var slice []*RepaymentPlan
	var object *RepaymentPlan

	if singular {
		var ok bool
		object, ok = maybeRepaymentPlan.(*RepaymentPlan)
		if !ok {
			object = new(RepaymentPlan)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRepaymentPlan)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRepaymentPlan))
			}
		}
	} else {
		s, ok := maybeRepaymentPlan.(*[]*RepaymentPlan)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRepaymentPlan)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRepaymentPlan))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &repaymentPlanR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &repaymentPlanR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`repayment_plan_installments`),
		qm.WhereIn(`repayment_plan_installments.plan_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load repayment_plan_installments")
	}

	var resultSlice []*RepaymentPlanInstallment
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice repayment_plan_installments")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on repayment_plan_installments")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for repayment_plan_installments")
	}

	if len(repaymentPlanInstallmentAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PlanRepaymentPlanInstallments = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &repaymentPlanInstallmentR{}
			}
			foreign.R.Plan = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.PlanID {
				local.R.PlanRepaymentPlanInstallments = append(local.R.PlanRepaymentPlanInstallments, foreign)
				if foreign.R == nil {
					foreign.R = &repaymentPlanInstallmentR{}
				}
				foreign.R.Plan = local
				break
			}
		}
	}

	return nil
}

// AddPlanRepaymentPlanAudits adds the given related objects to the existing relationships
// of the repayment_plan, optionally inserting them as new records.
// Appends related to o.R.PlanRepaymentPlanAudits.
// Sets related.R.Plan appropriately.
func (o *RepaymentPlan) AddPlanRepaymentPlanAudits(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*RepaymentPlanAudit) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PlanID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"repayment_plan_audit\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"plan_id"}),
				strmangle.WhereClause("\"", "\"", 2, repaymentPlanAuditPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PlanID = o.ID
		}
	}

	if o.R == nil {
		o.R = &repaymentPlanR{
			PlanRepaymentPlanAudits: related,
		}
	} else {
		o.R.PlanRepaymentPlanAudits = append(o.R.PlanRepaymentPlanAudits, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &repaymentPlanAuditR{
				Plan: o,
			}
		} else {
			rel.R.Plan = o
		}
	}
	return nil
}

// AddPlanRepaymentPlanInstallments adds the given related objects to the existing relationships
// of the repayment_plan, optionally inserting them as new records.
// Appends related to o.R.PlanRepaymentPlanInstallments.
// Sets related.R.Plan appropriately.
func (o *RepaymentPlan) AddPlanRepaymentPlanInstallments(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*RepaymentPlanInstallment) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PlanID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"repayment_plan_installments\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"plan_id"}),
				strmangle.WhereClause("\"", "\"", 2, repaymentPlanInstallmentPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PlanID = o.ID
		}
	}

	if o.R == nil {
		o.R = &repaymentPlanR{
			PlanRepaymentPlanInstallments: related,
		}
	} else {
		o.R.PlanRepaymentPlanInstallments = append(o.R.PlanRepaymentPlanInstallments, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &repaymentPlanInstallmentR{
				Plan: o,
			}
		} else {
			rel.R.Plan = o
		}
	}
	return nil
}

// RepaymentPlans retrieves all the records using an executor.
func RepaymentPlans(mods ...qm.QueryMod) repaymentPlanQuery {
	mods = append(mods, qm.From("\"repayment_plans\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"repayment_plans\".*"})
	}

	return repaymentPlanQuery{q}
}

// FindRepaymentPlan retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRepaymentPlan(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*RepaymentPlan, error) {
	repaymentPlanObj := &RepaymentPlan{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"repayment_plans\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, repaymentPlanObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from repayment_plans")
	}

	if err = repaymentPlanObj.doAfterSelectHooks(ctx, exec); err != nil {
		return repaymentPlanObj, err
	}

	return repaymentPlanObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RepaymentPlan) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no repayment_plans provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(repaymentPlanColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	repaymentPlanInsertCacheMut.RLock()
	cache, cached := repaymentPlanInsertCache[key]
	repaymentPlanInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			repaymentPlanAllColumns,
			repaymentPlanColumnsWithDefault,
			repaymentPlanColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(repaymentPlanType, repaymentPlanMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(repaymentPlanType, repaymentPlanMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"repayment_plans\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"repayment_plans\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into repayment_plans")
	}

	if !cached {
		repaymentPlanInsertCacheMut.Lock()
		repaymentPlanInsertCache[key] = cache
		repaymentPlanInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RepaymentPlan.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RepaymentPlan) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	repaymentPlanUpdateCacheMut.RLock()
	cache, cached := repaymentPlanUpdateCache[key]
	repaymentPlanUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			repaymentPlanAllColumns,
			repaymentPlanPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update repayment_plans, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"repayment_plans\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, repaymentPlanPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(repaymentPlanType, repaymentPlanMapping, append(wl, repaymentPlanPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update repayment_plans row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for repayment_plans")
	}

	if !cached {
		repaymentPlanUpdateCacheMut.Lock()
		repaymentPlanUpdateCache[key] = cache
		repaymentPlanUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q repaymentPlanQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for repayment_plans")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for repayment_plans")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RepaymentPlanSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), repaymentPlanPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"repayment_plans\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, repaymentPlanPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in repaymentPlan slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all repaymentPlan")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RepaymentPlan) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no repayment_plans provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(repaymentPlanColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	repaymentPlanUpsertCacheMut.RLock()
	cache, cached := repaymentPlanUpsertCache[key]
	repaymentPlanUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			repaymentPlanAllColumns,
			repaymentPlanColumnsWithDefault,
			repaymentPlanColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			repaymentPlanAllColumns,
			repaymentPlanPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert repayment_plans, could not build update column list")
		}

		ret := strmangle.SetComplement(repaymentPlanAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(repaymentPlanPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert repayment_plans, could not build conflict column list")
			}

			conflict = make([]string, len(repaymentPlanPrimaryKeyColumns))
			copy(conflict, repaymentPlanPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"repayment_plans\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(repaymentPlanType, repaymentPlanMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(repaymentPlanType, repaymentPlanMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert repayment_plans")
	}

	if !cached {
		repaymentPlanUpsertCacheMut.Lock()
		repaymentPlanUpsertCache[key] = cache
		repaymentPlanUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RepaymentPlan record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RepaymentPlan) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no RepaymentPlan provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), repaymentPlanPrimaryKeyMapping)
	sql := "DELETE FROM \"repayment_plans\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from repayment_plans")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for repayment_plans")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q repaymentPlanQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no repaymentPlanQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from repayment_plans")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for repayment_plans")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RepaymentPlanSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(repaymentPlanBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), repaymentPlanPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"repayment_plans\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, repaymentPlanPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from repaymentPlan slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for repayment_plans")
	}

	if len(repaymentPlanAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RepaymentPlan) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRepaymentPlan(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RepaymentPlanSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RepaymentPlanSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), repaymentPlanPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"repayment_plans\".* FROM \"repayment_plans\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, repaymentPlanPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in RepaymentPlanSlice")
	}

	*o = slice

	return nil
}

// RepaymentPlanExists checks if the RepaymentPlan row exists.
func RepaymentPlanExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"repayment_plans\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if repayment_plans exists")
	}

	return exists, nil
}

// Exists checks if the RepaymentPlan row exists.
func (o *RepaymentPlan) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return RepaymentPlanExists(ctx, exec, o.ID)
}
