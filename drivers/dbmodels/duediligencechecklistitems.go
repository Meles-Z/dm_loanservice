// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// Duediligencechecklistitem is an object representing the database table.
type Duediligencechecklistitem struct {
	ID            string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	AccountID     string      `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	ChecklistItem string      `boil:"checklist_item" json:"checklist_item" toml:"checklist_item" yaml:"checklist_item"`
	Status        string      `boil:"status" json:"status" toml:"status" yaml:"status"`
	Comments      null.String `boil:"comments" json:"comments,omitempty" toml:"comments" yaml:"comments,omitempty"`
	CreatedBy     null.Int    `boil:"created_by" json:"created_by,omitempty" toml:"created_by" yaml:"created_by,omitempty"`
	CreatedAt     null.Time   `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt     null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *duediligencechecklistitemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L duediligencechecklistitemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DuediligencechecklistitemColumns = struct {
	ID            string
	AccountID     string
	ChecklistItem string
	Status        string
	Comments      string
	CreatedBy     string
	CreatedAt     string
	UpdatedAt     string
}{
	ID:            "id",
	AccountID:     "account_id",
	ChecklistItem: "checklist_item",
	Status:        "status",
	Comments:      "comments",
	CreatedBy:     "created_by",
	CreatedAt:     "created_at",
	UpdatedAt:     "updated_at",
}

var DuediligencechecklistitemTableColumns = struct {
	ID            string
	AccountID     string
	ChecklistItem string
	Status        string
	Comments      string
	CreatedBy     string
	CreatedAt     string
	UpdatedAt     string
}{
	ID:            "duediligencechecklistitems.id",
	AccountID:     "duediligencechecklistitems.account_id",
	ChecklistItem: "duediligencechecklistitems.checklist_item",
	Status:        "duediligencechecklistitems.status",
	Comments:      "duediligencechecklistitems.comments",
	CreatedBy:     "duediligencechecklistitems.created_by",
	CreatedAt:     "duediligencechecklistitems.created_at",
	UpdatedAt:     "duediligencechecklistitems.updated_at",
}

// Generated where

var DuediligencechecklistitemWhere = struct {
	ID            whereHelperstring
	AccountID     whereHelperstring
	ChecklistItem whereHelperstring
	Status        whereHelperstring
	Comments      whereHelpernull_String
	CreatedBy     whereHelpernull_Int
	CreatedAt     whereHelpernull_Time
	UpdatedAt     whereHelpernull_Time
}{
	ID:            whereHelperstring{field: "\"duediligencechecklistitems\".\"id\""},
	AccountID:     whereHelperstring{field: "\"duediligencechecklistitems\".\"account_id\""},
	ChecklistItem: whereHelperstring{field: "\"duediligencechecklistitems\".\"checklist_item\""},
	Status:        whereHelperstring{field: "\"duediligencechecklistitems\".\"status\""},
	Comments:      whereHelpernull_String{field: "\"duediligencechecklistitems\".\"comments\""},
	CreatedBy:     whereHelpernull_Int{field: "\"duediligencechecklistitems\".\"created_by\""},
	CreatedAt:     whereHelpernull_Time{field: "\"duediligencechecklistitems\".\"created_at\""},
	UpdatedAt:     whereHelpernull_Time{field: "\"duediligencechecklistitems\".\"updated_at\""},
}

// DuediligencechecklistitemRels is where relationship names are stored.
var DuediligencechecklistitemRels = struct {
	Account string
}{
	Account: "Account",
}

// duediligencechecklistitemR is where relationships are stored.
type duediligencechecklistitemR struct {
	Account *Account `boil:"Account" json:"Account" toml:"Account" yaml:"Account"`
}

// NewStruct creates a new relationship struct
func (*duediligencechecklistitemR) NewStruct() *duediligencechecklistitemR {
	return &duediligencechecklistitemR{}
}

func (o *Duediligencechecklistitem) GetAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetAccount()
}

func (r *duediligencechecklistitemR) GetAccount() *Account {
	if r == nil {
		return nil
	}

	return r.Account
}

// duediligencechecklistitemL is where Load methods for each relationship are stored.
type duediligencechecklistitemL struct{}

var (
	duediligencechecklistitemAllColumns            = []string{"id", "account_id", "checklist_item", "status", "comments", "created_by", "created_at", "updated_at"}
	duediligencechecklistitemColumnsWithoutDefault = []string{"id", "account_id", "checklist_item", "status"}
	duediligencechecklistitemColumnsWithDefault    = []string{"comments", "created_by", "created_at", "updated_at"}
	duediligencechecklistitemPrimaryKeyColumns     = []string{"id"}
	duediligencechecklistitemGeneratedColumns      = []string{}
)

type (
	// DuediligencechecklistitemSlice is an alias for a slice of pointers to Duediligencechecklistitem.
	// This should almost always be used instead of []Duediligencechecklistitem.
	DuediligencechecklistitemSlice []*Duediligencechecklistitem
	// DuediligencechecklistitemHook is the signature for custom Duediligencechecklistitem hook methods
	DuediligencechecklistitemHook func(context.Context, boil.ContextExecutor, *Duediligencechecklistitem) error

	duediligencechecklistitemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	duediligencechecklistitemType                 = reflect.TypeOf(&Duediligencechecklistitem{})
	duediligencechecklistitemMapping              = queries.MakeStructMapping(duediligencechecklistitemType)
	duediligencechecklistitemPrimaryKeyMapping, _ = queries.BindMapping(duediligencechecklistitemType, duediligencechecklistitemMapping, duediligencechecklistitemPrimaryKeyColumns)
	duediligencechecklistitemInsertCacheMut       sync.RWMutex
	duediligencechecklistitemInsertCache          = make(map[string]insertCache)
	duediligencechecklistitemUpdateCacheMut       sync.RWMutex
	duediligencechecklistitemUpdateCache          = make(map[string]updateCache)
	duediligencechecklistitemUpsertCacheMut       sync.RWMutex
	duediligencechecklistitemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var duediligencechecklistitemAfterSelectMu sync.Mutex
var duediligencechecklistitemAfterSelectHooks []DuediligencechecklistitemHook

var duediligencechecklistitemBeforeInsertMu sync.Mutex
var duediligencechecklistitemBeforeInsertHooks []DuediligencechecklistitemHook
var duediligencechecklistitemAfterInsertMu sync.Mutex
var duediligencechecklistitemAfterInsertHooks []DuediligencechecklistitemHook

var duediligencechecklistitemBeforeUpdateMu sync.Mutex
var duediligencechecklistitemBeforeUpdateHooks []DuediligencechecklistitemHook
var duediligencechecklistitemAfterUpdateMu sync.Mutex
var duediligencechecklistitemAfterUpdateHooks []DuediligencechecklistitemHook

var duediligencechecklistitemBeforeDeleteMu sync.Mutex
var duediligencechecklistitemBeforeDeleteHooks []DuediligencechecklistitemHook
var duediligencechecklistitemAfterDeleteMu sync.Mutex
var duediligencechecklistitemAfterDeleteHooks []DuediligencechecklistitemHook

var duediligencechecklistitemBeforeUpsertMu sync.Mutex
var duediligencechecklistitemBeforeUpsertHooks []DuediligencechecklistitemHook
var duediligencechecklistitemAfterUpsertMu sync.Mutex
var duediligencechecklistitemAfterUpsertHooks []DuediligencechecklistitemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Duediligencechecklistitem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range duediligencechecklistitemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Duediligencechecklistitem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range duediligencechecklistitemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Duediligencechecklistitem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range duediligencechecklistitemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Duediligencechecklistitem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range duediligencechecklistitemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Duediligencechecklistitem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range duediligencechecklistitemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Duediligencechecklistitem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range duediligencechecklistitemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Duediligencechecklistitem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range duediligencechecklistitemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Duediligencechecklistitem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range duediligencechecklistitemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Duediligencechecklistitem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range duediligencechecklistitemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDuediligencechecklistitemHook registers your hook function for all future operations.
func AddDuediligencechecklistitemHook(hookPoint boil.HookPoint, duediligencechecklistitemHook DuediligencechecklistitemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		duediligencechecklistitemAfterSelectMu.Lock()
		duediligencechecklistitemAfterSelectHooks = append(duediligencechecklistitemAfterSelectHooks, duediligencechecklistitemHook)
		duediligencechecklistitemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		duediligencechecklistitemBeforeInsertMu.Lock()
		duediligencechecklistitemBeforeInsertHooks = append(duediligencechecklistitemBeforeInsertHooks, duediligencechecklistitemHook)
		duediligencechecklistitemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		duediligencechecklistitemAfterInsertMu.Lock()
		duediligencechecklistitemAfterInsertHooks = append(duediligencechecklistitemAfterInsertHooks, duediligencechecklistitemHook)
		duediligencechecklistitemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		duediligencechecklistitemBeforeUpdateMu.Lock()
		duediligencechecklistitemBeforeUpdateHooks = append(duediligencechecklistitemBeforeUpdateHooks, duediligencechecklistitemHook)
		duediligencechecklistitemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		duediligencechecklistitemAfterUpdateMu.Lock()
		duediligencechecklistitemAfterUpdateHooks = append(duediligencechecklistitemAfterUpdateHooks, duediligencechecklistitemHook)
		duediligencechecklistitemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		duediligencechecklistitemBeforeDeleteMu.Lock()
		duediligencechecklistitemBeforeDeleteHooks = append(duediligencechecklistitemBeforeDeleteHooks, duediligencechecklistitemHook)
		duediligencechecklistitemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		duediligencechecklistitemAfterDeleteMu.Lock()
		duediligencechecklistitemAfterDeleteHooks = append(duediligencechecklistitemAfterDeleteHooks, duediligencechecklistitemHook)
		duediligencechecklistitemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		duediligencechecklistitemBeforeUpsertMu.Lock()
		duediligencechecklistitemBeforeUpsertHooks = append(duediligencechecklistitemBeforeUpsertHooks, duediligencechecklistitemHook)
		duediligencechecklistitemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		duediligencechecklistitemAfterUpsertMu.Lock()
		duediligencechecklistitemAfterUpsertHooks = append(duediligencechecklistitemAfterUpsertHooks, duediligencechecklistitemHook)
		duediligencechecklistitemAfterUpsertMu.Unlock()
	}
}

// One returns a single duediligencechecklistitem record from the query.
func (q duediligencechecklistitemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Duediligencechecklistitem, error) {
	o := &Duediligencechecklistitem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for duediligencechecklistitems")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Duediligencechecklistitem records from the query.
func (q duediligencechecklistitemQuery) All(ctx context.Context, exec boil.ContextExecutor) (DuediligencechecklistitemSlice, error) {
	var o []*Duediligencechecklistitem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to Duediligencechecklistitem slice")
	}

	if len(duediligencechecklistitemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Duediligencechecklistitem records in the query.
func (q duediligencechecklistitemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count duediligencechecklistitems rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q duediligencechecklistitemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if duediligencechecklistitems exists")
	}

	return count > 0, nil
}

// Account pointed to by the foreign key.
func (o *Duediligencechecklistitem) Account(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// LoadAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (duediligencechecklistitemL) LoadAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDuediligencechecklistitem interface{}, mods queries.Applicator) error {
	var slice []*Duediligencechecklistitem
	var object *Duediligencechecklistitem

	if singular {
		var ok bool
		object, ok = maybeDuediligencechecklistitem.(*Duediligencechecklistitem)
		if !ok {
			object = new(Duediligencechecklistitem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDuediligencechecklistitem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDuediligencechecklistitem))
			}
		}
	} else {
		s, ok := maybeDuediligencechecklistitem.(*[]*Duediligencechecklistitem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDuediligencechecklistitem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDuediligencechecklistitem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &duediligencechecklistitemR{}
		}
		args[object.AccountID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &duediligencechecklistitemR{}
			}

			args[obj.AccountID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Account = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.Duediligencechecklistitems = append(foreign.R.Duediligencechecklistitems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AccountID == foreign.ID {
				local.R.Account = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.Duediligencechecklistitems = append(foreign.R.Duediligencechecklistitems, local)
				break
			}
		}
	}

	return nil
}

// SetAccount of the duediligencechecklistitem to the related item.
// Sets o.R.Account to related.
// Adds o to related.R.Duediligencechecklistitems.
func (o *Duediligencechecklistitem) SetAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"duediligencechecklistitems\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
		strmangle.WhereClause("\"", "\"", 2, duediligencechecklistitemPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AccountID = related.ID
	if o.R == nil {
		o.R = &duediligencechecklistitemR{
			Account: related,
		}
	} else {
		o.R.Account = related
	}

	if related.R == nil {
		related.R = &accountR{
			Duediligencechecklistitems: DuediligencechecklistitemSlice{o},
		}
	} else {
		related.R.Duediligencechecklistitems = append(related.R.Duediligencechecklistitems, o)
	}

	return nil
}

// Duediligencechecklistitems retrieves all the records using an executor.
func Duediligencechecklistitems(mods ...qm.QueryMod) duediligencechecklistitemQuery {
	mods = append(mods, qm.From("\"duediligencechecklistitems\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"duediligencechecklistitems\".*"})
	}

	return duediligencechecklistitemQuery{q}
}

// FindDuediligencechecklistitem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDuediligencechecklistitem(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*Duediligencechecklistitem, error) {
	duediligencechecklistitemObj := &Duediligencechecklistitem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"duediligencechecklistitems\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, duediligencechecklistitemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from duediligencechecklistitems")
	}

	if err = duediligencechecklistitemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return duediligencechecklistitemObj, err
	}

	return duediligencechecklistitemObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Duediligencechecklistitem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no duediligencechecklistitems provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(duediligencechecklistitemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	duediligencechecklistitemInsertCacheMut.RLock()
	cache, cached := duediligencechecklistitemInsertCache[key]
	duediligencechecklistitemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			duediligencechecklistitemAllColumns,
			duediligencechecklistitemColumnsWithDefault,
			duediligencechecklistitemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(duediligencechecklistitemType, duediligencechecklistitemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(duediligencechecklistitemType, duediligencechecklistitemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"duediligencechecklistitems\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"duediligencechecklistitems\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into duediligencechecklistitems")
	}

	if !cached {
		duediligencechecklistitemInsertCacheMut.Lock()
		duediligencechecklistitemInsertCache[key] = cache
		duediligencechecklistitemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Duediligencechecklistitem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Duediligencechecklistitem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	duediligencechecklistitemUpdateCacheMut.RLock()
	cache, cached := duediligencechecklistitemUpdateCache[key]
	duediligencechecklistitemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			duediligencechecklistitemAllColumns,
			duediligencechecklistitemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update duediligencechecklistitems, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"duediligencechecklistitems\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, duediligencechecklistitemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(duediligencechecklistitemType, duediligencechecklistitemMapping, append(wl, duediligencechecklistitemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update duediligencechecklistitems row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for duediligencechecklistitems")
	}

	if !cached {
		duediligencechecklistitemUpdateCacheMut.Lock()
		duediligencechecklistitemUpdateCache[key] = cache
		duediligencechecklistitemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q duediligencechecklistitemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for duediligencechecklistitems")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for duediligencechecklistitems")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DuediligencechecklistitemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), duediligencechecklistitemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"duediligencechecklistitems\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, duediligencechecklistitemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in duediligencechecklistitem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all duediligencechecklistitem")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Duediligencechecklistitem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no duediligencechecklistitems provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(duediligencechecklistitemColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	duediligencechecklistitemUpsertCacheMut.RLock()
	cache, cached := duediligencechecklistitemUpsertCache[key]
	duediligencechecklistitemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			duediligencechecklistitemAllColumns,
			duediligencechecklistitemColumnsWithDefault,
			duediligencechecklistitemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			duediligencechecklistitemAllColumns,
			duediligencechecklistitemPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert duediligencechecklistitems, could not build update column list")
		}

		ret := strmangle.SetComplement(duediligencechecklistitemAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(duediligencechecklistitemPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert duediligencechecklistitems, could not build conflict column list")
			}

			conflict = make([]string, len(duediligencechecklistitemPrimaryKeyColumns))
			copy(conflict, duediligencechecklistitemPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"duediligencechecklistitems\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(duediligencechecklistitemType, duediligencechecklistitemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(duediligencechecklistitemType, duediligencechecklistitemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert duediligencechecklistitems")
	}

	if !cached {
		duediligencechecklistitemUpsertCacheMut.Lock()
		duediligencechecklistitemUpsertCache[key] = cache
		duediligencechecklistitemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Duediligencechecklistitem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Duediligencechecklistitem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no Duediligencechecklistitem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), duediligencechecklistitemPrimaryKeyMapping)
	sql := "DELETE FROM \"duediligencechecklistitems\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from duediligencechecklistitems")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for duediligencechecklistitems")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q duediligencechecklistitemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no duediligencechecklistitemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from duediligencechecklistitems")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for duediligencechecklistitems")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DuediligencechecklistitemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(duediligencechecklistitemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), duediligencechecklistitemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"duediligencechecklistitems\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, duediligencechecklistitemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from duediligencechecklistitem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for duediligencechecklistitems")
	}

	if len(duediligencechecklistitemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Duediligencechecklistitem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDuediligencechecklistitem(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DuediligencechecklistitemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DuediligencechecklistitemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), duediligencechecklistitemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"duediligencechecklistitems\".* FROM \"duediligencechecklistitems\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, duediligencechecklistitemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in DuediligencechecklistitemSlice")
	}

	*o = slice

	return nil
}

// DuediligencechecklistitemExists checks if the Duediligencechecklistitem row exists.
func DuediligencechecklistitemExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"duediligencechecklistitems\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if duediligencechecklistitems exists")
	}

	return exists, nil
}

// Exists checks if the Duediligencechecklistitem row exists.
func (o *Duediligencechecklistitem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DuediligencechecklistitemExists(ctx, exec, o.ID)
}
