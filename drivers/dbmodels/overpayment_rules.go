// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// OverpaymentRule is an object representing the database table.
type OverpaymentRule struct {
	ID               string        `boil:"id" json:"id" toml:"id" yaml:"id"`
	ProductID        string        `boil:"product_id" json:"product_id" toml:"product_id" yaml:"product_id"`
	AnnualLimitType  string        `boil:"annual_limit_type" json:"annual_limit_type" toml:"annual_limit_type" yaml:"annual_limit_type"`
	AnnualLimitValue types.Decimal `boil:"annual_limit_value" json:"annual_limit_value" toml:"annual_limit_value" yaml:"annual_limit_value"`
	PenaltyRate      types.Decimal `boil:"penalty_rate" json:"penalty_rate" toml:"penalty_rate" yaml:"penalty_rate"`
	AllowedTypes     string        `boil:"allowed_types" json:"allowed_types" toml:"allowed_types" yaml:"allowed_types"`
	EffectiveFrom    time.Time     `boil:"effective_from" json:"effective_from" toml:"effective_from" yaml:"effective_from"`
	EffectiveTo      null.Time     `boil:"effective_to" json:"effective_to,omitempty" toml:"effective_to" yaml:"effective_to,omitempty"`
	CreatedAt        time.Time     `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt        time.Time     `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *overpaymentRuleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L overpaymentRuleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var OverpaymentRuleColumns = struct {
	ID               string
	ProductID        string
	AnnualLimitType  string
	AnnualLimitValue string
	PenaltyRate      string
	AllowedTypes     string
	EffectiveFrom    string
	EffectiveTo      string
	CreatedAt        string
	UpdatedAt        string
}{
	ID:               "id",
	ProductID:        "product_id",
	AnnualLimitType:  "annual_limit_type",
	AnnualLimitValue: "annual_limit_value",
	PenaltyRate:      "penalty_rate",
	AllowedTypes:     "allowed_types",
	EffectiveFrom:    "effective_from",
	EffectiveTo:      "effective_to",
	CreatedAt:        "created_at",
	UpdatedAt:        "updated_at",
}

var OverpaymentRuleTableColumns = struct {
	ID               string
	ProductID        string
	AnnualLimitType  string
	AnnualLimitValue string
	PenaltyRate      string
	AllowedTypes     string
	EffectiveFrom    string
	EffectiveTo      string
	CreatedAt        string
	UpdatedAt        string
}{
	ID:               "overpayment_rules.id",
	ProductID:        "overpayment_rules.product_id",
	AnnualLimitType:  "overpayment_rules.annual_limit_type",
	AnnualLimitValue: "overpayment_rules.annual_limit_value",
	PenaltyRate:      "overpayment_rules.penalty_rate",
	AllowedTypes:     "overpayment_rules.allowed_types",
	EffectiveFrom:    "overpayment_rules.effective_from",
	EffectiveTo:      "overpayment_rules.effective_to",
	CreatedAt:        "overpayment_rules.created_at",
	UpdatedAt:        "overpayment_rules.updated_at",
}

// Generated where

var OverpaymentRuleWhere = struct {
	ID               whereHelperstring
	ProductID        whereHelperstring
	AnnualLimitType  whereHelperstring
	AnnualLimitValue whereHelpertypes_Decimal
	PenaltyRate      whereHelpertypes_Decimal
	AllowedTypes     whereHelperstring
	EffectiveFrom    whereHelpertime_Time
	EffectiveTo      whereHelpernull_Time
	CreatedAt        whereHelpertime_Time
	UpdatedAt        whereHelpertime_Time
}{
	ID:               whereHelperstring{field: "\"overpayment_rules\".\"id\""},
	ProductID:        whereHelperstring{field: "\"overpayment_rules\".\"product_id\""},
	AnnualLimitType:  whereHelperstring{field: "\"overpayment_rules\".\"annual_limit_type\""},
	AnnualLimitValue: whereHelpertypes_Decimal{field: "\"overpayment_rules\".\"annual_limit_value\""},
	PenaltyRate:      whereHelpertypes_Decimal{field: "\"overpayment_rules\".\"penalty_rate\""},
	AllowedTypes:     whereHelperstring{field: "\"overpayment_rules\".\"allowed_types\""},
	EffectiveFrom:    whereHelpertime_Time{field: "\"overpayment_rules\".\"effective_from\""},
	EffectiveTo:      whereHelpernull_Time{field: "\"overpayment_rules\".\"effective_to\""},
	CreatedAt:        whereHelpertime_Time{field: "\"overpayment_rules\".\"created_at\""},
	UpdatedAt:        whereHelpertime_Time{field: "\"overpayment_rules\".\"updated_at\""},
}

// OverpaymentRuleRels is where relationship names are stored.
var OverpaymentRuleRels = struct {
	Product string
}{
	Product: "Product",
}

// overpaymentRuleR is where relationships are stored.
type overpaymentRuleR struct {
	Product *Product `boil:"Product" json:"Product" toml:"Product" yaml:"Product"`
}

// NewStruct creates a new relationship struct
func (*overpaymentRuleR) NewStruct() *overpaymentRuleR {
	return &overpaymentRuleR{}
}

func (o *OverpaymentRule) GetProduct() *Product {
	if o == nil {
		return nil
	}

	return o.R.GetProduct()
}

func (r *overpaymentRuleR) GetProduct() *Product {
	if r == nil {
		return nil
	}

	return r.Product
}

// overpaymentRuleL is where Load methods for each relationship are stored.
type overpaymentRuleL struct{}

var (
	overpaymentRuleAllColumns            = []string{"id", "product_id", "annual_limit_type", "annual_limit_value", "penalty_rate", "allowed_types", "effective_from", "effective_to", "created_at", "updated_at"}
	overpaymentRuleColumnsWithoutDefault = []string{"id", "product_id", "annual_limit_type", "annual_limit_value", "penalty_rate", "allowed_types"}
	overpaymentRuleColumnsWithDefault    = []string{"effective_from", "effective_to", "created_at", "updated_at"}
	overpaymentRulePrimaryKeyColumns     = []string{"id"}
	overpaymentRuleGeneratedColumns      = []string{}
)

type (
	// OverpaymentRuleSlice is an alias for a slice of pointers to OverpaymentRule.
	// This should almost always be used instead of []OverpaymentRule.
	OverpaymentRuleSlice []*OverpaymentRule
	// OverpaymentRuleHook is the signature for custom OverpaymentRule hook methods
	OverpaymentRuleHook func(context.Context, boil.ContextExecutor, *OverpaymentRule) error

	overpaymentRuleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	overpaymentRuleType                 = reflect.TypeOf(&OverpaymentRule{})
	overpaymentRuleMapping              = queries.MakeStructMapping(overpaymentRuleType)
	overpaymentRulePrimaryKeyMapping, _ = queries.BindMapping(overpaymentRuleType, overpaymentRuleMapping, overpaymentRulePrimaryKeyColumns)
	overpaymentRuleInsertCacheMut       sync.RWMutex
	overpaymentRuleInsertCache          = make(map[string]insertCache)
	overpaymentRuleUpdateCacheMut       sync.RWMutex
	overpaymentRuleUpdateCache          = make(map[string]updateCache)
	overpaymentRuleUpsertCacheMut       sync.RWMutex
	overpaymentRuleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var overpaymentRuleAfterSelectMu sync.Mutex
var overpaymentRuleAfterSelectHooks []OverpaymentRuleHook

var overpaymentRuleBeforeInsertMu sync.Mutex
var overpaymentRuleBeforeInsertHooks []OverpaymentRuleHook
var overpaymentRuleAfterInsertMu sync.Mutex
var overpaymentRuleAfterInsertHooks []OverpaymentRuleHook

var overpaymentRuleBeforeUpdateMu sync.Mutex
var overpaymentRuleBeforeUpdateHooks []OverpaymentRuleHook
var overpaymentRuleAfterUpdateMu sync.Mutex
var overpaymentRuleAfterUpdateHooks []OverpaymentRuleHook

var overpaymentRuleBeforeDeleteMu sync.Mutex
var overpaymentRuleBeforeDeleteHooks []OverpaymentRuleHook
var overpaymentRuleAfterDeleteMu sync.Mutex
var overpaymentRuleAfterDeleteHooks []OverpaymentRuleHook

var overpaymentRuleBeforeUpsertMu sync.Mutex
var overpaymentRuleBeforeUpsertHooks []OverpaymentRuleHook
var overpaymentRuleAfterUpsertMu sync.Mutex
var overpaymentRuleAfterUpsertHooks []OverpaymentRuleHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *OverpaymentRule) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range overpaymentRuleAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *OverpaymentRule) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range overpaymentRuleBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *OverpaymentRule) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range overpaymentRuleAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *OverpaymentRule) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range overpaymentRuleBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *OverpaymentRule) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range overpaymentRuleAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *OverpaymentRule) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range overpaymentRuleBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *OverpaymentRule) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range overpaymentRuleAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *OverpaymentRule) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range overpaymentRuleBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *OverpaymentRule) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range overpaymentRuleAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddOverpaymentRuleHook registers your hook function for all future operations.
func AddOverpaymentRuleHook(hookPoint boil.HookPoint, overpaymentRuleHook OverpaymentRuleHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		overpaymentRuleAfterSelectMu.Lock()
		overpaymentRuleAfterSelectHooks = append(overpaymentRuleAfterSelectHooks, overpaymentRuleHook)
		overpaymentRuleAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		overpaymentRuleBeforeInsertMu.Lock()
		overpaymentRuleBeforeInsertHooks = append(overpaymentRuleBeforeInsertHooks, overpaymentRuleHook)
		overpaymentRuleBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		overpaymentRuleAfterInsertMu.Lock()
		overpaymentRuleAfterInsertHooks = append(overpaymentRuleAfterInsertHooks, overpaymentRuleHook)
		overpaymentRuleAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		overpaymentRuleBeforeUpdateMu.Lock()
		overpaymentRuleBeforeUpdateHooks = append(overpaymentRuleBeforeUpdateHooks, overpaymentRuleHook)
		overpaymentRuleBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		overpaymentRuleAfterUpdateMu.Lock()
		overpaymentRuleAfterUpdateHooks = append(overpaymentRuleAfterUpdateHooks, overpaymentRuleHook)
		overpaymentRuleAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		overpaymentRuleBeforeDeleteMu.Lock()
		overpaymentRuleBeforeDeleteHooks = append(overpaymentRuleBeforeDeleteHooks, overpaymentRuleHook)
		overpaymentRuleBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		overpaymentRuleAfterDeleteMu.Lock()
		overpaymentRuleAfterDeleteHooks = append(overpaymentRuleAfterDeleteHooks, overpaymentRuleHook)
		overpaymentRuleAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		overpaymentRuleBeforeUpsertMu.Lock()
		overpaymentRuleBeforeUpsertHooks = append(overpaymentRuleBeforeUpsertHooks, overpaymentRuleHook)
		overpaymentRuleBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		overpaymentRuleAfterUpsertMu.Lock()
		overpaymentRuleAfterUpsertHooks = append(overpaymentRuleAfterUpsertHooks, overpaymentRuleHook)
		overpaymentRuleAfterUpsertMu.Unlock()
	}
}

// One returns a single overpaymentRule record from the query.
func (q overpaymentRuleQuery) One(ctx context.Context, exec boil.ContextExecutor) (*OverpaymentRule, error) {
	o := &OverpaymentRule{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for overpayment_rules")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all OverpaymentRule records from the query.
func (q overpaymentRuleQuery) All(ctx context.Context, exec boil.ContextExecutor) (OverpaymentRuleSlice, error) {
	var o []*OverpaymentRule

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to OverpaymentRule slice")
	}

	if len(overpaymentRuleAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all OverpaymentRule records in the query.
func (q overpaymentRuleQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count overpayment_rules rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q overpaymentRuleQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if overpayment_rules exists")
	}

	return count > 0, nil
}

// Product pointed to by the foreign key.
func (o *OverpaymentRule) Product(mods ...qm.QueryMod) productQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ProductID),
	}

	queryMods = append(queryMods, mods...)

	return Products(queryMods...)
}

// LoadProduct allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (overpaymentRuleL) LoadProduct(ctx context.Context, e boil.ContextExecutor, singular bool, maybeOverpaymentRule interface{}, mods queries.Applicator) error {
	var slice []*OverpaymentRule
	var object *OverpaymentRule

	if singular {
		var ok bool
		object, ok = maybeOverpaymentRule.(*OverpaymentRule)
		if !ok {
			object = new(OverpaymentRule)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeOverpaymentRule)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeOverpaymentRule))
			}
		}
	} else {
		s, ok := maybeOverpaymentRule.(*[]*OverpaymentRule)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeOverpaymentRule)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeOverpaymentRule))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &overpaymentRuleR{}
		}
		args[object.ProductID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &overpaymentRuleR{}
			}

			args[obj.ProductID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`products`),
		qm.WhereIn(`products.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Product")
	}

	var resultSlice []*Product
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Product")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for products")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for products")
	}

	if len(productAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Product = foreign
		if foreign.R == nil {
			foreign.R = &productR{}
		}
		foreign.R.OverpaymentRules = append(foreign.R.OverpaymentRules, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProductID == foreign.ID {
				local.R.Product = foreign
				if foreign.R == nil {
					foreign.R = &productR{}
				}
				foreign.R.OverpaymentRules = append(foreign.R.OverpaymentRules, local)
				break
			}
		}
	}

	return nil
}

// SetProduct of the overpaymentRule to the related item.
// Sets o.R.Product to related.
// Adds o to related.R.OverpaymentRules.
func (o *OverpaymentRule) SetProduct(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Product) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"overpayment_rules\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"product_id"}),
		strmangle.WhereClause("\"", "\"", 2, overpaymentRulePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProductID = related.ID
	if o.R == nil {
		o.R = &overpaymentRuleR{
			Product: related,
		}
	} else {
		o.R.Product = related
	}

	if related.R == nil {
		related.R = &productR{
			OverpaymentRules: OverpaymentRuleSlice{o},
		}
	} else {
		related.R.OverpaymentRules = append(related.R.OverpaymentRules, o)
	}

	return nil
}

// OverpaymentRules retrieves all the records using an executor.
func OverpaymentRules(mods ...qm.QueryMod) overpaymentRuleQuery {
	mods = append(mods, qm.From("\"overpayment_rules\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"overpayment_rules\".*"})
	}

	return overpaymentRuleQuery{q}
}

// FindOverpaymentRule retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindOverpaymentRule(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*OverpaymentRule, error) {
	overpaymentRuleObj := &OverpaymentRule{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"overpayment_rules\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, overpaymentRuleObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from overpayment_rules")
	}

	if err = overpaymentRuleObj.doAfterSelectHooks(ctx, exec); err != nil {
		return overpaymentRuleObj, err
	}

	return overpaymentRuleObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *OverpaymentRule) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no overpayment_rules provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(overpaymentRuleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	overpaymentRuleInsertCacheMut.RLock()
	cache, cached := overpaymentRuleInsertCache[key]
	overpaymentRuleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			overpaymentRuleAllColumns,
			overpaymentRuleColumnsWithDefault,
			overpaymentRuleColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(overpaymentRuleType, overpaymentRuleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(overpaymentRuleType, overpaymentRuleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"overpayment_rules\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"overpayment_rules\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into overpayment_rules")
	}

	if !cached {
		overpaymentRuleInsertCacheMut.Lock()
		overpaymentRuleInsertCache[key] = cache
		overpaymentRuleInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the OverpaymentRule.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *OverpaymentRule) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	overpaymentRuleUpdateCacheMut.RLock()
	cache, cached := overpaymentRuleUpdateCache[key]
	overpaymentRuleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			overpaymentRuleAllColumns,
			overpaymentRulePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update overpayment_rules, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"overpayment_rules\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, overpaymentRulePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(overpaymentRuleType, overpaymentRuleMapping, append(wl, overpaymentRulePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update overpayment_rules row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for overpayment_rules")
	}

	if !cached {
		overpaymentRuleUpdateCacheMut.Lock()
		overpaymentRuleUpdateCache[key] = cache
		overpaymentRuleUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q overpaymentRuleQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for overpayment_rules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for overpayment_rules")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o OverpaymentRuleSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), overpaymentRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"overpayment_rules\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, overpaymentRulePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in overpaymentRule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all overpaymentRule")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *OverpaymentRule) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no overpayment_rules provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(overpaymentRuleColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	overpaymentRuleUpsertCacheMut.RLock()
	cache, cached := overpaymentRuleUpsertCache[key]
	overpaymentRuleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			overpaymentRuleAllColumns,
			overpaymentRuleColumnsWithDefault,
			overpaymentRuleColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			overpaymentRuleAllColumns,
			overpaymentRulePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert overpayment_rules, could not build update column list")
		}

		ret := strmangle.SetComplement(overpaymentRuleAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(overpaymentRulePrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert overpayment_rules, could not build conflict column list")
			}

			conflict = make([]string, len(overpaymentRulePrimaryKeyColumns))
			copy(conflict, overpaymentRulePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"overpayment_rules\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(overpaymentRuleType, overpaymentRuleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(overpaymentRuleType, overpaymentRuleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert overpayment_rules")
	}

	if !cached {
		overpaymentRuleUpsertCacheMut.Lock()
		overpaymentRuleUpsertCache[key] = cache
		overpaymentRuleUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single OverpaymentRule record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *OverpaymentRule) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no OverpaymentRule provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), overpaymentRulePrimaryKeyMapping)
	sql := "DELETE FROM \"overpayment_rules\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from overpayment_rules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for overpayment_rules")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q overpaymentRuleQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no overpaymentRuleQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from overpayment_rules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for overpayment_rules")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o OverpaymentRuleSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(overpaymentRuleBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), overpaymentRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"overpayment_rules\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, overpaymentRulePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from overpaymentRule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for overpayment_rules")
	}

	if len(overpaymentRuleAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *OverpaymentRule) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindOverpaymentRule(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *OverpaymentRuleSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := OverpaymentRuleSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), overpaymentRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"overpayment_rules\".* FROM \"overpayment_rules\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, overpaymentRulePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in OverpaymentRuleSlice")
	}

	*o = slice

	return nil
}

// OverpaymentRuleExists checks if the OverpaymentRule row exists.
func OverpaymentRuleExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"overpayment_rules\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if overpayment_rules exists")
	}

	return exists, nil
}

// Exists checks if the OverpaymentRule row exists.
func (o *OverpaymentRule) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return OverpaymentRuleExists(ctx, exec, o.ID)
}
