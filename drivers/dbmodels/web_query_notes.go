// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WebQueryNote is an object representing the database table.
type WebQueryNote struct {
	ID           int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt    time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt    time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	WebQueryID   int       `boil:"web_query_id" json:"web_query_id" toml:"web_query_id" yaml:"web_query_id"`
	OriginatorID int       `boil:"originator_id" json:"originator_id" toml:"originator_id" yaml:"originator_id"`
	Notes        string    `boil:"notes" json:"notes" toml:"notes" yaml:"notes"`

	R *webQueryNoteR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L webQueryNoteL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WebQueryNoteColumns = struct {
	ID           string
	CreatedAt    string
	UpdatedAt    string
	WebQueryID   string
	OriginatorID string
	Notes        string
}{
	ID:           "id",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
	WebQueryID:   "web_query_id",
	OriginatorID: "originator_id",
	Notes:        "notes",
}

var WebQueryNoteTableColumns = struct {
	ID           string
	CreatedAt    string
	UpdatedAt    string
	WebQueryID   string
	OriginatorID string
	Notes        string
}{
	ID:           "web_query_notes.id",
	CreatedAt:    "web_query_notes.created_at",
	UpdatedAt:    "web_query_notes.updated_at",
	WebQueryID:   "web_query_notes.web_query_id",
	OriginatorID: "web_query_notes.originator_id",
	Notes:        "web_query_notes.notes",
}

// Generated where

var WebQueryNoteWhere = struct {
	ID           whereHelperint
	CreatedAt    whereHelpertime_Time
	UpdatedAt    whereHelpertime_Time
	WebQueryID   whereHelperint
	OriginatorID whereHelperint
	Notes        whereHelperstring
}{
	ID:           whereHelperint{field: "\"web_query_notes\".\"id\""},
	CreatedAt:    whereHelpertime_Time{field: "\"web_query_notes\".\"created_at\""},
	UpdatedAt:    whereHelpertime_Time{field: "\"web_query_notes\".\"updated_at\""},
	WebQueryID:   whereHelperint{field: "\"web_query_notes\".\"web_query_id\""},
	OriginatorID: whereHelperint{field: "\"web_query_notes\".\"originator_id\""},
	Notes:        whereHelperstring{field: "\"web_query_notes\".\"notes\""},
}

// WebQueryNoteRels is where relationship names are stored.
var WebQueryNoteRels = struct {
	Originator string
	WebQuery   string
}{
	Originator: "Originator",
	WebQuery:   "WebQuery",
}

// webQueryNoteR is where relationships are stored.
type webQueryNoteR struct {
	Originator *User     `boil:"Originator" json:"Originator" toml:"Originator" yaml:"Originator"`
	WebQuery   *WebQuery `boil:"WebQuery" json:"WebQuery" toml:"WebQuery" yaml:"WebQuery"`
}

// NewStruct creates a new relationship struct
func (*webQueryNoteR) NewStruct() *webQueryNoteR {
	return &webQueryNoteR{}
}

func (o *WebQueryNote) GetOriginator() *User {
	if o == nil {
		return nil
	}

	return o.R.GetOriginator()
}

func (r *webQueryNoteR) GetOriginator() *User {
	if r == nil {
		return nil
	}

	return r.Originator
}

func (o *WebQueryNote) GetWebQuery() *WebQuery {
	if o == nil {
		return nil
	}

	return o.R.GetWebQuery()
}

func (r *webQueryNoteR) GetWebQuery() *WebQuery {
	if r == nil {
		return nil
	}

	return r.WebQuery
}

// webQueryNoteL is where Load methods for each relationship are stored.
type webQueryNoteL struct{}

var (
	webQueryNoteAllColumns            = []string{"id", "created_at", "updated_at", "web_query_id", "originator_id", "notes"}
	webQueryNoteColumnsWithoutDefault = []string{"updated_at", "web_query_id", "originator_id"}
	webQueryNoteColumnsWithDefault    = []string{"id", "created_at", "notes"}
	webQueryNotePrimaryKeyColumns     = []string{"id"}
	webQueryNoteGeneratedColumns      = []string{}
)

type (
	// WebQueryNoteSlice is an alias for a slice of pointers to WebQueryNote.
	// This should almost always be used instead of []WebQueryNote.
	WebQueryNoteSlice []*WebQueryNote
	// WebQueryNoteHook is the signature for custom WebQueryNote hook methods
	WebQueryNoteHook func(context.Context, boil.ContextExecutor, *WebQueryNote) error

	webQueryNoteQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	webQueryNoteType                 = reflect.TypeOf(&WebQueryNote{})
	webQueryNoteMapping              = queries.MakeStructMapping(webQueryNoteType)
	webQueryNotePrimaryKeyMapping, _ = queries.BindMapping(webQueryNoteType, webQueryNoteMapping, webQueryNotePrimaryKeyColumns)
	webQueryNoteInsertCacheMut       sync.RWMutex
	webQueryNoteInsertCache          = make(map[string]insertCache)
	webQueryNoteUpdateCacheMut       sync.RWMutex
	webQueryNoteUpdateCache          = make(map[string]updateCache)
	webQueryNoteUpsertCacheMut       sync.RWMutex
	webQueryNoteUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var webQueryNoteAfterSelectMu sync.Mutex
var webQueryNoteAfterSelectHooks []WebQueryNoteHook

var webQueryNoteBeforeInsertMu sync.Mutex
var webQueryNoteBeforeInsertHooks []WebQueryNoteHook
var webQueryNoteAfterInsertMu sync.Mutex
var webQueryNoteAfterInsertHooks []WebQueryNoteHook

var webQueryNoteBeforeUpdateMu sync.Mutex
var webQueryNoteBeforeUpdateHooks []WebQueryNoteHook
var webQueryNoteAfterUpdateMu sync.Mutex
var webQueryNoteAfterUpdateHooks []WebQueryNoteHook

var webQueryNoteBeforeDeleteMu sync.Mutex
var webQueryNoteBeforeDeleteHooks []WebQueryNoteHook
var webQueryNoteAfterDeleteMu sync.Mutex
var webQueryNoteAfterDeleteHooks []WebQueryNoteHook

var webQueryNoteBeforeUpsertMu sync.Mutex
var webQueryNoteBeforeUpsertHooks []WebQueryNoteHook
var webQueryNoteAfterUpsertMu sync.Mutex
var webQueryNoteAfterUpsertHooks []WebQueryNoteHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WebQueryNote) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range webQueryNoteAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WebQueryNote) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range webQueryNoteBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WebQueryNote) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range webQueryNoteAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WebQueryNote) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range webQueryNoteBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WebQueryNote) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range webQueryNoteAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WebQueryNote) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range webQueryNoteBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WebQueryNote) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range webQueryNoteAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WebQueryNote) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range webQueryNoteBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WebQueryNote) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range webQueryNoteAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWebQueryNoteHook registers your hook function for all future operations.
func AddWebQueryNoteHook(hookPoint boil.HookPoint, webQueryNoteHook WebQueryNoteHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		webQueryNoteAfterSelectMu.Lock()
		webQueryNoteAfterSelectHooks = append(webQueryNoteAfterSelectHooks, webQueryNoteHook)
		webQueryNoteAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		webQueryNoteBeforeInsertMu.Lock()
		webQueryNoteBeforeInsertHooks = append(webQueryNoteBeforeInsertHooks, webQueryNoteHook)
		webQueryNoteBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		webQueryNoteAfterInsertMu.Lock()
		webQueryNoteAfterInsertHooks = append(webQueryNoteAfterInsertHooks, webQueryNoteHook)
		webQueryNoteAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		webQueryNoteBeforeUpdateMu.Lock()
		webQueryNoteBeforeUpdateHooks = append(webQueryNoteBeforeUpdateHooks, webQueryNoteHook)
		webQueryNoteBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		webQueryNoteAfterUpdateMu.Lock()
		webQueryNoteAfterUpdateHooks = append(webQueryNoteAfterUpdateHooks, webQueryNoteHook)
		webQueryNoteAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		webQueryNoteBeforeDeleteMu.Lock()
		webQueryNoteBeforeDeleteHooks = append(webQueryNoteBeforeDeleteHooks, webQueryNoteHook)
		webQueryNoteBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		webQueryNoteAfterDeleteMu.Lock()
		webQueryNoteAfterDeleteHooks = append(webQueryNoteAfterDeleteHooks, webQueryNoteHook)
		webQueryNoteAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		webQueryNoteBeforeUpsertMu.Lock()
		webQueryNoteBeforeUpsertHooks = append(webQueryNoteBeforeUpsertHooks, webQueryNoteHook)
		webQueryNoteBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		webQueryNoteAfterUpsertMu.Lock()
		webQueryNoteAfterUpsertHooks = append(webQueryNoteAfterUpsertHooks, webQueryNoteHook)
		webQueryNoteAfterUpsertMu.Unlock()
	}
}

// One returns a single webQueryNote record from the query.
func (q webQueryNoteQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WebQueryNote, error) {
	o := &WebQueryNote{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for web_query_notes")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WebQueryNote records from the query.
func (q webQueryNoteQuery) All(ctx context.Context, exec boil.ContextExecutor) (WebQueryNoteSlice, error) {
	var o []*WebQueryNote

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to WebQueryNote slice")
	}

	if len(webQueryNoteAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WebQueryNote records in the query.
func (q webQueryNoteQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count web_query_notes rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q webQueryNoteQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if web_query_notes exists")
	}

	return count > 0, nil
}

// Originator pointed to by the foreign key.
func (o *WebQueryNote) Originator(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.OriginatorID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// WebQuery pointed to by the foreign key.
func (o *WebQueryNote) WebQuery(mods ...qm.QueryMod) webQueryQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WebQueryID),
	}

	queryMods = append(queryMods, mods...)

	return WebQueries(queryMods...)
}

// LoadOriginator allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (webQueryNoteL) LoadOriginator(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWebQueryNote interface{}, mods queries.Applicator) error {
	var slice []*WebQueryNote
	var object *WebQueryNote

	if singular {
		var ok bool
		object, ok = maybeWebQueryNote.(*WebQueryNote)
		if !ok {
			object = new(WebQueryNote)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWebQueryNote)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWebQueryNote))
			}
		}
	} else {
		s, ok := maybeWebQueryNote.(*[]*WebQueryNote)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWebQueryNote)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWebQueryNote))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &webQueryNoteR{}
		}
		args[object.OriginatorID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &webQueryNoteR{}
			}

			args[obj.OriginatorID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Originator = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.OriginatorWebQueryNotes = append(foreign.R.OriginatorWebQueryNotes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.OriginatorID == foreign.ID {
				local.R.Originator = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.OriginatorWebQueryNotes = append(foreign.R.OriginatorWebQueryNotes, local)
				break
			}
		}
	}

	return nil
}

// LoadWebQuery allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (webQueryNoteL) LoadWebQuery(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWebQueryNote interface{}, mods queries.Applicator) error {
	var slice []*WebQueryNote
	var object *WebQueryNote

	if singular {
		var ok bool
		object, ok = maybeWebQueryNote.(*WebQueryNote)
		if !ok {
			object = new(WebQueryNote)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWebQueryNote)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWebQueryNote))
			}
		}
	} else {
		s, ok := maybeWebQueryNote.(*[]*WebQueryNote)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWebQueryNote)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWebQueryNote))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &webQueryNoteR{}
		}
		args[object.WebQueryID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &webQueryNoteR{}
			}

			args[obj.WebQueryID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`web_query`),
		qm.WhereIn(`web_query.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WebQuery")
	}

	var resultSlice []*WebQuery
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WebQuery")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for web_query")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for web_query")
	}

	if len(webQueryAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.WebQuery = foreign
		if foreign.R == nil {
			foreign.R = &webQueryR{}
		}
		foreign.R.WebQueryNotes = append(foreign.R.WebQueryNotes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WebQueryID == foreign.ID {
				local.R.WebQuery = foreign
				if foreign.R == nil {
					foreign.R = &webQueryR{}
				}
				foreign.R.WebQueryNotes = append(foreign.R.WebQueryNotes, local)
				break
			}
		}
	}

	return nil
}

// SetOriginator of the webQueryNote to the related item.
// Sets o.R.Originator to related.
// Adds o to related.R.OriginatorWebQueryNotes.
func (o *WebQueryNote) SetOriginator(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"web_query_notes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"originator_id"}),
		strmangle.WhereClause("\"", "\"", 2, webQueryNotePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.OriginatorID = related.ID
	if o.R == nil {
		o.R = &webQueryNoteR{
			Originator: related,
		}
	} else {
		o.R.Originator = related
	}

	if related.R == nil {
		related.R = &userR{
			OriginatorWebQueryNotes: WebQueryNoteSlice{o},
		}
	} else {
		related.R.OriginatorWebQueryNotes = append(related.R.OriginatorWebQueryNotes, o)
	}

	return nil
}

// SetWebQuery of the webQueryNote to the related item.
// Sets o.R.WebQuery to related.
// Adds o to related.R.WebQueryNotes.
func (o *WebQueryNote) SetWebQuery(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WebQuery) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"web_query_notes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"web_query_id"}),
		strmangle.WhereClause("\"", "\"", 2, webQueryNotePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WebQueryID = related.ID
	if o.R == nil {
		o.R = &webQueryNoteR{
			WebQuery: related,
		}
	} else {
		o.R.WebQuery = related
	}

	if related.R == nil {
		related.R = &webQueryR{
			WebQueryNotes: WebQueryNoteSlice{o},
		}
	} else {
		related.R.WebQueryNotes = append(related.R.WebQueryNotes, o)
	}

	return nil
}

// WebQueryNotes retrieves all the records using an executor.
func WebQueryNotes(mods ...qm.QueryMod) webQueryNoteQuery {
	mods = append(mods, qm.From("\"web_query_notes\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"web_query_notes\".*"})
	}

	return webQueryNoteQuery{q}
}

// FindWebQueryNote retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWebQueryNote(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*WebQueryNote, error) {
	webQueryNoteObj := &WebQueryNote{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"web_query_notes\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, webQueryNoteObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from web_query_notes")
	}

	if err = webQueryNoteObj.doAfterSelectHooks(ctx, exec); err != nil {
		return webQueryNoteObj, err
	}

	return webQueryNoteObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WebQueryNote) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no web_query_notes provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(webQueryNoteColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	webQueryNoteInsertCacheMut.RLock()
	cache, cached := webQueryNoteInsertCache[key]
	webQueryNoteInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			webQueryNoteAllColumns,
			webQueryNoteColumnsWithDefault,
			webQueryNoteColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(webQueryNoteType, webQueryNoteMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(webQueryNoteType, webQueryNoteMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"web_query_notes\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"web_query_notes\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into web_query_notes")
	}

	if !cached {
		webQueryNoteInsertCacheMut.Lock()
		webQueryNoteInsertCache[key] = cache
		webQueryNoteInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WebQueryNote.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WebQueryNote) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	webQueryNoteUpdateCacheMut.RLock()
	cache, cached := webQueryNoteUpdateCache[key]
	webQueryNoteUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			webQueryNoteAllColumns,
			webQueryNotePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update web_query_notes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"web_query_notes\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, webQueryNotePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(webQueryNoteType, webQueryNoteMapping, append(wl, webQueryNotePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update web_query_notes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for web_query_notes")
	}

	if !cached {
		webQueryNoteUpdateCacheMut.Lock()
		webQueryNoteUpdateCache[key] = cache
		webQueryNoteUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q webQueryNoteQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for web_query_notes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for web_query_notes")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WebQueryNoteSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), webQueryNotePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"web_query_notes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, webQueryNotePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in webQueryNote slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all webQueryNote")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WebQueryNote) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no web_query_notes provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(webQueryNoteColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	webQueryNoteUpsertCacheMut.RLock()
	cache, cached := webQueryNoteUpsertCache[key]
	webQueryNoteUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			webQueryNoteAllColumns,
			webQueryNoteColumnsWithDefault,
			webQueryNoteColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			webQueryNoteAllColumns,
			webQueryNotePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert web_query_notes, could not build update column list")
		}

		ret := strmangle.SetComplement(webQueryNoteAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(webQueryNotePrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert web_query_notes, could not build conflict column list")
			}

			conflict = make([]string, len(webQueryNotePrimaryKeyColumns))
			copy(conflict, webQueryNotePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"web_query_notes\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(webQueryNoteType, webQueryNoteMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(webQueryNoteType, webQueryNoteMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert web_query_notes")
	}

	if !cached {
		webQueryNoteUpsertCacheMut.Lock()
		webQueryNoteUpsertCache[key] = cache
		webQueryNoteUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WebQueryNote record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WebQueryNote) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no WebQueryNote provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), webQueryNotePrimaryKeyMapping)
	sql := "DELETE FROM \"web_query_notes\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from web_query_notes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for web_query_notes")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q webQueryNoteQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no webQueryNoteQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from web_query_notes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for web_query_notes")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WebQueryNoteSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(webQueryNoteBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), webQueryNotePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"web_query_notes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, webQueryNotePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from webQueryNote slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for web_query_notes")
	}

	if len(webQueryNoteAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WebQueryNote) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWebQueryNote(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WebQueryNoteSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WebQueryNoteSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), webQueryNotePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"web_query_notes\".* FROM \"web_query_notes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, webQueryNotePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in WebQueryNoteSlice")
	}

	*o = slice

	return nil
}

// WebQueryNoteExists checks if the WebQueryNote row exists.
func WebQueryNoteExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"web_query_notes\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if web_query_notes exists")
	}

	return exists, nil
}

// Exists checks if the WebQueryNote row exists.
func (o *WebQueryNote) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WebQueryNoteExists(ctx, exec, o.ID)
}
