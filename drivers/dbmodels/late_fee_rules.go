// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// LateFeeRule is an object representing the database table.
type LateFeeRule struct {
	ID              string        `boil:"id" json:"id" toml:"id" yaml:"id"`
	ProductID       string        `boil:"product_id" json:"product_id" toml:"product_id" yaml:"product_id"`
	FeeType         string        `boil:"fee_type" json:"fee_type" toml:"fee_type" yaml:"fee_type"`
	RateOrAmount    types.Decimal `boil:"rate_or_amount" json:"rate_or_amount" toml:"rate_or_amount" yaml:"rate_or_amount"`
	GracePeriodDays int           `boil:"grace_period_days" json:"grace_period_days" toml:"grace_period_days" yaml:"grace_period_days"`
	MaxFeeAmount    types.Decimal `boil:"max_fee_amount" json:"max_fee_amount" toml:"max_fee_amount" yaml:"max_fee_amount"`
	InterestRate    types.Decimal `boil:"interest_rate" json:"interest_rate" toml:"interest_rate" yaml:"interest_rate"`
	RegulatoryCap   types.Decimal `boil:"regulatory_cap" json:"regulatory_cap" toml:"regulatory_cap" yaml:"regulatory_cap"`
	EffectiveFrom   time.Time     `boil:"effective_from" json:"effective_from" toml:"effective_from" yaml:"effective_from"`
	EffectiveTo     time.Time     `boil:"effective_to" json:"effective_to" toml:"effective_to" yaml:"effective_to"`
	CreatedAt       time.Time     `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt       time.Time     `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *lateFeeRuleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L lateFeeRuleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var LateFeeRuleColumns = struct {
	ID              string
	ProductID       string
	FeeType         string
	RateOrAmount    string
	GracePeriodDays string
	MaxFeeAmount    string
	InterestRate    string
	RegulatoryCap   string
	EffectiveFrom   string
	EffectiveTo     string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "id",
	ProductID:       "product_id",
	FeeType:         "fee_type",
	RateOrAmount:    "rate_or_amount",
	GracePeriodDays: "grace_period_days",
	MaxFeeAmount:    "max_fee_amount",
	InterestRate:    "interest_rate",
	RegulatoryCap:   "regulatory_cap",
	EffectiveFrom:   "effective_from",
	EffectiveTo:     "effective_to",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
}

var LateFeeRuleTableColumns = struct {
	ID              string
	ProductID       string
	FeeType         string
	RateOrAmount    string
	GracePeriodDays string
	MaxFeeAmount    string
	InterestRate    string
	RegulatoryCap   string
	EffectiveFrom   string
	EffectiveTo     string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "late_fee_rules.id",
	ProductID:       "late_fee_rules.product_id",
	FeeType:         "late_fee_rules.fee_type",
	RateOrAmount:    "late_fee_rules.rate_or_amount",
	GracePeriodDays: "late_fee_rules.grace_period_days",
	MaxFeeAmount:    "late_fee_rules.max_fee_amount",
	InterestRate:    "late_fee_rules.interest_rate",
	RegulatoryCap:   "late_fee_rules.regulatory_cap",
	EffectiveFrom:   "late_fee_rules.effective_from",
	EffectiveTo:     "late_fee_rules.effective_to",
	CreatedAt:       "late_fee_rules.created_at",
	UpdatedAt:       "late_fee_rules.updated_at",
}

// Generated where

var LateFeeRuleWhere = struct {
	ID              whereHelperstring
	ProductID       whereHelperstring
	FeeType         whereHelperstring
	RateOrAmount    whereHelpertypes_Decimal
	GracePeriodDays whereHelperint
	MaxFeeAmount    whereHelpertypes_Decimal
	InterestRate    whereHelpertypes_Decimal
	RegulatoryCap   whereHelpertypes_Decimal
	EffectiveFrom   whereHelpertime_Time
	EffectiveTo     whereHelpertime_Time
	CreatedAt       whereHelpertime_Time
	UpdatedAt       whereHelpertime_Time
}{
	ID:              whereHelperstring{field: "\"late_fee_rules\".\"id\""},
	ProductID:       whereHelperstring{field: "\"late_fee_rules\".\"product_id\""},
	FeeType:         whereHelperstring{field: "\"late_fee_rules\".\"fee_type\""},
	RateOrAmount:    whereHelpertypes_Decimal{field: "\"late_fee_rules\".\"rate_or_amount\""},
	GracePeriodDays: whereHelperint{field: "\"late_fee_rules\".\"grace_period_days\""},
	MaxFeeAmount:    whereHelpertypes_Decimal{field: "\"late_fee_rules\".\"max_fee_amount\""},
	InterestRate:    whereHelpertypes_Decimal{field: "\"late_fee_rules\".\"interest_rate\""},
	RegulatoryCap:   whereHelpertypes_Decimal{field: "\"late_fee_rules\".\"regulatory_cap\""},
	EffectiveFrom:   whereHelpertime_Time{field: "\"late_fee_rules\".\"effective_from\""},
	EffectiveTo:     whereHelpertime_Time{field: "\"late_fee_rules\".\"effective_to\""},
	CreatedAt:       whereHelpertime_Time{field: "\"late_fee_rules\".\"created_at\""},
	UpdatedAt:       whereHelpertime_Time{field: "\"late_fee_rules\".\"updated_at\""},
}

// LateFeeRuleRels is where relationship names are stored.
var LateFeeRuleRels = struct {
	Product string
}{
	Product: "Product",
}

// lateFeeRuleR is where relationships are stored.
type lateFeeRuleR struct {
	Product *Product `boil:"Product" json:"Product" toml:"Product" yaml:"Product"`
}

// NewStruct creates a new relationship struct
func (*lateFeeRuleR) NewStruct() *lateFeeRuleR {
	return &lateFeeRuleR{}
}

func (o *LateFeeRule) GetProduct() *Product {
	if o == nil {
		return nil
	}

	return o.R.GetProduct()
}

func (r *lateFeeRuleR) GetProduct() *Product {
	if r == nil {
		return nil
	}

	return r.Product
}

// lateFeeRuleL is where Load methods for each relationship are stored.
type lateFeeRuleL struct{}

var (
	lateFeeRuleAllColumns            = []string{"id", "product_id", "fee_type", "rate_or_amount", "grace_period_days", "max_fee_amount", "interest_rate", "regulatory_cap", "effective_from", "effective_to", "created_at", "updated_at"}
	lateFeeRuleColumnsWithoutDefault = []string{"id", "product_id", "fee_type", "rate_or_amount", "max_fee_amount", "interest_rate", "regulatory_cap"}
	lateFeeRuleColumnsWithDefault    = []string{"grace_period_days", "effective_from", "effective_to", "created_at", "updated_at"}
	lateFeeRulePrimaryKeyColumns     = []string{"id"}
	lateFeeRuleGeneratedColumns      = []string{}
)

type (
	// LateFeeRuleSlice is an alias for a slice of pointers to LateFeeRule.
	// This should almost always be used instead of []LateFeeRule.
	LateFeeRuleSlice []*LateFeeRule
	// LateFeeRuleHook is the signature for custom LateFeeRule hook methods
	LateFeeRuleHook func(context.Context, boil.ContextExecutor, *LateFeeRule) error

	lateFeeRuleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	lateFeeRuleType                 = reflect.TypeOf(&LateFeeRule{})
	lateFeeRuleMapping              = queries.MakeStructMapping(lateFeeRuleType)
	lateFeeRulePrimaryKeyMapping, _ = queries.BindMapping(lateFeeRuleType, lateFeeRuleMapping, lateFeeRulePrimaryKeyColumns)
	lateFeeRuleInsertCacheMut       sync.RWMutex
	lateFeeRuleInsertCache          = make(map[string]insertCache)
	lateFeeRuleUpdateCacheMut       sync.RWMutex
	lateFeeRuleUpdateCache          = make(map[string]updateCache)
	lateFeeRuleUpsertCacheMut       sync.RWMutex
	lateFeeRuleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var lateFeeRuleAfterSelectMu sync.Mutex
var lateFeeRuleAfterSelectHooks []LateFeeRuleHook

var lateFeeRuleBeforeInsertMu sync.Mutex
var lateFeeRuleBeforeInsertHooks []LateFeeRuleHook
var lateFeeRuleAfterInsertMu sync.Mutex
var lateFeeRuleAfterInsertHooks []LateFeeRuleHook

var lateFeeRuleBeforeUpdateMu sync.Mutex
var lateFeeRuleBeforeUpdateHooks []LateFeeRuleHook
var lateFeeRuleAfterUpdateMu sync.Mutex
var lateFeeRuleAfterUpdateHooks []LateFeeRuleHook

var lateFeeRuleBeforeDeleteMu sync.Mutex
var lateFeeRuleBeforeDeleteHooks []LateFeeRuleHook
var lateFeeRuleAfterDeleteMu sync.Mutex
var lateFeeRuleAfterDeleteHooks []LateFeeRuleHook

var lateFeeRuleBeforeUpsertMu sync.Mutex
var lateFeeRuleBeforeUpsertHooks []LateFeeRuleHook
var lateFeeRuleAfterUpsertMu sync.Mutex
var lateFeeRuleAfterUpsertHooks []LateFeeRuleHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *LateFeeRule) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeRuleAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *LateFeeRule) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeRuleBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *LateFeeRule) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeRuleAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *LateFeeRule) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeRuleBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *LateFeeRule) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeRuleAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *LateFeeRule) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeRuleBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *LateFeeRule) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeRuleAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *LateFeeRule) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeRuleBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *LateFeeRule) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeRuleAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddLateFeeRuleHook registers your hook function for all future operations.
func AddLateFeeRuleHook(hookPoint boil.HookPoint, lateFeeRuleHook LateFeeRuleHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		lateFeeRuleAfterSelectMu.Lock()
		lateFeeRuleAfterSelectHooks = append(lateFeeRuleAfterSelectHooks, lateFeeRuleHook)
		lateFeeRuleAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		lateFeeRuleBeforeInsertMu.Lock()
		lateFeeRuleBeforeInsertHooks = append(lateFeeRuleBeforeInsertHooks, lateFeeRuleHook)
		lateFeeRuleBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		lateFeeRuleAfterInsertMu.Lock()
		lateFeeRuleAfterInsertHooks = append(lateFeeRuleAfterInsertHooks, lateFeeRuleHook)
		lateFeeRuleAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		lateFeeRuleBeforeUpdateMu.Lock()
		lateFeeRuleBeforeUpdateHooks = append(lateFeeRuleBeforeUpdateHooks, lateFeeRuleHook)
		lateFeeRuleBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		lateFeeRuleAfterUpdateMu.Lock()
		lateFeeRuleAfterUpdateHooks = append(lateFeeRuleAfterUpdateHooks, lateFeeRuleHook)
		lateFeeRuleAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		lateFeeRuleBeforeDeleteMu.Lock()
		lateFeeRuleBeforeDeleteHooks = append(lateFeeRuleBeforeDeleteHooks, lateFeeRuleHook)
		lateFeeRuleBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		lateFeeRuleAfterDeleteMu.Lock()
		lateFeeRuleAfterDeleteHooks = append(lateFeeRuleAfterDeleteHooks, lateFeeRuleHook)
		lateFeeRuleAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		lateFeeRuleBeforeUpsertMu.Lock()
		lateFeeRuleBeforeUpsertHooks = append(lateFeeRuleBeforeUpsertHooks, lateFeeRuleHook)
		lateFeeRuleBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		lateFeeRuleAfterUpsertMu.Lock()
		lateFeeRuleAfterUpsertHooks = append(lateFeeRuleAfterUpsertHooks, lateFeeRuleHook)
		lateFeeRuleAfterUpsertMu.Unlock()
	}
}

// One returns a single lateFeeRule record from the query.
func (q lateFeeRuleQuery) One(ctx context.Context, exec boil.ContextExecutor) (*LateFeeRule, error) {
	o := &LateFeeRule{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for late_fee_rules")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all LateFeeRule records from the query.
func (q lateFeeRuleQuery) All(ctx context.Context, exec boil.ContextExecutor) (LateFeeRuleSlice, error) {
	var o []*LateFeeRule

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to LateFeeRule slice")
	}

	if len(lateFeeRuleAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all LateFeeRule records in the query.
func (q lateFeeRuleQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count late_fee_rules rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q lateFeeRuleQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if late_fee_rules exists")
	}

	return count > 0, nil
}

// Product pointed to by the foreign key.
func (o *LateFeeRule) Product(mods ...qm.QueryMod) productQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ProductID),
	}

	queryMods = append(queryMods, mods...)

	return Products(queryMods...)
}

// LoadProduct allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (lateFeeRuleL) LoadProduct(ctx context.Context, e boil.ContextExecutor, singular bool, maybeLateFeeRule interface{}, mods queries.Applicator) error {
	var slice []*LateFeeRule
	var object *LateFeeRule

	if singular {
		var ok bool
		object, ok = maybeLateFeeRule.(*LateFeeRule)
		if !ok {
			object = new(LateFeeRule)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeLateFeeRule)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeLateFeeRule))
			}
		}
	} else {
		s, ok := maybeLateFeeRule.(*[]*LateFeeRule)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeLateFeeRule)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeLateFeeRule))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &lateFeeRuleR{}
		}
		args[object.ProductID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &lateFeeRuleR{}
			}

			args[obj.ProductID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`products`),
		qm.WhereIn(`products.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Product")
	}

	var resultSlice []*Product
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Product")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for products")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for products")
	}

	if len(productAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Product = foreign
		if foreign.R == nil {
			foreign.R = &productR{}
		}
		foreign.R.LateFeeRules = append(foreign.R.LateFeeRules, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProductID == foreign.ID {
				local.R.Product = foreign
				if foreign.R == nil {
					foreign.R = &productR{}
				}
				foreign.R.LateFeeRules = append(foreign.R.LateFeeRules, local)
				break
			}
		}
	}

	return nil
}

// SetProduct of the lateFeeRule to the related item.
// Sets o.R.Product to related.
// Adds o to related.R.LateFeeRules.
func (o *LateFeeRule) SetProduct(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Product) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"late_fee_rules\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"product_id"}),
		strmangle.WhereClause("\"", "\"", 2, lateFeeRulePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProductID = related.ID
	if o.R == nil {
		o.R = &lateFeeRuleR{
			Product: related,
		}
	} else {
		o.R.Product = related
	}

	if related.R == nil {
		related.R = &productR{
			LateFeeRules: LateFeeRuleSlice{o},
		}
	} else {
		related.R.LateFeeRules = append(related.R.LateFeeRules, o)
	}

	return nil
}

// LateFeeRules retrieves all the records using an executor.
func LateFeeRules(mods ...qm.QueryMod) lateFeeRuleQuery {
	mods = append(mods, qm.From("\"late_fee_rules\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"late_fee_rules\".*"})
	}

	return lateFeeRuleQuery{q}
}

// FindLateFeeRule retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindLateFeeRule(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*LateFeeRule, error) {
	lateFeeRuleObj := &LateFeeRule{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"late_fee_rules\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, lateFeeRuleObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from late_fee_rules")
	}

	if err = lateFeeRuleObj.doAfterSelectHooks(ctx, exec); err != nil {
		return lateFeeRuleObj, err
	}

	return lateFeeRuleObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *LateFeeRule) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no late_fee_rules provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lateFeeRuleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	lateFeeRuleInsertCacheMut.RLock()
	cache, cached := lateFeeRuleInsertCache[key]
	lateFeeRuleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			lateFeeRuleAllColumns,
			lateFeeRuleColumnsWithDefault,
			lateFeeRuleColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(lateFeeRuleType, lateFeeRuleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(lateFeeRuleType, lateFeeRuleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"late_fee_rules\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"late_fee_rules\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into late_fee_rules")
	}

	if !cached {
		lateFeeRuleInsertCacheMut.Lock()
		lateFeeRuleInsertCache[key] = cache
		lateFeeRuleInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the LateFeeRule.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *LateFeeRule) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	lateFeeRuleUpdateCacheMut.RLock()
	cache, cached := lateFeeRuleUpdateCache[key]
	lateFeeRuleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			lateFeeRuleAllColumns,
			lateFeeRulePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update late_fee_rules, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"late_fee_rules\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, lateFeeRulePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(lateFeeRuleType, lateFeeRuleMapping, append(wl, lateFeeRulePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update late_fee_rules row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for late_fee_rules")
	}

	if !cached {
		lateFeeRuleUpdateCacheMut.Lock()
		lateFeeRuleUpdateCache[key] = cache
		lateFeeRuleUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q lateFeeRuleQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for late_fee_rules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for late_fee_rules")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o LateFeeRuleSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lateFeeRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"late_fee_rules\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, lateFeeRulePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in lateFeeRule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all lateFeeRule")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *LateFeeRule) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no late_fee_rules provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lateFeeRuleColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	lateFeeRuleUpsertCacheMut.RLock()
	cache, cached := lateFeeRuleUpsertCache[key]
	lateFeeRuleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			lateFeeRuleAllColumns,
			lateFeeRuleColumnsWithDefault,
			lateFeeRuleColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			lateFeeRuleAllColumns,
			lateFeeRulePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert late_fee_rules, could not build update column list")
		}

		ret := strmangle.SetComplement(lateFeeRuleAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(lateFeeRulePrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert late_fee_rules, could not build conflict column list")
			}

			conflict = make([]string, len(lateFeeRulePrimaryKeyColumns))
			copy(conflict, lateFeeRulePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"late_fee_rules\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(lateFeeRuleType, lateFeeRuleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(lateFeeRuleType, lateFeeRuleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert late_fee_rules")
	}

	if !cached {
		lateFeeRuleUpsertCacheMut.Lock()
		lateFeeRuleUpsertCache[key] = cache
		lateFeeRuleUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single LateFeeRule record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *LateFeeRule) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no LateFeeRule provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), lateFeeRulePrimaryKeyMapping)
	sql := "DELETE FROM \"late_fee_rules\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from late_fee_rules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for late_fee_rules")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q lateFeeRuleQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no lateFeeRuleQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from late_fee_rules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for late_fee_rules")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o LateFeeRuleSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(lateFeeRuleBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lateFeeRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"late_fee_rules\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lateFeeRulePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from lateFeeRule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for late_fee_rules")
	}

	if len(lateFeeRuleAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *LateFeeRule) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindLateFeeRule(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *LateFeeRuleSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := LateFeeRuleSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lateFeeRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"late_fee_rules\".* FROM \"late_fee_rules\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lateFeeRulePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in LateFeeRuleSlice")
	}

	*o = slice

	return nil
}

// LateFeeRuleExists checks if the LateFeeRule row exists.
func LateFeeRuleExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"late_fee_rules\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if late_fee_rules exists")
	}

	return exists, nil
}

// Exists checks if the LateFeeRule row exists.
func (o *LateFeeRule) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return LateFeeRuleExists(ctx, exec, o.ID)
}
