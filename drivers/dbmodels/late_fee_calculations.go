// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// LateFeeCalculation is an object representing the database table.
type LateFeeCalculation struct {
	ID               string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	AccountID        string      `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	PaymentID        string      `boil:"payment_id" json:"payment_id" toml:"payment_id" yaml:"payment_id"`
	CalculationDate  time.Time   `boil:"calculation_date" json:"calculation_date" toml:"calculation_date" yaml:"calculation_date"`
	FeeType          string      `boil:"fee_type" json:"fee_type" toml:"fee_type" yaml:"fee_type"`
	BaseAmount       string      `boil:"base_amount" json:"base_amount" toml:"base_amount" yaml:"base_amount"`
	CalculatedAmount string      `boil:"calculated_amount" json:"calculated_amount" toml:"calculated_amount" yaml:"calculated_amount"`
	Status           string      `boil:"status" json:"status" toml:"status" yaml:"status"`
	CreatedBy        int         `boil:"created_by" json:"created_by" toml:"created_by" yaml:"created_by"`
	ApprovedBy       null.Int    `boil:"approved_by" json:"approved_by,omitempty" toml:"approved_by" yaml:"approved_by,omitempty"`
	ApprovedAt       null.Time   `boil:"approved_at" json:"approved_at,omitempty" toml:"approved_at" yaml:"approved_at,omitempty"`
	Reason           null.String `boil:"reason" json:"reason,omitempty" toml:"reason" yaml:"reason,omitempty"`
	CreatedAt        time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt        time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *lateFeeCalculationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L lateFeeCalculationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var LateFeeCalculationColumns = struct {
	ID               string
	AccountID        string
	PaymentID        string
	CalculationDate  string
	FeeType          string
	BaseAmount       string
	CalculatedAmount string
	Status           string
	CreatedBy        string
	ApprovedBy       string
	ApprovedAt       string
	Reason           string
	CreatedAt        string
	UpdatedAt        string
}{
	ID:               "id",
	AccountID:        "account_id",
	PaymentID:        "payment_id",
	CalculationDate:  "calculation_date",
	FeeType:          "fee_type",
	BaseAmount:       "base_amount",
	CalculatedAmount: "calculated_amount",
	Status:           "status",
	CreatedBy:        "created_by",
	ApprovedBy:       "approved_by",
	ApprovedAt:       "approved_at",
	Reason:           "reason",
	CreatedAt:        "created_at",
	UpdatedAt:        "updated_at",
}

var LateFeeCalculationTableColumns = struct {
	ID               string
	AccountID        string
	PaymentID        string
	CalculationDate  string
	FeeType          string
	BaseAmount       string
	CalculatedAmount string
	Status           string
	CreatedBy        string
	ApprovedBy       string
	ApprovedAt       string
	Reason           string
	CreatedAt        string
	UpdatedAt        string
}{
	ID:               "late_fee_calculations.id",
	AccountID:        "late_fee_calculations.account_id",
	PaymentID:        "late_fee_calculations.payment_id",
	CalculationDate:  "late_fee_calculations.calculation_date",
	FeeType:          "late_fee_calculations.fee_type",
	BaseAmount:       "late_fee_calculations.base_amount",
	CalculatedAmount: "late_fee_calculations.calculated_amount",
	Status:           "late_fee_calculations.status",
	CreatedBy:        "late_fee_calculations.created_by",
	ApprovedBy:       "late_fee_calculations.approved_by",
	ApprovedAt:       "late_fee_calculations.approved_at",
	Reason:           "late_fee_calculations.reason",
	CreatedAt:        "late_fee_calculations.created_at",
	UpdatedAt:        "late_fee_calculations.updated_at",
}

// Generated where

var LateFeeCalculationWhere = struct {
	ID               whereHelperstring
	AccountID        whereHelperstring
	PaymentID        whereHelperstring
	CalculationDate  whereHelpertime_Time
	FeeType          whereHelperstring
	BaseAmount       whereHelperstring
	CalculatedAmount whereHelperstring
	Status           whereHelperstring
	CreatedBy        whereHelperint
	ApprovedBy       whereHelpernull_Int
	ApprovedAt       whereHelpernull_Time
	Reason           whereHelpernull_String
	CreatedAt        whereHelpertime_Time
	UpdatedAt        whereHelpertime_Time
}{
	ID:               whereHelperstring{field: "\"late_fee_calculations\".\"id\""},
	AccountID:        whereHelperstring{field: "\"late_fee_calculations\".\"account_id\""},
	PaymentID:        whereHelperstring{field: "\"late_fee_calculations\".\"payment_id\""},
	CalculationDate:  whereHelpertime_Time{field: "\"late_fee_calculations\".\"calculation_date\""},
	FeeType:          whereHelperstring{field: "\"late_fee_calculations\".\"fee_type\""},
	BaseAmount:       whereHelperstring{field: "\"late_fee_calculations\".\"base_amount\""},
	CalculatedAmount: whereHelperstring{field: "\"late_fee_calculations\".\"calculated_amount\""},
	Status:           whereHelperstring{field: "\"late_fee_calculations\".\"status\""},
	CreatedBy:        whereHelperint{field: "\"late_fee_calculations\".\"created_by\""},
	ApprovedBy:       whereHelpernull_Int{field: "\"late_fee_calculations\".\"approved_by\""},
	ApprovedAt:       whereHelpernull_Time{field: "\"late_fee_calculations\".\"approved_at\""},
	Reason:           whereHelpernull_String{field: "\"late_fee_calculations\".\"reason\""},
	CreatedAt:        whereHelpertime_Time{field: "\"late_fee_calculations\".\"created_at\""},
	UpdatedAt:        whereHelpertime_Time{field: "\"late_fee_calculations\".\"updated_at\""},
}

// LateFeeCalculationRels is where relationship names are stored.
var LateFeeCalculationRels = struct {
	Account string
}{
	Account: "Account",
}

// lateFeeCalculationR is where relationships are stored.
type lateFeeCalculationR struct {
	Account *Account `boil:"Account" json:"Account" toml:"Account" yaml:"Account"`
}

// NewStruct creates a new relationship struct
func (*lateFeeCalculationR) NewStruct() *lateFeeCalculationR {
	return &lateFeeCalculationR{}
}

func (o *LateFeeCalculation) GetAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetAccount()
}

func (r *lateFeeCalculationR) GetAccount() *Account {
	if r == nil {
		return nil
	}

	return r.Account
}

// lateFeeCalculationL is where Load methods for each relationship are stored.
type lateFeeCalculationL struct{}

var (
	lateFeeCalculationAllColumns            = []string{"id", "account_id", "payment_id", "calculation_date", "fee_type", "base_amount", "calculated_amount", "status", "created_by", "approved_by", "approved_at", "reason", "created_at", "updated_at"}
	lateFeeCalculationColumnsWithoutDefault = []string{"id", "account_id", "payment_id", "fee_type", "base_amount", "calculated_amount", "status", "created_by"}
	lateFeeCalculationColumnsWithDefault    = []string{"calculation_date", "approved_by", "approved_at", "reason", "created_at", "updated_at"}
	lateFeeCalculationPrimaryKeyColumns     = []string{"id"}
	lateFeeCalculationGeneratedColumns      = []string{}
)

type (
	// LateFeeCalculationSlice is an alias for a slice of pointers to LateFeeCalculation.
	// This should almost always be used instead of []LateFeeCalculation.
	LateFeeCalculationSlice []*LateFeeCalculation
	// LateFeeCalculationHook is the signature for custom LateFeeCalculation hook methods
	LateFeeCalculationHook func(context.Context, boil.ContextExecutor, *LateFeeCalculation) error

	lateFeeCalculationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	lateFeeCalculationType                 = reflect.TypeOf(&LateFeeCalculation{})
	lateFeeCalculationMapping              = queries.MakeStructMapping(lateFeeCalculationType)
	lateFeeCalculationPrimaryKeyMapping, _ = queries.BindMapping(lateFeeCalculationType, lateFeeCalculationMapping, lateFeeCalculationPrimaryKeyColumns)
	lateFeeCalculationInsertCacheMut       sync.RWMutex
	lateFeeCalculationInsertCache          = make(map[string]insertCache)
	lateFeeCalculationUpdateCacheMut       sync.RWMutex
	lateFeeCalculationUpdateCache          = make(map[string]updateCache)
	lateFeeCalculationUpsertCacheMut       sync.RWMutex
	lateFeeCalculationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var lateFeeCalculationAfterSelectMu sync.Mutex
var lateFeeCalculationAfterSelectHooks []LateFeeCalculationHook

var lateFeeCalculationBeforeInsertMu sync.Mutex
var lateFeeCalculationBeforeInsertHooks []LateFeeCalculationHook
var lateFeeCalculationAfterInsertMu sync.Mutex
var lateFeeCalculationAfterInsertHooks []LateFeeCalculationHook

var lateFeeCalculationBeforeUpdateMu sync.Mutex
var lateFeeCalculationBeforeUpdateHooks []LateFeeCalculationHook
var lateFeeCalculationAfterUpdateMu sync.Mutex
var lateFeeCalculationAfterUpdateHooks []LateFeeCalculationHook

var lateFeeCalculationBeforeDeleteMu sync.Mutex
var lateFeeCalculationBeforeDeleteHooks []LateFeeCalculationHook
var lateFeeCalculationAfterDeleteMu sync.Mutex
var lateFeeCalculationAfterDeleteHooks []LateFeeCalculationHook

var lateFeeCalculationBeforeUpsertMu sync.Mutex
var lateFeeCalculationBeforeUpsertHooks []LateFeeCalculationHook
var lateFeeCalculationAfterUpsertMu sync.Mutex
var lateFeeCalculationAfterUpsertHooks []LateFeeCalculationHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *LateFeeCalculation) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeCalculationAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *LateFeeCalculation) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeCalculationBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *LateFeeCalculation) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeCalculationAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *LateFeeCalculation) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeCalculationBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *LateFeeCalculation) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeCalculationAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *LateFeeCalculation) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeCalculationBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *LateFeeCalculation) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeCalculationAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *LateFeeCalculation) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeCalculationBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *LateFeeCalculation) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lateFeeCalculationAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddLateFeeCalculationHook registers your hook function for all future operations.
func AddLateFeeCalculationHook(hookPoint boil.HookPoint, lateFeeCalculationHook LateFeeCalculationHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		lateFeeCalculationAfterSelectMu.Lock()
		lateFeeCalculationAfterSelectHooks = append(lateFeeCalculationAfterSelectHooks, lateFeeCalculationHook)
		lateFeeCalculationAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		lateFeeCalculationBeforeInsertMu.Lock()
		lateFeeCalculationBeforeInsertHooks = append(lateFeeCalculationBeforeInsertHooks, lateFeeCalculationHook)
		lateFeeCalculationBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		lateFeeCalculationAfterInsertMu.Lock()
		lateFeeCalculationAfterInsertHooks = append(lateFeeCalculationAfterInsertHooks, lateFeeCalculationHook)
		lateFeeCalculationAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		lateFeeCalculationBeforeUpdateMu.Lock()
		lateFeeCalculationBeforeUpdateHooks = append(lateFeeCalculationBeforeUpdateHooks, lateFeeCalculationHook)
		lateFeeCalculationBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		lateFeeCalculationAfterUpdateMu.Lock()
		lateFeeCalculationAfterUpdateHooks = append(lateFeeCalculationAfterUpdateHooks, lateFeeCalculationHook)
		lateFeeCalculationAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		lateFeeCalculationBeforeDeleteMu.Lock()
		lateFeeCalculationBeforeDeleteHooks = append(lateFeeCalculationBeforeDeleteHooks, lateFeeCalculationHook)
		lateFeeCalculationBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		lateFeeCalculationAfterDeleteMu.Lock()
		lateFeeCalculationAfterDeleteHooks = append(lateFeeCalculationAfterDeleteHooks, lateFeeCalculationHook)
		lateFeeCalculationAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		lateFeeCalculationBeforeUpsertMu.Lock()
		lateFeeCalculationBeforeUpsertHooks = append(lateFeeCalculationBeforeUpsertHooks, lateFeeCalculationHook)
		lateFeeCalculationBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		lateFeeCalculationAfterUpsertMu.Lock()
		lateFeeCalculationAfterUpsertHooks = append(lateFeeCalculationAfterUpsertHooks, lateFeeCalculationHook)
		lateFeeCalculationAfterUpsertMu.Unlock()
	}
}

// One returns a single lateFeeCalculation record from the query.
func (q lateFeeCalculationQuery) One(ctx context.Context, exec boil.ContextExecutor) (*LateFeeCalculation, error) {
	o := &LateFeeCalculation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for late_fee_calculations")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all LateFeeCalculation records from the query.
func (q lateFeeCalculationQuery) All(ctx context.Context, exec boil.ContextExecutor) (LateFeeCalculationSlice, error) {
	var o []*LateFeeCalculation

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to LateFeeCalculation slice")
	}

	if len(lateFeeCalculationAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all LateFeeCalculation records in the query.
func (q lateFeeCalculationQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count late_fee_calculations rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q lateFeeCalculationQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if late_fee_calculations exists")
	}

	return count > 0, nil
}

// Account pointed to by the foreign key.
func (o *LateFeeCalculation) Account(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// LoadAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (lateFeeCalculationL) LoadAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeLateFeeCalculation interface{}, mods queries.Applicator) error {
	var slice []*LateFeeCalculation
	var object *LateFeeCalculation

	if singular {
		var ok bool
		object, ok = maybeLateFeeCalculation.(*LateFeeCalculation)
		if !ok {
			object = new(LateFeeCalculation)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeLateFeeCalculation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeLateFeeCalculation))
			}
		}
	} else {
		s, ok := maybeLateFeeCalculation.(*[]*LateFeeCalculation)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeLateFeeCalculation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeLateFeeCalculation))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &lateFeeCalculationR{}
		}
		args[object.AccountID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &lateFeeCalculationR{}
			}

			args[obj.AccountID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Account = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.LateFeeCalculations = append(foreign.R.LateFeeCalculations, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AccountID == foreign.ID {
				local.R.Account = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.LateFeeCalculations = append(foreign.R.LateFeeCalculations, local)
				break
			}
		}
	}

	return nil
}

// SetAccount of the lateFeeCalculation to the related item.
// Sets o.R.Account to related.
// Adds o to related.R.LateFeeCalculations.
func (o *LateFeeCalculation) SetAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"late_fee_calculations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
		strmangle.WhereClause("\"", "\"", 2, lateFeeCalculationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AccountID = related.ID
	if o.R == nil {
		o.R = &lateFeeCalculationR{
			Account: related,
		}
	} else {
		o.R.Account = related
	}

	if related.R == nil {
		related.R = &accountR{
			LateFeeCalculations: LateFeeCalculationSlice{o},
		}
	} else {
		related.R.LateFeeCalculations = append(related.R.LateFeeCalculations, o)
	}

	return nil
}

// LateFeeCalculations retrieves all the records using an executor.
func LateFeeCalculations(mods ...qm.QueryMod) lateFeeCalculationQuery {
	mods = append(mods, qm.From("\"late_fee_calculations\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"late_fee_calculations\".*"})
	}

	return lateFeeCalculationQuery{q}
}

// FindLateFeeCalculation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindLateFeeCalculation(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*LateFeeCalculation, error) {
	lateFeeCalculationObj := &LateFeeCalculation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"late_fee_calculations\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, lateFeeCalculationObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from late_fee_calculations")
	}

	if err = lateFeeCalculationObj.doAfterSelectHooks(ctx, exec); err != nil {
		return lateFeeCalculationObj, err
	}

	return lateFeeCalculationObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *LateFeeCalculation) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no late_fee_calculations provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lateFeeCalculationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	lateFeeCalculationInsertCacheMut.RLock()
	cache, cached := lateFeeCalculationInsertCache[key]
	lateFeeCalculationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			lateFeeCalculationAllColumns,
			lateFeeCalculationColumnsWithDefault,
			lateFeeCalculationColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(lateFeeCalculationType, lateFeeCalculationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(lateFeeCalculationType, lateFeeCalculationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"late_fee_calculations\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"late_fee_calculations\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into late_fee_calculations")
	}

	if !cached {
		lateFeeCalculationInsertCacheMut.Lock()
		lateFeeCalculationInsertCache[key] = cache
		lateFeeCalculationInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the LateFeeCalculation.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *LateFeeCalculation) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	lateFeeCalculationUpdateCacheMut.RLock()
	cache, cached := lateFeeCalculationUpdateCache[key]
	lateFeeCalculationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			lateFeeCalculationAllColumns,
			lateFeeCalculationPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update late_fee_calculations, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"late_fee_calculations\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, lateFeeCalculationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(lateFeeCalculationType, lateFeeCalculationMapping, append(wl, lateFeeCalculationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update late_fee_calculations row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for late_fee_calculations")
	}

	if !cached {
		lateFeeCalculationUpdateCacheMut.Lock()
		lateFeeCalculationUpdateCache[key] = cache
		lateFeeCalculationUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q lateFeeCalculationQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for late_fee_calculations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for late_fee_calculations")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o LateFeeCalculationSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lateFeeCalculationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"late_fee_calculations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, lateFeeCalculationPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in lateFeeCalculation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all lateFeeCalculation")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *LateFeeCalculation) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no late_fee_calculations provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lateFeeCalculationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	lateFeeCalculationUpsertCacheMut.RLock()
	cache, cached := lateFeeCalculationUpsertCache[key]
	lateFeeCalculationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			lateFeeCalculationAllColumns,
			lateFeeCalculationColumnsWithDefault,
			lateFeeCalculationColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			lateFeeCalculationAllColumns,
			lateFeeCalculationPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert late_fee_calculations, could not build update column list")
		}

		ret := strmangle.SetComplement(lateFeeCalculationAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(lateFeeCalculationPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert late_fee_calculations, could not build conflict column list")
			}

			conflict = make([]string, len(lateFeeCalculationPrimaryKeyColumns))
			copy(conflict, lateFeeCalculationPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"late_fee_calculations\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(lateFeeCalculationType, lateFeeCalculationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(lateFeeCalculationType, lateFeeCalculationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert late_fee_calculations")
	}

	if !cached {
		lateFeeCalculationUpsertCacheMut.Lock()
		lateFeeCalculationUpsertCache[key] = cache
		lateFeeCalculationUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single LateFeeCalculation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *LateFeeCalculation) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no LateFeeCalculation provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), lateFeeCalculationPrimaryKeyMapping)
	sql := "DELETE FROM \"late_fee_calculations\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from late_fee_calculations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for late_fee_calculations")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q lateFeeCalculationQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no lateFeeCalculationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from late_fee_calculations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for late_fee_calculations")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o LateFeeCalculationSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(lateFeeCalculationBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lateFeeCalculationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"late_fee_calculations\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lateFeeCalculationPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from lateFeeCalculation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for late_fee_calculations")
	}

	if len(lateFeeCalculationAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *LateFeeCalculation) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindLateFeeCalculation(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *LateFeeCalculationSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := LateFeeCalculationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lateFeeCalculationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"late_fee_calculations\".* FROM \"late_fee_calculations\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lateFeeCalculationPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in LateFeeCalculationSlice")
	}

	*o = slice

	return nil
}

// LateFeeCalculationExists checks if the LateFeeCalculation row exists.
func LateFeeCalculationExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"late_fee_calculations\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if late_fee_calculations exists")
	}

	return exists, nil
}

// Exists checks if the LateFeeCalculation row exists.
func (o *LateFeeCalculation) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return LateFeeCalculationExists(ctx, exec, o.ID)
}
