// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// QuoteHeader is an object representing the database table.
type QuoteHeader struct {
	ID                string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	FlightRequestID   int       `boil:"flight_request_id" json:"flight_request_id" toml:"flight_request_id" yaml:"flight_request_id"`
	CreatedAt         time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt         time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DepartsAt         time.Time `boil:"departs_at" json:"departs_at" toml:"departs_at" yaml:"departs_at"`
	FromAirportID     string    `boil:"from_airport_id" json:"from_airport_id" toml:"from_airport_id" yaml:"from_airport_id"`
	ToAirportID       string    `boil:"to_airport_id" json:"to_airport_id" toml:"to_airport_id" yaml:"to_airport_id"`
	Passenger         int       `boil:"passenger" json:"passenger" toml:"passenger" yaml:"passenger"`
	Email             string    `boil:"email" json:"email" toml:"email" yaml:"email"`
	FirstName         string    `boil:"first_name" json:"first_name" toml:"first_name" yaml:"first_name"`
	Surname           string    `boil:"surname" json:"surname" toml:"surname" yaml:"surname"`
	FullName          string    `boil:"full_name" json:"full_name" toml:"full_name" yaml:"full_name"`
	DepartsAtTimezone string    `boil:"departs_at_timezone" json:"departs_at_timezone" toml:"departs_at_timezone" yaml:"departs_at_timezone"`
	Status            string    `boil:"status" json:"status" toml:"status" yaml:"status"`
	QuoteOpsTotal     int       `boil:"quote_ops_total" json:"quote_ops_total" toml:"quote_ops_total" yaml:"quote_ops_total"`
	QuoteOpsReceived  int       `boil:"quote_ops_received" json:"quote_ops_received" toml:"quote_ops_received" yaml:"quote_ops_received"`
	QuoteOpsRejected  int       `boil:"quote_ops_rejected" json:"quote_ops_rejected" toml:"quote_ops_rejected" yaml:"quote_ops_rejected"`
	QuoteOpsDone      int       `boil:"quote_ops_done" json:"quote_ops_done" toml:"quote_ops_done" yaml:"quote_ops_done"`

	R *quoteHeaderR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L quoteHeaderL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var QuoteHeaderColumns = struct {
	ID                string
	FlightRequestID   string
	CreatedAt         string
	UpdatedAt         string
	DepartsAt         string
	FromAirportID     string
	ToAirportID       string
	Passenger         string
	Email             string
	FirstName         string
	Surname           string
	FullName          string
	DepartsAtTimezone string
	Status            string
	QuoteOpsTotal     string
	QuoteOpsReceived  string
	QuoteOpsRejected  string
	QuoteOpsDone      string
}{
	ID:                "id",
	FlightRequestID:   "flight_request_id",
	CreatedAt:         "created_at",
	UpdatedAt:         "updated_at",
	DepartsAt:         "departs_at",
	FromAirportID:     "from_airport_id",
	ToAirportID:       "to_airport_id",
	Passenger:         "passenger",
	Email:             "email",
	FirstName:         "first_name",
	Surname:           "surname",
	FullName:          "full_name",
	DepartsAtTimezone: "departs_at_timezone",
	Status:            "status",
	QuoteOpsTotal:     "quote_ops_total",
	QuoteOpsReceived:  "quote_ops_received",
	QuoteOpsRejected:  "quote_ops_rejected",
	QuoteOpsDone:      "quote_ops_done",
}

var QuoteHeaderTableColumns = struct {
	ID                string
	FlightRequestID   string
	CreatedAt         string
	UpdatedAt         string
	DepartsAt         string
	FromAirportID     string
	ToAirportID       string
	Passenger         string
	Email             string
	FirstName         string
	Surname           string
	FullName          string
	DepartsAtTimezone string
	Status            string
	QuoteOpsTotal     string
	QuoteOpsReceived  string
	QuoteOpsRejected  string
	QuoteOpsDone      string
}{
	ID:                "quote_header.id",
	FlightRequestID:   "quote_header.flight_request_id",
	CreatedAt:         "quote_header.created_at",
	UpdatedAt:         "quote_header.updated_at",
	DepartsAt:         "quote_header.departs_at",
	FromAirportID:     "quote_header.from_airport_id",
	ToAirportID:       "quote_header.to_airport_id",
	Passenger:         "quote_header.passenger",
	Email:             "quote_header.email",
	FirstName:         "quote_header.first_name",
	Surname:           "quote_header.surname",
	FullName:          "quote_header.full_name",
	DepartsAtTimezone: "quote_header.departs_at_timezone",
	Status:            "quote_header.status",
	QuoteOpsTotal:     "quote_header.quote_ops_total",
	QuoteOpsReceived:  "quote_header.quote_ops_received",
	QuoteOpsRejected:  "quote_header.quote_ops_rejected",
	QuoteOpsDone:      "quote_header.quote_ops_done",
}

// Generated where

var QuoteHeaderWhere = struct {
	ID                whereHelperstring
	FlightRequestID   whereHelperint
	CreatedAt         whereHelpertime_Time
	UpdatedAt         whereHelpertime_Time
	DepartsAt         whereHelpertime_Time
	FromAirportID     whereHelperstring
	ToAirportID       whereHelperstring
	Passenger         whereHelperint
	Email             whereHelperstring
	FirstName         whereHelperstring
	Surname           whereHelperstring
	FullName          whereHelperstring
	DepartsAtTimezone whereHelperstring
	Status            whereHelperstring
	QuoteOpsTotal     whereHelperint
	QuoteOpsReceived  whereHelperint
	QuoteOpsRejected  whereHelperint
	QuoteOpsDone      whereHelperint
}{
	ID:                whereHelperstring{field: "\"quote_header\".\"id\""},
	FlightRequestID:   whereHelperint{field: "\"quote_header\".\"flight_request_id\""},
	CreatedAt:         whereHelpertime_Time{field: "\"quote_header\".\"created_at\""},
	UpdatedAt:         whereHelpertime_Time{field: "\"quote_header\".\"updated_at\""},
	DepartsAt:         whereHelpertime_Time{field: "\"quote_header\".\"departs_at\""},
	FromAirportID:     whereHelperstring{field: "\"quote_header\".\"from_airport_id\""},
	ToAirportID:       whereHelperstring{field: "\"quote_header\".\"to_airport_id\""},
	Passenger:         whereHelperint{field: "\"quote_header\".\"passenger\""},
	Email:             whereHelperstring{field: "\"quote_header\".\"email\""},
	FirstName:         whereHelperstring{field: "\"quote_header\".\"first_name\""},
	Surname:           whereHelperstring{field: "\"quote_header\".\"surname\""},
	FullName:          whereHelperstring{field: "\"quote_header\".\"full_name\""},
	DepartsAtTimezone: whereHelperstring{field: "\"quote_header\".\"departs_at_timezone\""},
	Status:            whereHelperstring{field: "\"quote_header\".\"status\""},
	QuoteOpsTotal:     whereHelperint{field: "\"quote_header\".\"quote_ops_total\""},
	QuoteOpsReceived:  whereHelperint{field: "\"quote_header\".\"quote_ops_received\""},
	QuoteOpsRejected:  whereHelperint{field: "\"quote_header\".\"quote_ops_rejected\""},
	QuoteOpsDone:      whereHelperint{field: "\"quote_header\".\"quote_ops_done\""},
}

// QuoteHeaderRels is where relationship names are stored.
var QuoteHeaderRels = struct {
	FromAirport     string
	ToAirport       string
	FlightRequest   string
	QuoteOperations string
}{
	FromAirport:     "FromAirport",
	ToAirport:       "ToAirport",
	FlightRequest:   "FlightRequest",
	QuoteOperations: "QuoteOperations",
}

// quoteHeaderR is where relationships are stored.
type quoteHeaderR struct {
	FromAirport     *Airport            `boil:"FromAirport" json:"FromAirport" toml:"FromAirport" yaml:"FromAirport"`
	ToAirport       *Airport            `boil:"ToAirport" json:"ToAirport" toml:"ToAirport" yaml:"ToAirport"`
	FlightRequest   *FlightRequestNew   `boil:"FlightRequest" json:"FlightRequest" toml:"FlightRequest" yaml:"FlightRequest"`
	QuoteOperations QuoteOperationSlice `boil:"QuoteOperations" json:"QuoteOperations" toml:"QuoteOperations" yaml:"QuoteOperations"`
}

// NewStruct creates a new relationship struct
func (*quoteHeaderR) NewStruct() *quoteHeaderR {
	return &quoteHeaderR{}
}

func (o *QuoteHeader) GetFromAirport() *Airport {
	if o == nil {
		return nil
	}

	return o.R.GetFromAirport()
}

func (r *quoteHeaderR) GetFromAirport() *Airport {
	if r == nil {
		return nil
	}

	return r.FromAirport
}

func (o *QuoteHeader) GetToAirport() *Airport {
	if o == nil {
		return nil
	}

	return o.R.GetToAirport()
}

func (r *quoteHeaderR) GetToAirport() *Airport {
	if r == nil {
		return nil
	}

	return r.ToAirport
}

func (o *QuoteHeader) GetFlightRequest() *FlightRequestNew {
	if o == nil {
		return nil
	}

	return o.R.GetFlightRequest()
}

func (r *quoteHeaderR) GetFlightRequest() *FlightRequestNew {
	if r == nil {
		return nil
	}

	return r.FlightRequest
}

func (o *QuoteHeader) GetQuoteOperations() QuoteOperationSlice {
	if o == nil {
		return nil
	}

	return o.R.GetQuoteOperations()
}

func (r *quoteHeaderR) GetQuoteOperations() QuoteOperationSlice {
	if r == nil {
		return nil
	}

	return r.QuoteOperations
}

// quoteHeaderL is where Load methods for each relationship are stored.
type quoteHeaderL struct{}

var (
	quoteHeaderAllColumns            = []string{"id", "flight_request_id", "created_at", "updated_at", "departs_at", "from_airport_id", "to_airport_id", "passenger", "email", "first_name", "surname", "full_name", "departs_at_timezone", "status", "quote_ops_total", "quote_ops_received", "quote_ops_rejected", "quote_ops_done"}
	quoteHeaderColumnsWithoutDefault = []string{"id", "flight_request_id", "updated_at", "departs_at", "from_airport_id", "to_airport_id", "email", "first_name", "surname", "full_name"}
	quoteHeaderColumnsWithDefault    = []string{"created_at", "passenger", "departs_at_timezone", "status", "quote_ops_total", "quote_ops_received", "quote_ops_rejected", "quote_ops_done"}
	quoteHeaderPrimaryKeyColumns     = []string{"id"}
	quoteHeaderGeneratedColumns      = []string{}
)

type (
	// QuoteHeaderSlice is an alias for a slice of pointers to QuoteHeader.
	// This should almost always be used instead of []QuoteHeader.
	QuoteHeaderSlice []*QuoteHeader
	// QuoteHeaderHook is the signature for custom QuoteHeader hook methods
	QuoteHeaderHook func(context.Context, boil.ContextExecutor, *QuoteHeader) error

	quoteHeaderQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	quoteHeaderType                 = reflect.TypeOf(&QuoteHeader{})
	quoteHeaderMapping              = queries.MakeStructMapping(quoteHeaderType)
	quoteHeaderPrimaryKeyMapping, _ = queries.BindMapping(quoteHeaderType, quoteHeaderMapping, quoteHeaderPrimaryKeyColumns)
	quoteHeaderInsertCacheMut       sync.RWMutex
	quoteHeaderInsertCache          = make(map[string]insertCache)
	quoteHeaderUpdateCacheMut       sync.RWMutex
	quoteHeaderUpdateCache          = make(map[string]updateCache)
	quoteHeaderUpsertCacheMut       sync.RWMutex
	quoteHeaderUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var quoteHeaderAfterSelectMu sync.Mutex
var quoteHeaderAfterSelectHooks []QuoteHeaderHook

var quoteHeaderBeforeInsertMu sync.Mutex
var quoteHeaderBeforeInsertHooks []QuoteHeaderHook
var quoteHeaderAfterInsertMu sync.Mutex
var quoteHeaderAfterInsertHooks []QuoteHeaderHook

var quoteHeaderBeforeUpdateMu sync.Mutex
var quoteHeaderBeforeUpdateHooks []QuoteHeaderHook
var quoteHeaderAfterUpdateMu sync.Mutex
var quoteHeaderAfterUpdateHooks []QuoteHeaderHook

var quoteHeaderBeforeDeleteMu sync.Mutex
var quoteHeaderBeforeDeleteHooks []QuoteHeaderHook
var quoteHeaderAfterDeleteMu sync.Mutex
var quoteHeaderAfterDeleteHooks []QuoteHeaderHook

var quoteHeaderBeforeUpsertMu sync.Mutex
var quoteHeaderBeforeUpsertHooks []QuoteHeaderHook
var quoteHeaderAfterUpsertMu sync.Mutex
var quoteHeaderAfterUpsertHooks []QuoteHeaderHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *QuoteHeader) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteHeaderAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *QuoteHeader) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteHeaderBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *QuoteHeader) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteHeaderAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *QuoteHeader) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteHeaderBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *QuoteHeader) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteHeaderAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *QuoteHeader) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteHeaderBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *QuoteHeader) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteHeaderAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *QuoteHeader) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteHeaderBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *QuoteHeader) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteHeaderAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddQuoteHeaderHook registers your hook function for all future operations.
func AddQuoteHeaderHook(hookPoint boil.HookPoint, quoteHeaderHook QuoteHeaderHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		quoteHeaderAfterSelectMu.Lock()
		quoteHeaderAfterSelectHooks = append(quoteHeaderAfterSelectHooks, quoteHeaderHook)
		quoteHeaderAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		quoteHeaderBeforeInsertMu.Lock()
		quoteHeaderBeforeInsertHooks = append(quoteHeaderBeforeInsertHooks, quoteHeaderHook)
		quoteHeaderBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		quoteHeaderAfterInsertMu.Lock()
		quoteHeaderAfterInsertHooks = append(quoteHeaderAfterInsertHooks, quoteHeaderHook)
		quoteHeaderAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		quoteHeaderBeforeUpdateMu.Lock()
		quoteHeaderBeforeUpdateHooks = append(quoteHeaderBeforeUpdateHooks, quoteHeaderHook)
		quoteHeaderBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		quoteHeaderAfterUpdateMu.Lock()
		quoteHeaderAfterUpdateHooks = append(quoteHeaderAfterUpdateHooks, quoteHeaderHook)
		quoteHeaderAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		quoteHeaderBeforeDeleteMu.Lock()
		quoteHeaderBeforeDeleteHooks = append(quoteHeaderBeforeDeleteHooks, quoteHeaderHook)
		quoteHeaderBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		quoteHeaderAfterDeleteMu.Lock()
		quoteHeaderAfterDeleteHooks = append(quoteHeaderAfterDeleteHooks, quoteHeaderHook)
		quoteHeaderAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		quoteHeaderBeforeUpsertMu.Lock()
		quoteHeaderBeforeUpsertHooks = append(quoteHeaderBeforeUpsertHooks, quoteHeaderHook)
		quoteHeaderBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		quoteHeaderAfterUpsertMu.Lock()
		quoteHeaderAfterUpsertHooks = append(quoteHeaderAfterUpsertHooks, quoteHeaderHook)
		quoteHeaderAfterUpsertMu.Unlock()
	}
}

// One returns a single quoteHeader record from the query.
func (q quoteHeaderQuery) One(ctx context.Context, exec boil.ContextExecutor) (*QuoteHeader, error) {
	o := &QuoteHeader{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for quote_header")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all QuoteHeader records from the query.
func (q quoteHeaderQuery) All(ctx context.Context, exec boil.ContextExecutor) (QuoteHeaderSlice, error) {
	var o []*QuoteHeader

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to QuoteHeader slice")
	}

	if len(quoteHeaderAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all QuoteHeader records in the query.
func (q quoteHeaderQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count quote_header rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q quoteHeaderQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if quote_header exists")
	}

	return count > 0, nil
}

// FromAirport pointed to by the foreign key.
func (o *QuoteHeader) FromAirport(mods ...qm.QueryMod) airportQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.FromAirportID),
	}

	queryMods = append(queryMods, mods...)

	return Airports(queryMods...)
}

// ToAirport pointed to by the foreign key.
func (o *QuoteHeader) ToAirport(mods ...qm.QueryMod) airportQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ToAirportID),
	}

	queryMods = append(queryMods, mods...)

	return Airports(queryMods...)
}

// FlightRequest pointed to by the foreign key.
func (o *QuoteHeader) FlightRequest(mods ...qm.QueryMod) flightRequestNewQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.FlightRequestID),
	}

	queryMods = append(queryMods, mods...)

	return FlightRequestNews(queryMods...)
}

// QuoteOperations retrieves all the quote_operation's QuoteOperations with an executor.
func (o *QuoteHeader) QuoteOperations(mods ...qm.QueryMod) quoteOperationQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"quote_operation\".\"quote_header_id\"=?", o.ID),
	)

	return QuoteOperations(queryMods...)
}

// LoadFromAirport allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (quoteHeaderL) LoadFromAirport(ctx context.Context, e boil.ContextExecutor, singular bool, maybeQuoteHeader interface{}, mods queries.Applicator) error {
	var slice []*QuoteHeader
	var object *QuoteHeader

	if singular {
		var ok bool
		object, ok = maybeQuoteHeader.(*QuoteHeader)
		if !ok {
			object = new(QuoteHeader)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeQuoteHeader)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeQuoteHeader))
			}
		}
	} else {
		s, ok := maybeQuoteHeader.(*[]*QuoteHeader)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeQuoteHeader)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeQuoteHeader))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &quoteHeaderR{}
		}
		args[object.FromAirportID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &quoteHeaderR{}
			}

			args[obj.FromAirportID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`airport`),
		qm.WhereIn(`airport.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Airport")
	}

	var resultSlice []*Airport
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Airport")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for airport")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for airport")
	}

	if len(airportAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.FromAirport = foreign
		if foreign.R == nil {
			foreign.R = &airportR{}
		}
		foreign.R.FromAirportQuoteHeaders = append(foreign.R.FromAirportQuoteHeaders, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.FromAirportID == foreign.ID {
				local.R.FromAirport = foreign
				if foreign.R == nil {
					foreign.R = &airportR{}
				}
				foreign.R.FromAirportQuoteHeaders = append(foreign.R.FromAirportQuoteHeaders, local)
				break
			}
		}
	}

	return nil
}

// LoadToAirport allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (quoteHeaderL) LoadToAirport(ctx context.Context, e boil.ContextExecutor, singular bool, maybeQuoteHeader interface{}, mods queries.Applicator) error {
	var slice []*QuoteHeader
	var object *QuoteHeader

	if singular {
		var ok bool
		object, ok = maybeQuoteHeader.(*QuoteHeader)
		if !ok {
			object = new(QuoteHeader)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeQuoteHeader)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeQuoteHeader))
			}
		}
	} else {
		s, ok := maybeQuoteHeader.(*[]*QuoteHeader)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeQuoteHeader)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeQuoteHeader))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &quoteHeaderR{}
		}
		args[object.ToAirportID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &quoteHeaderR{}
			}

			args[obj.ToAirportID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`airport`),
		qm.WhereIn(`airport.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Airport")
	}

	var resultSlice []*Airport
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Airport")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for airport")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for airport")
	}

	if len(airportAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ToAirport = foreign
		if foreign.R == nil {
			foreign.R = &airportR{}
		}
		foreign.R.ToAirportQuoteHeaders = append(foreign.R.ToAirportQuoteHeaders, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ToAirportID == foreign.ID {
				local.R.ToAirport = foreign
				if foreign.R == nil {
					foreign.R = &airportR{}
				}
				foreign.R.ToAirportQuoteHeaders = append(foreign.R.ToAirportQuoteHeaders, local)
				break
			}
		}
	}

	return nil
}

// LoadFlightRequest allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (quoteHeaderL) LoadFlightRequest(ctx context.Context, e boil.ContextExecutor, singular bool, maybeQuoteHeader interface{}, mods queries.Applicator) error {
	var slice []*QuoteHeader
	var object *QuoteHeader

	if singular {
		var ok bool
		object, ok = maybeQuoteHeader.(*QuoteHeader)
		if !ok {
			object = new(QuoteHeader)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeQuoteHeader)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeQuoteHeader))
			}
		}
	} else {
		s, ok := maybeQuoteHeader.(*[]*QuoteHeader)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeQuoteHeader)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeQuoteHeader))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &quoteHeaderR{}
		}
		args[object.FlightRequestID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &quoteHeaderR{}
			}

			args[obj.FlightRequestID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`flight_request_new`),
		qm.WhereIn(`flight_request_new.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load FlightRequestNew")
	}

	var resultSlice []*FlightRequestNew
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice FlightRequestNew")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for flight_request_new")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for flight_request_new")
	}

	if len(flightRequestNewAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.FlightRequest = foreign
		if foreign.R == nil {
			foreign.R = &flightRequestNewR{}
		}
		foreign.R.FlightRequestQuoteHeaders = append(foreign.R.FlightRequestQuoteHeaders, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.FlightRequestID == foreign.ID {
				local.R.FlightRequest = foreign
				if foreign.R == nil {
					foreign.R = &flightRequestNewR{}
				}
				foreign.R.FlightRequestQuoteHeaders = append(foreign.R.FlightRequestQuoteHeaders, local)
				break
			}
		}
	}

	return nil
}

// LoadQuoteOperations allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (quoteHeaderL) LoadQuoteOperations(ctx context.Context, e boil.ContextExecutor, singular bool, maybeQuoteHeader interface{}, mods queries.Applicator) error {
	var slice []*QuoteHeader
	var object *QuoteHeader

	if singular {
		var ok bool
		object, ok = maybeQuoteHeader.(*QuoteHeader)
		if !ok {
			object = new(QuoteHeader)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeQuoteHeader)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeQuoteHeader))
			}
		}
	} else {
		s, ok := maybeQuoteHeader.(*[]*QuoteHeader)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeQuoteHeader)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeQuoteHeader))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &quoteHeaderR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &quoteHeaderR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`quote_operation`),
		qm.WhereIn(`quote_operation.quote_header_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load quote_operation")
	}

	var resultSlice []*QuoteOperation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice quote_operation")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on quote_operation")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for quote_operation")
	}

	if len(quoteOperationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.QuoteOperations = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &quoteOperationR{}
			}
			foreign.R.QuoteHeader = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.QuoteHeaderID {
				local.R.QuoteOperations = append(local.R.QuoteOperations, foreign)
				if foreign.R == nil {
					foreign.R = &quoteOperationR{}
				}
				foreign.R.QuoteHeader = local
				break
			}
		}
	}

	return nil
}

// SetFromAirport of the quoteHeader to the related item.
// Sets o.R.FromAirport to related.
// Adds o to related.R.FromAirportQuoteHeaders.
func (o *QuoteHeader) SetFromAirport(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Airport) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"quote_header\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"from_airport_id"}),
		strmangle.WhereClause("\"", "\"", 2, quoteHeaderPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.FromAirportID = related.ID
	if o.R == nil {
		o.R = &quoteHeaderR{
			FromAirport: related,
		}
	} else {
		o.R.FromAirport = related
	}

	if related.R == nil {
		related.R = &airportR{
			FromAirportQuoteHeaders: QuoteHeaderSlice{o},
		}
	} else {
		related.R.FromAirportQuoteHeaders = append(related.R.FromAirportQuoteHeaders, o)
	}

	return nil
}

// SetToAirport of the quoteHeader to the related item.
// Sets o.R.ToAirport to related.
// Adds o to related.R.ToAirportQuoteHeaders.
func (o *QuoteHeader) SetToAirport(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Airport) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"quote_header\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"to_airport_id"}),
		strmangle.WhereClause("\"", "\"", 2, quoteHeaderPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ToAirportID = related.ID
	if o.R == nil {
		o.R = &quoteHeaderR{
			ToAirport: related,
		}
	} else {
		o.R.ToAirport = related
	}

	if related.R == nil {
		related.R = &airportR{
			ToAirportQuoteHeaders: QuoteHeaderSlice{o},
		}
	} else {
		related.R.ToAirportQuoteHeaders = append(related.R.ToAirportQuoteHeaders, o)
	}

	return nil
}

// SetFlightRequest of the quoteHeader to the related item.
// Sets o.R.FlightRequest to related.
// Adds o to related.R.FlightRequestQuoteHeaders.
func (o *QuoteHeader) SetFlightRequest(ctx context.Context, exec boil.ContextExecutor, insert bool, related *FlightRequestNew) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"quote_header\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"flight_request_id"}),
		strmangle.WhereClause("\"", "\"", 2, quoteHeaderPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.FlightRequestID = related.ID
	if o.R == nil {
		o.R = &quoteHeaderR{
			FlightRequest: related,
		}
	} else {
		o.R.FlightRequest = related
	}

	if related.R == nil {
		related.R = &flightRequestNewR{
			FlightRequestQuoteHeaders: QuoteHeaderSlice{o},
		}
	} else {
		related.R.FlightRequestQuoteHeaders = append(related.R.FlightRequestQuoteHeaders, o)
	}

	return nil
}

// AddQuoteOperations adds the given related objects to the existing relationships
// of the quote_header, optionally inserting them as new records.
// Appends related to o.R.QuoteOperations.
// Sets related.R.QuoteHeader appropriately.
func (o *QuoteHeader) AddQuoteOperations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*QuoteOperation) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.QuoteHeaderID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"quote_operation\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"quote_header_id"}),
				strmangle.WhereClause("\"", "\"", 2, quoteOperationPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.QuoteHeaderID = o.ID
		}
	}

	if o.R == nil {
		o.R = &quoteHeaderR{
			QuoteOperations: related,
		}
	} else {
		o.R.QuoteOperations = append(o.R.QuoteOperations, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &quoteOperationR{
				QuoteHeader: o,
			}
		} else {
			rel.R.QuoteHeader = o
		}
	}
	return nil
}

// QuoteHeaders retrieves all the records using an executor.
func QuoteHeaders(mods ...qm.QueryMod) quoteHeaderQuery {
	mods = append(mods, qm.From("\"quote_header\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"quote_header\".*"})
	}

	return quoteHeaderQuery{q}
}

// FindQuoteHeader retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindQuoteHeader(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*QuoteHeader, error) {
	quoteHeaderObj := &QuoteHeader{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"quote_header\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, quoteHeaderObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from quote_header")
	}

	if err = quoteHeaderObj.doAfterSelectHooks(ctx, exec); err != nil {
		return quoteHeaderObj, err
	}

	return quoteHeaderObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *QuoteHeader) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no quote_header provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(quoteHeaderColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	quoteHeaderInsertCacheMut.RLock()
	cache, cached := quoteHeaderInsertCache[key]
	quoteHeaderInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			quoteHeaderAllColumns,
			quoteHeaderColumnsWithDefault,
			quoteHeaderColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(quoteHeaderType, quoteHeaderMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(quoteHeaderType, quoteHeaderMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"quote_header\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"quote_header\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into quote_header")
	}

	if !cached {
		quoteHeaderInsertCacheMut.Lock()
		quoteHeaderInsertCache[key] = cache
		quoteHeaderInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the QuoteHeader.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *QuoteHeader) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	quoteHeaderUpdateCacheMut.RLock()
	cache, cached := quoteHeaderUpdateCache[key]
	quoteHeaderUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			quoteHeaderAllColumns,
			quoteHeaderPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update quote_header, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"quote_header\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, quoteHeaderPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(quoteHeaderType, quoteHeaderMapping, append(wl, quoteHeaderPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update quote_header row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for quote_header")
	}

	if !cached {
		quoteHeaderUpdateCacheMut.Lock()
		quoteHeaderUpdateCache[key] = cache
		quoteHeaderUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q quoteHeaderQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for quote_header")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for quote_header")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o QuoteHeaderSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), quoteHeaderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"quote_header\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, quoteHeaderPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in quoteHeader slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all quoteHeader")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *QuoteHeader) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no quote_header provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(quoteHeaderColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	quoteHeaderUpsertCacheMut.RLock()
	cache, cached := quoteHeaderUpsertCache[key]
	quoteHeaderUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			quoteHeaderAllColumns,
			quoteHeaderColumnsWithDefault,
			quoteHeaderColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			quoteHeaderAllColumns,
			quoteHeaderPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert quote_header, could not build update column list")
		}

		ret := strmangle.SetComplement(quoteHeaderAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(quoteHeaderPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert quote_header, could not build conflict column list")
			}

			conflict = make([]string, len(quoteHeaderPrimaryKeyColumns))
			copy(conflict, quoteHeaderPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"quote_header\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(quoteHeaderType, quoteHeaderMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(quoteHeaderType, quoteHeaderMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert quote_header")
	}

	if !cached {
		quoteHeaderUpsertCacheMut.Lock()
		quoteHeaderUpsertCache[key] = cache
		quoteHeaderUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single QuoteHeader record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *QuoteHeader) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no QuoteHeader provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), quoteHeaderPrimaryKeyMapping)
	sql := "DELETE FROM \"quote_header\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from quote_header")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for quote_header")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q quoteHeaderQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no quoteHeaderQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from quote_header")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for quote_header")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o QuoteHeaderSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(quoteHeaderBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), quoteHeaderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"quote_header\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, quoteHeaderPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from quoteHeader slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for quote_header")
	}

	if len(quoteHeaderAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *QuoteHeader) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindQuoteHeader(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *QuoteHeaderSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := QuoteHeaderSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), quoteHeaderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"quote_header\".* FROM \"quote_header\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, quoteHeaderPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in QuoteHeaderSlice")
	}

	*o = slice

	return nil
}

// QuoteHeaderExists checks if the QuoteHeader row exists.
func QuoteHeaderExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"quote_header\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if quote_header exists")
	}

	return exists, nil
}

// Exists checks if the QuoteHeader row exists.
func (o *QuoteHeader) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return QuoteHeaderExists(ctx, exec, o.ID)
}
