// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// QuoteChat is an object representing the database table.
type QuoteChat struct {
	ID        string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	QuoteID   string    `boil:"quote_id" json:"quote_id" toml:"quote_id" yaml:"quote_id"`
	Email     string    `boil:"email" json:"email" toml:"email" yaml:"email"`
	Message   string    `boil:"message" json:"message" toml:"message" yaml:"message"`

	R *quoteChatR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L quoteChatL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var QuoteChatColumns = struct {
	ID        string
	CreatedAt string
	QuoteID   string
	Email     string
	Message   string
}{
	ID:        "id",
	CreatedAt: "created_at",
	QuoteID:   "quote_id",
	Email:     "email",
	Message:   "message",
}

var QuoteChatTableColumns = struct {
	ID        string
	CreatedAt string
	QuoteID   string
	Email     string
	Message   string
}{
	ID:        "quote_chat.id",
	CreatedAt: "quote_chat.created_at",
	QuoteID:   "quote_chat.quote_id",
	Email:     "quote_chat.email",
	Message:   "quote_chat.message",
}

// Generated where

var QuoteChatWhere = struct {
	ID        whereHelperstring
	CreatedAt whereHelpertime_Time
	QuoteID   whereHelperstring
	Email     whereHelperstring
	Message   whereHelperstring
}{
	ID:        whereHelperstring{field: "\"quote_chat\".\"id\""},
	CreatedAt: whereHelpertime_Time{field: "\"quote_chat\".\"created_at\""},
	QuoteID:   whereHelperstring{field: "\"quote_chat\".\"quote_id\""},
	Email:     whereHelperstring{field: "\"quote_chat\".\"email\""},
	Message:   whereHelperstring{field: "\"quote_chat\".\"message\""},
}

// QuoteChatRels is where relationship names are stored.
var QuoteChatRels = struct {
}{}

// quoteChatR is where relationships are stored.
type quoteChatR struct {
}

// NewStruct creates a new relationship struct
func (*quoteChatR) NewStruct() *quoteChatR {
	return &quoteChatR{}
}

// quoteChatL is where Load methods for each relationship are stored.
type quoteChatL struct{}

var (
	quoteChatAllColumns            = []string{"id", "created_at", "quote_id", "email", "message"}
	quoteChatColumnsWithoutDefault = []string{"id", "quote_id", "email"}
	quoteChatColumnsWithDefault    = []string{"created_at", "message"}
	quoteChatPrimaryKeyColumns     = []string{"id"}
	quoteChatGeneratedColumns      = []string{}
)

type (
	// QuoteChatSlice is an alias for a slice of pointers to QuoteChat.
	// This should almost always be used instead of []QuoteChat.
	QuoteChatSlice []*QuoteChat
	// QuoteChatHook is the signature for custom QuoteChat hook methods
	QuoteChatHook func(context.Context, boil.ContextExecutor, *QuoteChat) error

	quoteChatQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	quoteChatType                 = reflect.TypeOf(&QuoteChat{})
	quoteChatMapping              = queries.MakeStructMapping(quoteChatType)
	quoteChatPrimaryKeyMapping, _ = queries.BindMapping(quoteChatType, quoteChatMapping, quoteChatPrimaryKeyColumns)
	quoteChatInsertCacheMut       sync.RWMutex
	quoteChatInsertCache          = make(map[string]insertCache)
	quoteChatUpdateCacheMut       sync.RWMutex
	quoteChatUpdateCache          = make(map[string]updateCache)
	quoteChatUpsertCacheMut       sync.RWMutex
	quoteChatUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var quoteChatAfterSelectMu sync.Mutex
var quoteChatAfterSelectHooks []QuoteChatHook

var quoteChatBeforeInsertMu sync.Mutex
var quoteChatBeforeInsertHooks []QuoteChatHook
var quoteChatAfterInsertMu sync.Mutex
var quoteChatAfterInsertHooks []QuoteChatHook

var quoteChatBeforeUpdateMu sync.Mutex
var quoteChatBeforeUpdateHooks []QuoteChatHook
var quoteChatAfterUpdateMu sync.Mutex
var quoteChatAfterUpdateHooks []QuoteChatHook

var quoteChatBeforeDeleteMu sync.Mutex
var quoteChatBeforeDeleteHooks []QuoteChatHook
var quoteChatAfterDeleteMu sync.Mutex
var quoteChatAfterDeleteHooks []QuoteChatHook

var quoteChatBeforeUpsertMu sync.Mutex
var quoteChatBeforeUpsertHooks []QuoteChatHook
var quoteChatAfterUpsertMu sync.Mutex
var quoteChatAfterUpsertHooks []QuoteChatHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *QuoteChat) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteChatAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *QuoteChat) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteChatBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *QuoteChat) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteChatAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *QuoteChat) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteChatBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *QuoteChat) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteChatAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *QuoteChat) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteChatBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *QuoteChat) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteChatAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *QuoteChat) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteChatBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *QuoteChat) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range quoteChatAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddQuoteChatHook registers your hook function for all future operations.
func AddQuoteChatHook(hookPoint boil.HookPoint, quoteChatHook QuoteChatHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		quoteChatAfterSelectMu.Lock()
		quoteChatAfterSelectHooks = append(quoteChatAfterSelectHooks, quoteChatHook)
		quoteChatAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		quoteChatBeforeInsertMu.Lock()
		quoteChatBeforeInsertHooks = append(quoteChatBeforeInsertHooks, quoteChatHook)
		quoteChatBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		quoteChatAfterInsertMu.Lock()
		quoteChatAfterInsertHooks = append(quoteChatAfterInsertHooks, quoteChatHook)
		quoteChatAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		quoteChatBeforeUpdateMu.Lock()
		quoteChatBeforeUpdateHooks = append(quoteChatBeforeUpdateHooks, quoteChatHook)
		quoteChatBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		quoteChatAfterUpdateMu.Lock()
		quoteChatAfterUpdateHooks = append(quoteChatAfterUpdateHooks, quoteChatHook)
		quoteChatAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		quoteChatBeforeDeleteMu.Lock()
		quoteChatBeforeDeleteHooks = append(quoteChatBeforeDeleteHooks, quoteChatHook)
		quoteChatBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		quoteChatAfterDeleteMu.Lock()
		quoteChatAfterDeleteHooks = append(quoteChatAfterDeleteHooks, quoteChatHook)
		quoteChatAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		quoteChatBeforeUpsertMu.Lock()
		quoteChatBeforeUpsertHooks = append(quoteChatBeforeUpsertHooks, quoteChatHook)
		quoteChatBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		quoteChatAfterUpsertMu.Lock()
		quoteChatAfterUpsertHooks = append(quoteChatAfterUpsertHooks, quoteChatHook)
		quoteChatAfterUpsertMu.Unlock()
	}
}

// One returns a single quoteChat record from the query.
func (q quoteChatQuery) One(ctx context.Context, exec boil.ContextExecutor) (*QuoteChat, error) {
	o := &QuoteChat{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for quote_chat")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all QuoteChat records from the query.
func (q quoteChatQuery) All(ctx context.Context, exec boil.ContextExecutor) (QuoteChatSlice, error) {
	var o []*QuoteChat

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to QuoteChat slice")
	}

	if len(quoteChatAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all QuoteChat records in the query.
func (q quoteChatQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count quote_chat rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q quoteChatQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if quote_chat exists")
	}

	return count > 0, nil
}

// QuoteChats retrieves all the records using an executor.
func QuoteChats(mods ...qm.QueryMod) quoteChatQuery {
	mods = append(mods, qm.From("\"quote_chat\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"quote_chat\".*"})
	}

	return quoteChatQuery{q}
}

// FindQuoteChat retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindQuoteChat(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*QuoteChat, error) {
	quoteChatObj := &QuoteChat{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"quote_chat\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, quoteChatObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from quote_chat")
	}

	if err = quoteChatObj.doAfterSelectHooks(ctx, exec); err != nil {
		return quoteChatObj, err
	}

	return quoteChatObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *QuoteChat) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no quote_chat provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(quoteChatColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	quoteChatInsertCacheMut.RLock()
	cache, cached := quoteChatInsertCache[key]
	quoteChatInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			quoteChatAllColumns,
			quoteChatColumnsWithDefault,
			quoteChatColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(quoteChatType, quoteChatMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(quoteChatType, quoteChatMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"quote_chat\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"quote_chat\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into quote_chat")
	}

	if !cached {
		quoteChatInsertCacheMut.Lock()
		quoteChatInsertCache[key] = cache
		quoteChatInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the QuoteChat.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *QuoteChat) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	quoteChatUpdateCacheMut.RLock()
	cache, cached := quoteChatUpdateCache[key]
	quoteChatUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			quoteChatAllColumns,
			quoteChatPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update quote_chat, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"quote_chat\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, quoteChatPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(quoteChatType, quoteChatMapping, append(wl, quoteChatPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update quote_chat row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for quote_chat")
	}

	if !cached {
		quoteChatUpdateCacheMut.Lock()
		quoteChatUpdateCache[key] = cache
		quoteChatUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q quoteChatQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for quote_chat")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for quote_chat")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o QuoteChatSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), quoteChatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"quote_chat\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, quoteChatPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in quoteChat slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all quoteChat")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *QuoteChat) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no quote_chat provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(quoteChatColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	quoteChatUpsertCacheMut.RLock()
	cache, cached := quoteChatUpsertCache[key]
	quoteChatUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			quoteChatAllColumns,
			quoteChatColumnsWithDefault,
			quoteChatColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			quoteChatAllColumns,
			quoteChatPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert quote_chat, could not build update column list")
		}

		ret := strmangle.SetComplement(quoteChatAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(quoteChatPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert quote_chat, could not build conflict column list")
			}

			conflict = make([]string, len(quoteChatPrimaryKeyColumns))
			copy(conflict, quoteChatPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"quote_chat\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(quoteChatType, quoteChatMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(quoteChatType, quoteChatMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert quote_chat")
	}

	if !cached {
		quoteChatUpsertCacheMut.Lock()
		quoteChatUpsertCache[key] = cache
		quoteChatUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single QuoteChat record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *QuoteChat) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no QuoteChat provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), quoteChatPrimaryKeyMapping)
	sql := "DELETE FROM \"quote_chat\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from quote_chat")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for quote_chat")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q quoteChatQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no quoteChatQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from quote_chat")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for quote_chat")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o QuoteChatSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(quoteChatBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), quoteChatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"quote_chat\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, quoteChatPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from quoteChat slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for quote_chat")
	}

	if len(quoteChatAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *QuoteChat) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindQuoteChat(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *QuoteChatSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := QuoteChatSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), quoteChatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"quote_chat\".* FROM \"quote_chat\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, quoteChatPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in QuoteChatSlice")
	}

	*o = slice

	return nil
}

// QuoteChatExists checks if the QuoteChat row exists.
func QuoteChatExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"quote_chat\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if quote_chat exists")
	}

	return exists, nil
}

// Exists checks if the QuoteChat row exists.
func (o *QuoteChat) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return QuoteChatExists(ctx, exec, o.ID)
}
