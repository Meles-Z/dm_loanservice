// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// RepaymentPlanInstallment is an object representing the database table.
type RepaymentPlanInstallment struct {
	ID         string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	PlanID     string      `boil:"plan_id" json:"plan_id" toml:"plan_id" yaml:"plan_id"`
	DueDate    time.Time   `boil:"due_date" json:"due_date" toml:"due_date" yaml:"due_date"`
	AmountDue  string      `boil:"amount_due" json:"amount_due" toml:"amount_due" yaml:"amount_due"`
	AmountPaid string      `boil:"amount_paid" json:"amount_paid" toml:"amount_paid" yaml:"amount_paid"`
	Status     string      `boil:"status" json:"status" toml:"status" yaml:"status"`
	PaymentID  null.String `boil:"payment_id" json:"payment_id,omitempty" toml:"payment_id" yaml:"payment_id,omitempty"`
	CreatedAt  time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt  time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *repaymentPlanInstallmentR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L repaymentPlanInstallmentL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RepaymentPlanInstallmentColumns = struct {
	ID         string
	PlanID     string
	DueDate    string
	AmountDue  string
	AmountPaid string
	Status     string
	PaymentID  string
	CreatedAt  string
	UpdatedAt  string
}{
	ID:         "id",
	PlanID:     "plan_id",
	DueDate:    "due_date",
	AmountDue:  "amount_due",
	AmountPaid: "amount_paid",
	Status:     "status",
	PaymentID:  "payment_id",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
}

var RepaymentPlanInstallmentTableColumns = struct {
	ID         string
	PlanID     string
	DueDate    string
	AmountDue  string
	AmountPaid string
	Status     string
	PaymentID  string
	CreatedAt  string
	UpdatedAt  string
}{
	ID:         "repayment_plan_installments.id",
	PlanID:     "repayment_plan_installments.plan_id",
	DueDate:    "repayment_plan_installments.due_date",
	AmountDue:  "repayment_plan_installments.amount_due",
	AmountPaid: "repayment_plan_installments.amount_paid",
	Status:     "repayment_plan_installments.status",
	PaymentID:  "repayment_plan_installments.payment_id",
	CreatedAt:  "repayment_plan_installments.created_at",
	UpdatedAt:  "repayment_plan_installments.updated_at",
}

// Generated where

var RepaymentPlanInstallmentWhere = struct {
	ID         whereHelperstring
	PlanID     whereHelperstring
	DueDate    whereHelpertime_Time
	AmountDue  whereHelperstring
	AmountPaid whereHelperstring
	Status     whereHelperstring
	PaymentID  whereHelpernull_String
	CreatedAt  whereHelpertime_Time
	UpdatedAt  whereHelpertime_Time
}{
	ID:         whereHelperstring{field: "\"repayment_plan_installments\".\"id\""},
	PlanID:     whereHelperstring{field: "\"repayment_plan_installments\".\"plan_id\""},
	DueDate:    whereHelpertime_Time{field: "\"repayment_plan_installments\".\"due_date\""},
	AmountDue:  whereHelperstring{field: "\"repayment_plan_installments\".\"amount_due\""},
	AmountPaid: whereHelperstring{field: "\"repayment_plan_installments\".\"amount_paid\""},
	Status:     whereHelperstring{field: "\"repayment_plan_installments\".\"status\""},
	PaymentID:  whereHelpernull_String{field: "\"repayment_plan_installments\".\"payment_id\""},
	CreatedAt:  whereHelpertime_Time{field: "\"repayment_plan_installments\".\"created_at\""},
	UpdatedAt:  whereHelpertime_Time{field: "\"repayment_plan_installments\".\"updated_at\""},
}

// RepaymentPlanInstallmentRels is where relationship names are stored.
var RepaymentPlanInstallmentRels = struct {
	Plan string
}{
	Plan: "Plan",
}

// repaymentPlanInstallmentR is where relationships are stored.
type repaymentPlanInstallmentR struct {
	Plan *RepaymentPlan `boil:"Plan" json:"Plan" toml:"Plan" yaml:"Plan"`
}

// NewStruct creates a new relationship struct
func (*repaymentPlanInstallmentR) NewStruct() *repaymentPlanInstallmentR {
	return &repaymentPlanInstallmentR{}
}

func (o *RepaymentPlanInstallment) GetPlan() *RepaymentPlan {
	if o == nil {
		return nil
	}

	return o.R.GetPlan()
}

func (r *repaymentPlanInstallmentR) GetPlan() *RepaymentPlan {
	if r == nil {
		return nil
	}

	return r.Plan
}

// repaymentPlanInstallmentL is where Load methods for each relationship are stored.
type repaymentPlanInstallmentL struct{}

var (
	repaymentPlanInstallmentAllColumns            = []string{"id", "plan_id", "due_date", "amount_due", "amount_paid", "status", "payment_id", "created_at", "updated_at"}
	repaymentPlanInstallmentColumnsWithoutDefault = []string{"id", "plan_id", "due_date", "amount_due"}
	repaymentPlanInstallmentColumnsWithDefault    = []string{"amount_paid", "status", "payment_id", "created_at", "updated_at"}
	repaymentPlanInstallmentPrimaryKeyColumns     = []string{"id"}
	repaymentPlanInstallmentGeneratedColumns      = []string{}
)

type (
	// RepaymentPlanInstallmentSlice is an alias for a slice of pointers to RepaymentPlanInstallment.
	// This should almost always be used instead of []RepaymentPlanInstallment.
	RepaymentPlanInstallmentSlice []*RepaymentPlanInstallment
	// RepaymentPlanInstallmentHook is the signature for custom RepaymentPlanInstallment hook methods
	RepaymentPlanInstallmentHook func(context.Context, boil.ContextExecutor, *RepaymentPlanInstallment) error

	repaymentPlanInstallmentQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	repaymentPlanInstallmentType                 = reflect.TypeOf(&RepaymentPlanInstallment{})
	repaymentPlanInstallmentMapping              = queries.MakeStructMapping(repaymentPlanInstallmentType)
	repaymentPlanInstallmentPrimaryKeyMapping, _ = queries.BindMapping(repaymentPlanInstallmentType, repaymentPlanInstallmentMapping, repaymentPlanInstallmentPrimaryKeyColumns)
	repaymentPlanInstallmentInsertCacheMut       sync.RWMutex
	repaymentPlanInstallmentInsertCache          = make(map[string]insertCache)
	repaymentPlanInstallmentUpdateCacheMut       sync.RWMutex
	repaymentPlanInstallmentUpdateCache          = make(map[string]updateCache)
	repaymentPlanInstallmentUpsertCacheMut       sync.RWMutex
	repaymentPlanInstallmentUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var repaymentPlanInstallmentAfterSelectMu sync.Mutex
var repaymentPlanInstallmentAfterSelectHooks []RepaymentPlanInstallmentHook

var repaymentPlanInstallmentBeforeInsertMu sync.Mutex
var repaymentPlanInstallmentBeforeInsertHooks []RepaymentPlanInstallmentHook
var repaymentPlanInstallmentAfterInsertMu sync.Mutex
var repaymentPlanInstallmentAfterInsertHooks []RepaymentPlanInstallmentHook

var repaymentPlanInstallmentBeforeUpdateMu sync.Mutex
var repaymentPlanInstallmentBeforeUpdateHooks []RepaymentPlanInstallmentHook
var repaymentPlanInstallmentAfterUpdateMu sync.Mutex
var repaymentPlanInstallmentAfterUpdateHooks []RepaymentPlanInstallmentHook

var repaymentPlanInstallmentBeforeDeleteMu sync.Mutex
var repaymentPlanInstallmentBeforeDeleteHooks []RepaymentPlanInstallmentHook
var repaymentPlanInstallmentAfterDeleteMu sync.Mutex
var repaymentPlanInstallmentAfterDeleteHooks []RepaymentPlanInstallmentHook

var repaymentPlanInstallmentBeforeUpsertMu sync.Mutex
var repaymentPlanInstallmentBeforeUpsertHooks []RepaymentPlanInstallmentHook
var repaymentPlanInstallmentAfterUpsertMu sync.Mutex
var repaymentPlanInstallmentAfterUpsertHooks []RepaymentPlanInstallmentHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RepaymentPlanInstallment) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanInstallmentAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RepaymentPlanInstallment) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanInstallmentBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RepaymentPlanInstallment) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanInstallmentAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RepaymentPlanInstallment) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanInstallmentBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RepaymentPlanInstallment) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanInstallmentAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RepaymentPlanInstallment) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanInstallmentBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RepaymentPlanInstallment) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanInstallmentAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RepaymentPlanInstallment) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanInstallmentBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RepaymentPlanInstallment) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range repaymentPlanInstallmentAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRepaymentPlanInstallmentHook registers your hook function for all future operations.
func AddRepaymentPlanInstallmentHook(hookPoint boil.HookPoint, repaymentPlanInstallmentHook RepaymentPlanInstallmentHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		repaymentPlanInstallmentAfterSelectMu.Lock()
		repaymentPlanInstallmentAfterSelectHooks = append(repaymentPlanInstallmentAfterSelectHooks, repaymentPlanInstallmentHook)
		repaymentPlanInstallmentAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		repaymentPlanInstallmentBeforeInsertMu.Lock()
		repaymentPlanInstallmentBeforeInsertHooks = append(repaymentPlanInstallmentBeforeInsertHooks, repaymentPlanInstallmentHook)
		repaymentPlanInstallmentBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		repaymentPlanInstallmentAfterInsertMu.Lock()
		repaymentPlanInstallmentAfterInsertHooks = append(repaymentPlanInstallmentAfterInsertHooks, repaymentPlanInstallmentHook)
		repaymentPlanInstallmentAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		repaymentPlanInstallmentBeforeUpdateMu.Lock()
		repaymentPlanInstallmentBeforeUpdateHooks = append(repaymentPlanInstallmentBeforeUpdateHooks, repaymentPlanInstallmentHook)
		repaymentPlanInstallmentBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		repaymentPlanInstallmentAfterUpdateMu.Lock()
		repaymentPlanInstallmentAfterUpdateHooks = append(repaymentPlanInstallmentAfterUpdateHooks, repaymentPlanInstallmentHook)
		repaymentPlanInstallmentAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		repaymentPlanInstallmentBeforeDeleteMu.Lock()
		repaymentPlanInstallmentBeforeDeleteHooks = append(repaymentPlanInstallmentBeforeDeleteHooks, repaymentPlanInstallmentHook)
		repaymentPlanInstallmentBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		repaymentPlanInstallmentAfterDeleteMu.Lock()
		repaymentPlanInstallmentAfterDeleteHooks = append(repaymentPlanInstallmentAfterDeleteHooks, repaymentPlanInstallmentHook)
		repaymentPlanInstallmentAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		repaymentPlanInstallmentBeforeUpsertMu.Lock()
		repaymentPlanInstallmentBeforeUpsertHooks = append(repaymentPlanInstallmentBeforeUpsertHooks, repaymentPlanInstallmentHook)
		repaymentPlanInstallmentBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		repaymentPlanInstallmentAfterUpsertMu.Lock()
		repaymentPlanInstallmentAfterUpsertHooks = append(repaymentPlanInstallmentAfterUpsertHooks, repaymentPlanInstallmentHook)
		repaymentPlanInstallmentAfterUpsertMu.Unlock()
	}
}

// One returns a single repaymentPlanInstallment record from the query.
func (q repaymentPlanInstallmentQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RepaymentPlanInstallment, error) {
	o := &RepaymentPlanInstallment{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for repayment_plan_installments")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RepaymentPlanInstallment records from the query.
func (q repaymentPlanInstallmentQuery) All(ctx context.Context, exec boil.ContextExecutor) (RepaymentPlanInstallmentSlice, error) {
	var o []*RepaymentPlanInstallment

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to RepaymentPlanInstallment slice")
	}

	if len(repaymentPlanInstallmentAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RepaymentPlanInstallment records in the query.
func (q repaymentPlanInstallmentQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count repayment_plan_installments rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q repaymentPlanInstallmentQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if repayment_plan_installments exists")
	}

	return count > 0, nil
}

// Plan pointed to by the foreign key.
func (o *RepaymentPlanInstallment) Plan(mods ...qm.QueryMod) repaymentPlanQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PlanID),
	}

	queryMods = append(queryMods, mods...)

	return RepaymentPlans(queryMods...)
}

// LoadPlan allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (repaymentPlanInstallmentL) LoadPlan(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRepaymentPlanInstallment interface{}, mods queries.Applicator) error {
	var slice []*RepaymentPlanInstallment
	var object *RepaymentPlanInstallment

	if singular {
		var ok bool
		object, ok = maybeRepaymentPlanInstallment.(*RepaymentPlanInstallment)
		if !ok {
			object = new(RepaymentPlanInstallment)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRepaymentPlanInstallment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRepaymentPlanInstallment))
			}
		}
	} else {
		s, ok := maybeRepaymentPlanInstallment.(*[]*RepaymentPlanInstallment)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRepaymentPlanInstallment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRepaymentPlanInstallment))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &repaymentPlanInstallmentR{}
		}
		args[object.PlanID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &repaymentPlanInstallmentR{}
			}

			args[obj.PlanID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`repayment_plans`),
		qm.WhereIn(`repayment_plans.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RepaymentPlan")
	}

	var resultSlice []*RepaymentPlan
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RepaymentPlan")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for repayment_plans")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for repayment_plans")
	}

	if len(repaymentPlanAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Plan = foreign
		if foreign.R == nil {
			foreign.R = &repaymentPlanR{}
		}
		foreign.R.PlanRepaymentPlanInstallments = append(foreign.R.PlanRepaymentPlanInstallments, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PlanID == foreign.ID {
				local.R.Plan = foreign
				if foreign.R == nil {
					foreign.R = &repaymentPlanR{}
				}
				foreign.R.PlanRepaymentPlanInstallments = append(foreign.R.PlanRepaymentPlanInstallments, local)
				break
			}
		}
	}

	return nil
}

// SetPlan of the repaymentPlanInstallment to the related item.
// Sets o.R.Plan to related.
// Adds o to related.R.PlanRepaymentPlanInstallments.
func (o *RepaymentPlanInstallment) SetPlan(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RepaymentPlan) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"repayment_plan_installments\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"plan_id"}),
		strmangle.WhereClause("\"", "\"", 2, repaymentPlanInstallmentPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PlanID = related.ID
	if o.R == nil {
		o.R = &repaymentPlanInstallmentR{
			Plan: related,
		}
	} else {
		o.R.Plan = related
	}

	if related.R == nil {
		related.R = &repaymentPlanR{
			PlanRepaymentPlanInstallments: RepaymentPlanInstallmentSlice{o},
		}
	} else {
		related.R.PlanRepaymentPlanInstallments = append(related.R.PlanRepaymentPlanInstallments, o)
	}

	return nil
}

// RepaymentPlanInstallments retrieves all the records using an executor.
func RepaymentPlanInstallments(mods ...qm.QueryMod) repaymentPlanInstallmentQuery {
	mods = append(mods, qm.From("\"repayment_plan_installments\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"repayment_plan_installments\".*"})
	}

	return repaymentPlanInstallmentQuery{q}
}

// FindRepaymentPlanInstallment retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRepaymentPlanInstallment(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*RepaymentPlanInstallment, error) {
	repaymentPlanInstallmentObj := &RepaymentPlanInstallment{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"repayment_plan_installments\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, repaymentPlanInstallmentObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from repayment_plan_installments")
	}

	if err = repaymentPlanInstallmentObj.doAfterSelectHooks(ctx, exec); err != nil {
		return repaymentPlanInstallmentObj, err
	}

	return repaymentPlanInstallmentObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RepaymentPlanInstallment) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no repayment_plan_installments provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(repaymentPlanInstallmentColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	repaymentPlanInstallmentInsertCacheMut.RLock()
	cache, cached := repaymentPlanInstallmentInsertCache[key]
	repaymentPlanInstallmentInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			repaymentPlanInstallmentAllColumns,
			repaymentPlanInstallmentColumnsWithDefault,
			repaymentPlanInstallmentColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(repaymentPlanInstallmentType, repaymentPlanInstallmentMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(repaymentPlanInstallmentType, repaymentPlanInstallmentMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"repayment_plan_installments\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"repayment_plan_installments\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into repayment_plan_installments")
	}

	if !cached {
		repaymentPlanInstallmentInsertCacheMut.Lock()
		repaymentPlanInstallmentInsertCache[key] = cache
		repaymentPlanInstallmentInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RepaymentPlanInstallment.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RepaymentPlanInstallment) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	repaymentPlanInstallmentUpdateCacheMut.RLock()
	cache, cached := repaymentPlanInstallmentUpdateCache[key]
	repaymentPlanInstallmentUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			repaymentPlanInstallmentAllColumns,
			repaymentPlanInstallmentPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update repayment_plan_installments, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"repayment_plan_installments\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, repaymentPlanInstallmentPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(repaymentPlanInstallmentType, repaymentPlanInstallmentMapping, append(wl, repaymentPlanInstallmentPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update repayment_plan_installments row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for repayment_plan_installments")
	}

	if !cached {
		repaymentPlanInstallmentUpdateCacheMut.Lock()
		repaymentPlanInstallmentUpdateCache[key] = cache
		repaymentPlanInstallmentUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q repaymentPlanInstallmentQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for repayment_plan_installments")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for repayment_plan_installments")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RepaymentPlanInstallmentSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), repaymentPlanInstallmentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"repayment_plan_installments\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, repaymentPlanInstallmentPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in repaymentPlanInstallment slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all repaymentPlanInstallment")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RepaymentPlanInstallment) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodels: no repayment_plan_installments provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(repaymentPlanInstallmentColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	repaymentPlanInstallmentUpsertCacheMut.RLock()
	cache, cached := repaymentPlanInstallmentUpsertCache[key]
	repaymentPlanInstallmentUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			repaymentPlanInstallmentAllColumns,
			repaymentPlanInstallmentColumnsWithDefault,
			repaymentPlanInstallmentColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			repaymentPlanInstallmentAllColumns,
			repaymentPlanInstallmentPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert repayment_plan_installments, could not build update column list")
		}

		ret := strmangle.SetComplement(repaymentPlanInstallmentAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(repaymentPlanInstallmentPrimaryKeyColumns) == 0 {
				return errors.New("dbmodels: unable to upsert repayment_plan_installments, could not build conflict column list")
			}

			conflict = make([]string, len(repaymentPlanInstallmentPrimaryKeyColumns))
			copy(conflict, repaymentPlanInstallmentPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"repayment_plan_installments\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(repaymentPlanInstallmentType, repaymentPlanInstallmentMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(repaymentPlanInstallmentType, repaymentPlanInstallmentMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert repayment_plan_installments")
	}

	if !cached {
		repaymentPlanInstallmentUpsertCacheMut.Lock()
		repaymentPlanInstallmentUpsertCache[key] = cache
		repaymentPlanInstallmentUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RepaymentPlanInstallment record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RepaymentPlanInstallment) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no RepaymentPlanInstallment provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), repaymentPlanInstallmentPrimaryKeyMapping)
	sql := "DELETE FROM \"repayment_plan_installments\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from repayment_plan_installments")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for repayment_plan_installments")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q repaymentPlanInstallmentQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no repaymentPlanInstallmentQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from repayment_plan_installments")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for repayment_plan_installments")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RepaymentPlanInstallmentSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(repaymentPlanInstallmentBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), repaymentPlanInstallmentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"repayment_plan_installments\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, repaymentPlanInstallmentPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from repaymentPlanInstallment slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for repayment_plan_installments")
	}

	if len(repaymentPlanInstallmentAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RepaymentPlanInstallment) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRepaymentPlanInstallment(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RepaymentPlanInstallmentSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RepaymentPlanInstallmentSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), repaymentPlanInstallmentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"repayment_plan_installments\".* FROM \"repayment_plan_installments\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, repaymentPlanInstallmentPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in RepaymentPlanInstallmentSlice")
	}

	*o = slice

	return nil
}

// RepaymentPlanInstallmentExists checks if the RepaymentPlanInstallment row exists.
func RepaymentPlanInstallmentExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"repayment_plan_installments\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if repayment_plan_installments exists")
	}

	return exists, nil
}

// Exists checks if the RepaymentPlanInstallment row exists.
func (o *RepaymentPlanInstallment) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return RepaymentPlanInstallmentExists(ctx, exec, o.ID)
}
